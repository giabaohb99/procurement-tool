/** Loại con dấu — danh mục nền của phân hệ Duyệt dấu (khớp `SealType` backend). */
export type SealType = {
  id: number
  name: string
  description: string
  is_active: boolean
  created_at?: string
  updated_at?: string
}
