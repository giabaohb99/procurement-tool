# NHẬT KÝ THAY ĐỔI — DẢI `bao`

Nhánh riêng của [`change-log.md`](change-log.md), tách ngày **07/09/2026**.

**Vì sao tách.** Luật tiền tố tên người (03/09) đã hết trùng SỐ, nhưng vẫn còn trùng CHỖ:
ai cũng chèn dòng mới vào **đúng dòng đầu bảng** của một tệp duy nhất, nên gần như lần
merge nào cũng dính xung đột ở đó — sửa tay không khó nhưng lặp lại mỗi ngày, và nguy hiểm
ở chỗ giải sai một phát là **nuốt mất dòng CR của người khác** mà không ai biết. Tách tệp
thì hai người ghi hai chỗ, git tự merge, không còn phải đụng tay.

**Tệp này chứa gì.** Mọi CR mang tiền tố `bao-` **từ bao-CR-310 trở đi**, và nhật ký deploy
của các CR đó.

**Tệp này KHÔNG chứa gì.** Ba thứ vẫn nằm ở `change-log.md` chung, cố ý:

- **CR cũ** (`CR-001`…`CR-271` và mọi `bao-CR-*` trước 310) — không dời sang đây. Dời là đẻ
  một diff khổng lồ, mà vẫn phải đọc hai tệp như cũ, chẳng được gì.
- **Việc còn nợ `N-xxx`** — đánh số KHÔNG tiền tố và được trích dẫn thẳng trong bình luận
  mã nguồn (`xem N-004`). Chẻ đôi là đẻ hai không gian số cho cùng một cái tên.
- **Quyết định `D-xxx` / `QĐ-x`** — cùng lý do, và cả đội đọc chung một danh sách.

Ba thứ đó hiếm khi thêm, nên xung đột không đáng kể.

---

## Luật ghi (giữ nguyên như tệp chung)

**Trạng thái:** `Đề xuất` → `Đã duyệt` → `Đang làm` → `Hoàn tất` (hoặc `Từ chối` / `Hoãn`).

**ID đầy đủ = `bao-CR-<số>-<slug>`**, slug kebab 3-5 chữ tóm tắt việc.

**ĐẶT CHỖ NGAY KHI NHẬN VIỆC** — cấp số xong là ghi ngay một dòng trạng thái `Đang làm`
(một câu là đủ) RỒI mới bắt tay code. Xong việc quay lại điền đủ; bỏ giữa chừng thì xóa
dòng đặt chỗ.

⚠️ **CẤP SỐ MỚI PHẢI GREP CẢ HAI TỆP.** Dải số là **chung cho cả đội**, không phải mỗi
người một dải — tách tệp chỉ để hết đụng chỗ ghi, không đổi cách đánh số:

```bash
grep -oh "CR-[0-9]\+" doc/tai-lieu-ky-thuat/change-log.md doc/tai-lieu-ky-thuat/change-log-bao.md | sort -t- -k2 -n | tail -3
```

Lỡ vẫn trùng số với người khác thì **kệ** — hai dòng cùng số khác slug là hợp lệ.

---

| CR | Ngày | Người đề xuất | Nội dung | Ảnh hưởng scope | Trạng thái | Tài liệu liên quan |
|---|---|---|---|---|---|---|
| **bao-CR-372-phan-loai-ngoai-danh-muc-ycmh** | 2026-09-11 | Khách (lỗi prod) | **[frontend] Màn Yêu cầu mua hàng: bảng dòng bỏ trắng ô Phân loại trong khi popup Chi tiết dòng vẫn hiện `ICARE`.** Khách chụp hai màn hình cùng một dòng hàng: ngoài bảng là `-- Phân loại --`, mở popup ra thì thấy `ICARE`. **Nguyên nhân:** ngoài bảng dùng thẻ `<select>` gốc, thẻ này chỉ vẽ được giá trị nào khớp TUYỆT ĐỐI với một `<option>`; `SearchSelect` trong popup thì cố ý làm ngược lại — giá trị lạ vẫn hiện chữ. Mà `tab_product.item_group` là **chuỗi tự do** (`String(50)`, không khóa ngoại sang `tab_item_group`) và schema backend cũng không kiểm, nên sản phẩm mang phân loại ngoài danh mục là chuyện bình thường. Đo trên hệ thật: prod **678/6877 sản phẩm** mang 11 giá trị ngoài danh mục (`ICARE 302 · Chai 269 · Can 34 · Bao 15 · Túi 14 · NDT 13 · Cóc 10 · Xô 9 · Tem 8 · Bồn 3 · Vật tư 1`), dev 685/6803 với 12 giá trị. Danh mục có `NL Icare` nhưng không có `ICARE` trần. **Hệ quả im lặng, đây mới là phần đáng sợ:** ô trắng nhưng state React vẫn giữ `ICARE`, người dùng lưu phiếu là giá trị đó xuống thẳng CSDL — rồi phân công tự động theo phân loại (`purchase_request/service.py`) và ngày quy định theo số ngày của phân loại đều trượt, không báo gì. **Sửa:** bê nguyên khuôn **CR-083** đã làm ở Đơn mua hàng sang `PurchaseRequestDetail.tsx` — `canonGroup` (khớp bỏ hoa/thường, trả về đúng cách viết của danh mục), `isGroupOutOfCatalog`, `groupOptions`; giá trị ngoài danh mục được chèn thành một `<option>` riêng dán nhãn `(ngoài danh mục)` ở cả bảng lẫn popup. Chặn thêm: `groups` rỗng nghĩa là **danh mục chưa tải xong**, không phải "ngoài danh mục" — không chặn thì lần vẽ đầu của mọi phiếu cũ đều gắn nhãn sai rồi mới tự sửa, người dùng kịp đọc. `groupDesc` cũng đổi sang khớp bỏ hoa/thường cho khớp `stdDaysMap`. | Chỉ `frontend/src/pages/PurchaseRequestDetail.tsx`. Không đụng CSDL, không migration, không đụng API — chỉ rebuild `web`. Bản **v2 còn nặng hơn** (popup chi tiết dòng đang là ô nhập chữ tự do), port sau | Hoàn tất — deploy **prod** + **dev** 11/09/2026 | CR-083 (cùng lỗi ở Đơn mua hàng) · `doc/tai-lieu-ky-thuat/mo-hinh-du-lieu-san-pham.md` |
| **bao-CR-371-pham-vi-proc-het-vong-doi** | 2026-09-11 | Khách (lỗi prod) | **[backend] Phạm vi "Thu mua (được giao + đã duyệt)" mất phiếu khi phiếu chạy tiếp.** Hiện tượng: tài khoản Admin thu mua đặt phạm vi `proc` ở Yêu cầu mua hàng, mở `PYC08092605` (trạng thái `purchased`) thì ra "Không tìm thấy phiếu ... hoặc bạn không có quyền truy cập"; khách phải đổi sang phạm vi *Tất cả* mới xem được. Nguyên nhân: `_role_scope_cond` trong `core/scoping.py` liệt kê TAY `["approved","dispatched"]`, nên phiếu vừa sang `processing` / `purchasing` / `purchased` / `completed` là rơi khỏi phạm vi — trừ khi người đó tình cờ là người tạo, người yêu cầu, người được gán, hoặc có dòng mang mã mình. Đúng những phiếu thu mua đang làm dở thì họ mất quyền xem. Sửa: khai bộ trạng thái MỘT CHỖ ở `purchase_request/model.py` (`STATUS_AFTER_APPROVE` / `STATUS_AFTER_DISPATCH`) rồi cho cả `core/scoping.py` lẫn chữ ký bản in ở `purchase_request/controller.py` cùng đọc — hai nơi trước đây gõ tay hai bản khác nhau, đó mới là gốc của lỗi. **Đơn mua hàng dính y hệt**: nhánh `proc` so `status == "approved"` nên nhận hàng xong (`partial` / `received` / `completed`) là đơn biến mất; thêm `STATUS_AFTER_APPROVE` ở `purchase_order/model.py`. Phiếu **chưa duyệt** (nháp / chờ duyệt / bị trả) vẫn KHÔNG lọt vào phạm vi này — có test canh. Test: `test/backend/test_pham_vi_thu_mua_cr371.py` (3 ca) + `test_dispatch.py` 35 ca cũ xanh. | **CÓ — nới phạm vi đọc** của grant `proc` trên `purchase_request` + `purchase_order`. Không đụng quyền hành động, không đụng phạm vi khác. Sau khi deploy, tài khoản Admin thu mua đang để tạm *Tất cả* trả lại được về *Thu mua*. | Hoàn tất — deploy **prod** 11/09/2026 (`c417bf64`, rebuild api + celery-worker + celery-beat, không có migration) và **dev** cùng ngày (`1f3c7685` trên `erp-v2`). Chờ nghiệm thu: đổi phạm vi Yêu cầu mua hàng của tài khoản `phuchau2004.degoholding@gmail.com` từ *Tất cả* về *Thu mua (được giao + đã duyệt)* rồi mở lại `PYC08092605`. | doc/phan-quyen/Thiet_Ke_Phan_Quyen.md |
| **bao-CR-368-nhan-su-trung-email** | 2026-09-11 | Khách | **[backend] Hai hồ sơ nhân sự dùng chung một email vẫn lưu được, mà email chính là khoá đăng nhập.** Khách chụp màn Nhân sự: hai dòng cùng tên *Danh Hoàng Nguyên*, cùng `dhnguyen.icare@gmail.com` — **NSU089** (ID 111, vị trí *Nhân sự*, tạo 01/07) và **NSU238** (ID 286, vị trí *Thủ kho*, tạo 11/09 11:56), rồi hỏi *"vậy thì user đăng nhập vào sẽ vào đâu"*. **Trả lời: vào NSU089.** Chỉ có một tài khoản mang email này (user 120 → nhân sự 111); NSU238 không có tài khoản nào. Đăng nhập mật khẩu tra `User.email`, đăng nhập Google tra `Employee.email` rồi `.first()` — cả hai đường đều rơi vào hồ sơ 111, nên vị trí *Thủ kho* vừa đặt **không có tác dụng gì**. Ai rơi vào hồ sơ nào là do thứ tự bản ghi quyết định chứ không phải do ai chọn. **Nguyên nhân:** `create_employee` chỉ kiểm trùng **mã nhân viên**, email thả nổi hoàn toàn — không có lỗi nào được ném ra nên cũng chẳng có gì để hoàn tác; hệ thống nhận bản ghi thứ hai một cách bình thường. Nhật ký cho thấy đúng vệt thao tác: 04:53–04:55 sửa ba hồ sơ, **xoá** hồ sơ 284, rồi 04:56:14 **tạo** 286. **Nhưng câu hỏi của khách lại lộ ra một lỗi THẬT ở cửa bên cạnh:** `update_employee` `db.commit()` hồ sơ **trước**, xong mới gọi `_sync_user_email_from_employee` — hàm này có thể ném 400 *"Email này đã được một tài khoản khác sử dụng"*, lúc đó người dùng thấy báo đỏ mà hồ sơ thì đã đổi rồi. Đó mới đúng là *"lỗi mà không hoàn tác"*. **Việc làm:** (1) thêm `ensure_email_unique` chặn trùng email ở **cả tạo mới lẫn sửa**, so sánh đã bỏ hoa/thường và cắt khoảng trắng (`A@X.com ` = `a@x.com`), email **rỗng không tính trùng** vì nhiều nhân sự chưa có email và họ đăng nhập bằng mã NV; thông báo chỉ rõ email đang thuộc về ai. (2) Gom `update_employee` về **một giao dịch**: kiểm hết · gán hết · commit một lần, vướng chỗ nào thì `rollback` trả cả lần lưu về nguyên trạng; bỏ `db.commit()` lẻ trong `_sync_user_email_from_employee`. (3) Bịt luôn cửa **nhập CSV** — đường này trước giờ ghi email không kiểm gì cả; dòng trùng bị bỏ qua và báo số lượng ở câu kết chứ không huỷ cả tệp. **Dữ liệu:** rà toàn bộ 236 nhân sự chỉ có **đúng một cặp trùng**; NSU238 không bị bảng nào tham chiếu (YCMH, YCKS, phiếu hỗ trợ, phân công danh mục, tài khoản đều 0) nên **đã xoá** theo chốt của khách. | `backend/app/modules/employee/service.py` · `controller.py` (nhập CSV). **Không** migration. CSDL prod: xoá 1 dòng `tab_employee` (ID 286). `seed.py`/`seed_prod.py` dựng `Employee` thẳng từ model nên không đi qua luật mới | **Hoàn tất** — dữ liệu đã xử lý prod 11/09/2026, mã nguồn chờ deploy | `test/backend/test_employee_email_trung_cr368.py` (7 ca) · CR-023 (tài khoản mồ côi) |
| **bao-CR-367-ngay-hoa-don-tu-nhay** | 2026-09-11 | Thu mua (ticket TK10092603) | **[frontend + backend] Ngày hóa đơn trên ĐMH tự nhảy thành ngày hôm nay mỗi lần lưu đơn.** Chị Lâm Bích Dư báo: *"Ngày hóa đơn đã chỉnh đúng cho 4 dòng là 9/9. Khi lưu thì nó lấy ngày hiện tại 10/9."* Bên thu mua còn nói ngày đó *"hệ thống tự cập nhật nhiều lắm"*. **Nguyên nhân là HAI chỗ cộng lại.** (1) `PurchaseOrderDetail.tsx` khi dựng dữ liệu gửi lên cho bảng *Giao hàng nhiều đợt* liệt kê 17 trường nhưng **quên đúng `invoice_date`** — hai bảng kia (chi phí, dòng hàng) đều có, nên lỗi nằm im rất lâu. Ngày người dùng gõ không bao giờ rời khỏi trình duyệt. (2) `_save_deliveries` nhận ô rỗng, tưởng người dùng bỏ trống, rồi chạy `if inv_no and not inv_date: inv_date = date.today()`. Hai thứ ghép lại thành: **mỗi lần lưu một ĐMH là mọi dòng giao hàng có số hóa đơn bị dập ngày = ngày hôm đó**, im lặng, không báo gì. **Bằng chứng trên hệ thật:** 96 dòng `tab_po_delivery` mang số hóa đơn thì **90 dòng** có `invoice_date` trùng khít `DATE(updated_at)`; nhóm theo ngày tháng 9 khớp 100% không sót ca nào. PO00103 có đúng 4 dòng (số HĐ 5017), cả 4 mang ngày 10/9 giờ lưu 10:36–10:37 — khớp từng chữ với ticket. Ngày sai còn chảy tiếp sang YCTT qua `delivery_invoice_date`. **Việc làm:** thêm `invoice_date` vào danh sách trường gửi lên; **bỏ hẳn** nhịp tự gán `date.today()` ở cả `_save_deliveries` lẫn `_save_items`; bỏ luôn đoạn giao diện tự điền ngày hôm nay khi gõ số hóa đơn. Lý do bỏ chứ không vá: đoán hộ biến *"thiếu dữ liệu"* thành *"dữ liệu sai"* — trống thì người dùng còn nhìn ra mà điền, điền bừa thì không ai biết đó là ngày bịa. **Không dựng được đường khôi phục tự động:** `tab_audit_log` để trống cả `message` lẫn `changed_fields` nên ngày gốc mất hẳn; chỉ 23 cặp (mã ĐMH + số HĐ) còn dấu vết trong `tab_payment_request_line`. Khách chốt **để thu mua nhập lại tay**. | `frontend/src/pages/PurchaseOrderDetail.tsx` · `backend/app/modules/purchase_order/service.py` + `model.py` (bình luận). **Không** đụng CSDL, không migration. Bản **v2 không dính lỗi** — `purchase-order-draft.ts` gửi nguyên cả cục `deliveries` nên `invoice_date` đi đủ | **Hoàn tất** — deploy prod 11/09/2026 | Ticket TK10092603 · N-017 (dữ liệu cũ đã hỏng, thu mua nhập lại tay) |
| **bao-CR-366-tra-pill-ve-nen-nhat** | 2026-09-11 | Khách | **[frontend] Bỏ bản pill tô đặc của `bao-CR-363` đợt 2, trả cả bộ về nền nhạt + chữ cùng tông như bản cũ; chỉ GIỮ phần thêm màu cho các mốc mới.** Khách xem bản vừa deploy và bảo *"màu trên quá xấu, mình không làm như bản cũ được hả, rồi thêm các màu trên trạng thái mới thôi"*. Đợt 2 tô đặc cả mười lớp pill để chữa lời than *"màu hơi nhợt nhạt"*, nhưng đổi hẳn công thức thì mỗi dòng danh sách thành một mảng màu no — nhiều dòng liền nhau nhìn rối và nặng hơn hẳn cái nó chữa. **Việc làm:** năm lớp cũ (`ok` · `err` · `warn` · `gray` · `info`) về đúng giá trị trước `bao-CR-363`; năm lớp mới (`violet` · `pink` · `teal` · `back` · `done`) giữ nguyên nhưng dựng lại theo đúng công thức nền nhạt + chữ đậm cùng tông. Riêng `done` không còn tô đặc: cùng họ xanh lá với `ok` nhưng chữ xuống bậc rất đậm `#14532d` (so với `#16a34a`) để hai mốc liền nhau trong vòng đời (Đã mua hàng → Hoàn thành) vẫn phân biệt được. Bảng tra mốc → lớp màu ở `cruds.tsx` **không đổi** — phần chia màu theo mốc của `bao-CR-363` vẫn nguyên, chỉ đổi sắc độ. **Việc làm — bản v2 (`frontend-v2`):** gỡ nốt nửa v2 của đợt 2 cho hai bản khỏi lệch — `TONE_CLASS` (`shared/ui/status-tone.ts`) trả năm tông cũ về token ngữ nghĩa `bg-*/10` và dựng năm tông mới theo thang Tailwind bậc 100/700 khớp hex bản v1; bốn màu tự khai của `PROGRESS_CLASS` (`procurement/components/document-status-badge.tsx`) về nền mờ theo. `STATUS_TONE` và `TONE_DOT` **không đụng** — đó là phần chia màu theo mốc, thứ khách muốn giữ. 27 test của huy hiệu vẫn xanh vì chúng khẳng định theo TÔNG chứ không theo lớp CSS. | `frontend/src/index.css` + 3 tệp màu của `frontend-v2`. Không đụng CSDL, API, không đụng bảng tra trạng thái | **Đã deploy prod + dev 11/09/2026** (v1 + v2) | `bao-CR-363` (bộ màu mốc trạng thái) |
| **bao-CR-364-nhan-tien-te-theo-dong** | 2026-09-11 | Nội bộ (khách phát hiện) | **[frontend] Bảng tổng tiền của Đơn mua hàng dán nhãn tiền tệ của ĐẦU PHIẾU lên những con số lấy từ DÒNG — đơn ngoại tệ đọc ra thành VND.** Khách chụp màn ĐMH: *Giá trị đặt hàng (trước thuế)* **6.500 VND**, *Tổng cộng (sau thuế)* **6.500 VND**, ngay dưới là *Quy đổi (VNĐ)* **172.250.000 đ** — 6.500 mà quy đổi ra 172 triệu thì không thể là VND. Kiểm chứng trên dữ liệu prod (PO00122): đầu phiếu `currency=VND, exchange_rate=1`, còn dòng hàng duy nhất mang `currency=USD, exchange_rate=26500`; 6.500 × 26.500 = 172.250.000. **Tiền và số quy đổi đều ĐÚNG, chỉ chữ tiền tệ sai** — vì từ `bao-CR-319` mỗi DÒNG có tiền tệ/tỷ giá riêng, còn nhãn thì vẫn đọc `po.currency`. **Việc làm: (1)** tính bộ tiền tệ THỰC của các dòng (`lineCurrency` đã có sẵn) rồi lấy đó làm nhãn cho ba dòng tổng, thay vì `poCurrency`. **(2)** Vá luôn lỗ sâu hơn mà chính khách nói trước — *"có thể có nhiều tiền khác"*: `orderTotal` / `orderBeforeTax` là phép cộng MÙ TIỀN TỆ, đơn trộn USD với VND ra một con số vô nghĩa. Khi các dòng không cùng một loại tiền thì ba dòng tổng chuyển hẳn sang bản **quy đổi VNĐ** (mỗi dòng nhân tỷ giá của chính nó rồi mới cộng), kèm câu nói rõ đơn có nhiều loại tiền; bỏ dòng *Quy đổi (VNĐ)* riêng vì lúc đó nó trùng với dòng tổng. **(3)** Bản in nhập khẩu (`PrintPurchaseOrderImport.tsx`) mang **đúng lỗi đó** và nặng hơn vì tờ này đưa cho nhà cung cấp: tiêu đề cột *Đơn giá (…)* / *Thành tiền (…)*, ô *Loại tiền* và ô *Tiền hàng (…)* đều lấy `po.currency`. Cả bốn chỗ đổi sang tiền tệ của dòng; đơn trộn nhiều loại tiền thì bảng A mọc thêm cột **Tiền tệ** và ô tổng nguyên tệ để gạch ngang thay vì in một tổng cộng ngang không có nghĩa. **(4)** Hai bản in còn lại cũng dính, và `PrintPurchaseOrderMH.tsx` là nặng nhất vì nó **ghi chết** `Loại tiền: VND` rồi đọc `po.order_total` (số NGUYÊN TỆ) thành chữ — đơn 6.500 USD in ra tờ gửi nhà cung cấp thành *"sáu nghìn năm trăm đồng chẵn"* trong khi thực trả 172 triệu. Sửa cả `PrintPurchaseOrder.tsx` và `PrintPurchaseOrderMH.tsx` theo cùng một luật, và khi là ngoại tệ thì dòng tiền-bằng-chữ đọc số **quy đổi VNĐ** kèm ghi chú, thêm dòng tổng quy đổi dưới dòng tổng nguyên tệ. Hàm chọn nhãn tách riêng ở `utils/money.ts` (`resolveTotalsCurrency`) để dùng chung bốn màn; **chưa kèm test vì `frontend/` không có bộ chạy test** (`package.json` chỉ có `dev`/`build`/`preview`) — test cho hàm này viết ở bước port sang `frontend-v2` (có Vitest). Sửa ở `frontend/` (đóng băng) vì đây là **lỗi hiển thị** của màn đang chạy thật, đúng ngoại lệ của D-026. **(5)** Port sang `frontend-v2` (`document-money-totals.tsx` + `purchase-order-detail-page.tsx` cũng truyền `data.currency` của đầu phiếu) sau khi v1 chạy ổn trên prod. | Không đụng CSDL, không đụng API, không đụng backend. Số tiền lưu dưới DB và số quy đổi `base_amount` **không đổi** — chỉ đổi chữ hiển thị và cách gom tổng khi trộn nhiều loại tiền | **Đã deploy prod + dev 11/09/2026** (v1) · port v2 chưa làm | [change-log.md](change-log.md) D-026 · `bao-CR-319` (tiền tệ theo dòng) |
| **bao-CR-365-bo-loc-dinh-cung-man-danh-sach** | 2026-09-11 | Khách (tài khoản demo TESTREQ) | **[frontend] Bộ lọc nhanh của màn danh sách không bao giờ tự trả về rỗng — bấm menu vào lại màn đó vẫn còn nguyên giá trị lọc cũ, F5 cũng không dứt.** Khách đăng nhập tài khoản demo **TESTREQ** (Nhân viên, người YÊU CẦU chứ không phải NSTM) thì màn *Yêu cầu mua hàng* đứng ở `?assignee=TESTREQ`, ô **NSTM phụ trách** dính sẵn tên họ và bảng rỗng 0/0 — TESTREQ không phụ trách dòng nào nên lọc theo họ thì đúng là không có gì. **Gốc lỗi nằm ở khuôn chung, không riêng màn này:** `CrudList` chỉ đặt lại `filters` trong effect nghe `cfg?.slug`, còn `FilterBar` giữ `vals` trong state riêng và khai `key={cfg.slug}`. Bấm menu *Yêu cầu mua hàng* khi đang đứng ở chính màn đó thì `slug` KHÔNG đổi → không effect nào chạy, không component nào dựng lại → bộ lọc sống sót qua mọi lần điều hướng; mà giá trị lại nằm trên URL nên F5 cũng khôi phục lại y nguyên. Lối thoát duy nhất là bấm đúng nút xóa trong thanh lọc — người dùng không biết nên đọc ra là *"dính cứng"*. **Việc làm:** khi URL KHÔNG còn khóa lọc nào mà state vẫn đang giữ giá trị (đúng cảnh bấm menu / bấm logo về màn đó) thì đặt lại `filters` + nạp lại danh sách, và bump một `resetToken` đưa vào `key` của `FilterBar` để ô lọc dựng lại rỗng. Đặt lại theo URL chứ không theo mỗi lần render nên **không** cướp con trỏ lúc đang gõ (ghi URL là `replace`, khóa lọc còn nguyên trên URL trong suốt lúc gõ) và **không** phá đường quay lại từ màn chi tiết (URL lúc đó vẫn mang khóa lọc). Áp cho MỌI màn chạy trên `CrudList`, không vá riêng YCMH. Sửa ở `frontend/` (đóng băng) vì là **lỗi** của màn đang chạy thật, đúng ngoại lệ của D-026. | Không đụng CSDL, không đụng API. Chỉ đổi cách khuôn danh sách đồng bộ bộ lọc với URL | **Đã deploy prod + dev 11/09/2026** (v1) | [change-log.md](change-log.md) D-026 |
| **bao-CR-363-mau-pill-trang-thai-pyc** | 2026-09-10 | Khách (ticket 38) | **[frontend] Mỗi mốc trạng thái một màu — trước CR này 11 mốc chỉ có 5 màu, nên hai mốc khác nhau hẳn nhìn ra y hệt.** Khách chụp màn *Yêu cầu mua hàng* trên prod khoanh đỏ ba dấu: **ĐÃ ĐIỀU PHỐI** và **ĐÃ MUA HÀNG** cùng một sắc xanh, **ĐANG XỬ LÝ** thì cùng sắc hổ phách với *Chờ duyệt* và *Bị trả lại*. Nguyên nhân KHÔNG phải gán màu sai: cả bộ pill của `frontend/` chỉ có **năm** lớp màu (`ok · warn · err · gray · info`), mà `PR_STATUS` phải xếp **11 mốc** vào đó — 6 mốc dồn vào `ok`, 5 mốc dồn vào `warn`. Lỗ này có từ đầu, `bao-CR-292` tách *Đang xử lý* thành ba mốc (`processing` · `purchasing` · `purchased`) mới làm nó lộ ra vì ba mốc mới rơi đúng vào hai màu đã kín chỗ. **Việc làm: (1)** khai **bốn lớp pill mới** trong `frontend/src/index.css` — `violet` · `pink` · `teal` · `back` (cam) — giữ đúng công thức nền pastel + chữ đậm cùng tông của các pill cũ, cộng lớp `done` **tô đặc** cho mốc cuối *Hoàn thành* (mốc đóng phiếu thì nhìn một lượt phải thấy, không nên là sắc xanh nhạt thứ hai cạnh *Đã mua hàng*). **(2)** Xếp lại màu cho **cả ba bảng trạng thái** trong `frontend/src/config/cruds.tsx` theo đúng thứ tự vòng đời, hai mốc LIỀN KỀ không bao giờ cùng tông: `PR_STATUS` (YCMH) xám → hổ phách → xanh dương → tím → hồng sen → xanh mòng → xanh lá → xanh lá đặc; `PO_STATUS` (ĐMH) tách *Đã duyệt* · *Đã nhận đủ* · *Hoàn thành* vốn chung một màu xanh; `SR_STATUS` (YCBG) tách *Đã duyệt* khỏi *Đã khảo sát* và *Đang xử lý* khỏi *Chờ duyệt*. Mốc nào trùng tên giữa ba bảng thì dùng **cùng một màu** (Chờ duyệt = hổ phách, Đã duyệt = xanh dương, Bị trả lại = cam, Đang xử lý = hồng sen, Hoàn thành = xanh lá đặc, Đã từ chối = đỏ) để một mốc không mang hai màu ở hai màn. **(3)** *Bị trả lại* lấy **cam**, cố ý không lấy đỏ: đỏ đang là mốc **khóa phiếu** (*Đã từ chối*), còn trả lại thì sửa rồi gửi duyệt lại được — hai thứ đó không được nhìn giống nhau. Sửa ở `frontend/` (đóng băng) vì đây là **lỗi hiển thị** của màn đang chạy thật, đúng ngoại lệ của D-026. **(4) Đợt 2 — CẢ BỘ PILL TÔ ĐẶC, CHỮ TRẮNG.** Khách xem bản pastel vừa deploy rồi báo *"màu hơi nhợt nhạt"*: chữ pill chỉ **10.5px in hoa**, nền nhạt gần như hòa vào nền bảng nên phải nhìn kỹ mới ra màu — phân biệt được về lý thuyết nhưng không quét mắt một lượt mà thấy được. Cả **mười** lớp pill đổi sang nền đặc + chữ trắng, mỗi màu chọn ở bậc đậm để chữ trắng đạt tương phản **≥ 4,5:1** (ngưỡng WCAG AA cho chữ nhỏ) — bậc tươi 500/600 chỉ được ~3:1 nên không dùng: `gray #64748b` · `warn #b45309` · `info #0369a1` · `violet #7c3aed` · `pink #db2777` · `teal #0f766e` · `ok #15803d` · `back #c2410c` · `err #b91c1c` · `done #14532d`. *Hoàn thành* xuống `#14532d` (đậm hơn `ok` một bậc) vì hai mốc này đứng liền nhau trong vòng đời. **(5) Bản v2** (`frontend-v2`) mang **đúng lỗi và đúng bảng màu** của v1 — bảng `TONE_CLASS` chỉ có 5 tông, và comment trong mã ghi rõ là chép từ v1 "để người dùng cũ khỏi học lại". Port sang `erp-v2` (làm ở nhánh đó, không nằm trong commit của `main`): thêm 5 tông (`handoff` · `active` · `partial` · `returned` · `closed`), xếp lại `STATUS_TONE`, đổi cả bộ sang tô đặc, và đưa **4 màu tự khai của `PROGRESS_CLASS`** theo cùng để một cột không nửa đậm nửa nhạt. Kèm **6 test** chặn tái phát: hai mốc liền kề trong vòng đời YCMH/ĐMH/YCBG không được cùng lớp màu, và 8 mốc của YCMH phải phân biệt hết. | Không đụng CSDL, không đụng API, không đụng backend. Chỉ CSS + bảng tra màu; nhãn chữ và giá trị `status` lưu dưới DB **không đổi** | **Đã deploy prod 10/09/2026** — đợt 1 `main` 39e07ca5, đợt 2 (tô đặc) xem bảng deploy | [change-log.md](change-log.md) D-026 · `bao-CR-292` (tách ba mốc) · `CR-077`/`CR-078` (hai lớp pill thêm trước đây) |
| **bao-CR-360-phien-dang-nhap-may-chu** | 2026-09-10 | Nội bộ (CR-312 **P3a**, đóng BM-002) | **[backend] Phiên đăng nhập có ở máy chủ — phần LÕI, chưa có màn hình nào.** Bảng `tab_login_session` + `tab_user.token_version`; vé đăng nhập mang thêm `jti` + `ver`; đăng xuất từ nay **thu hồi thật**. Theo **QĐ-D**: cửa chặn đặt ở `get_current_user`, dập dấu vết (`last_seen_at` / `last_seen_ip`) đặt ở middleware, hai chỗ nối nhau bằng `ContextVar` sẵn có; riêng bộ đếm gia hạn (`refreshed_at` / `refresh_count`) dập ngay trong `/refresh` vì đường đó công khai, middleware không có `session_id` mà dùng. | Thêm 1 bảng + 1 cột (có migration). Vé cũ không có `jti` nên **mọi người bị đăng xuất một lần** lúc deploy | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8, alembic dev tới `a7c5e91d3b04`) — prod chưa có | `nhat-ky-va-phien-dang-nhap.md` §5, §11 QĐ-D |
| **bao-CR-359-o-nhap-ngay-bi-cat-trong-bang-dong** | 2026-09-10 | Đại ca (nhìn bảng Giao hàng nhiều lần trên ĐMH) | **[frontend-v2] Ô CHỌN NGÀY trong bảng dòng hẹp hơn chính giá trị nó chứa, nên ngày nào cũng hiện «20/09/…».** Đo trên trình duyệt thật: nút bấm `DatePicker size="sm"` cần **135,3px** (lịch `size-4` + `gap-2` + `px-2` + chuỗi `20/09/2026` ở `text-sm` = 71,3px + dấu X xóa ngày), cộng `px-2.5` hai bên của ô bảng = 20px, ra **155,3px**. Ba cột khai 150 · hai cột khai 130 · một cột khai 140 — tất cả đều hụt, và chúng hụt **từ ngày dựng bảng**.<br>⚠️ **Đây là kiểu hỏng đọc ra một giá trị SAI, không phải một giá trị thiếu.** `20/09/2026` và `20/09/2027` cắt xong đều hiện `20/09/…`; người dùng không hề biết mình đang đọc thiếu, khác hẳn một cột tên bị cắt (thấy dấu ba chấm là biết còn nữa). Cũng vì vậy luật «cấm truncate» ở §6.3 của `doc/erp/13-...md` không bắt được ca này: luật đó nói về chữ do BẢNG vẽ, còn chữ nằm trong ô nhập thì chính ô đó tự cắt của mình.<br>**Cách vá — dựng nền chứ không sửa từng số.** Thêm `shared/data-table/line-column-width.ts` (`lineColumnMinWidth` + `lineColumnWidth` + hằng `DATE_CONTROL_MIN_WIDTH = 156`) và trường `control?: 'date'` trong `LinesTableColumn`. `LinesTable` chuẩn hóa bề rộng ngay tại `layoutColumns`, nên sàn áp cho **cả ô tiêu đề, ô dữ liệu lẫn tay kéo giãn** — không sót chỗ nào. Khai `control: 'date'` cho 8 cột ở 5 bảng (giao hàng nhiều lần · khoản chi phí nhập khẩu · dòng YCBG · dòng YCMH · dòng YCTT), kể cả mấy cột đang đủ rộng: sàn là thứ giữ cho lần sau ai đó chỉnh bề rộng xuống không âm thầm cắt mất ngày.<br>⚠️ **Sàn kẹp CẢ bề rộng người dùng đã kéo, có chủ ý.** `useTableLayout` chỉ ghi vào `columnWidths` những cột người dùng tự chỉnh, nên sửa `width` trong mã nguồn tới được gần hết mọi người — **trừ đúng người đã kéo cột đó**, tức là người quan tâm tới nó nhất. Kẹp ở tầng đọc thì bản lưu cũ 110px cũng tự nâng lên sàn.<br>⚠️ **`DatePicker` trong bảng dòng phải khai `size="sm"`.** Hai ô ngày của bảng _Khoản chi phí_ đang dùng cỡ mặc định (`h-9 px-4`) — cần tới **~189px**, tức là dù có nâng cột lên sàn 156 vẫn cắt. Đây là chỗ duy nhất trong 5 bảng dòng lệch khỏi lệ chung.<br>⚠️ Hằng 156 **gắn với chrome của `DatePicker`**: đổi `gap` / `px` / cỡ biểu tượng trong `shared/ui/date-picker.tsx` thì phải đo lại, có bài kiểm canh chặn dưới.<br>**Nút _Vừa nội dung_ ra thanh công cụ của `LinesTable`.** Cùng thao tác với mục cuối menu *Cột* (`autoFitAll`, có từ CR-101) nhưng bày thẳng ra ngoài — đây là đường thoát cho MỌI cột bị cắt mà sàn khai sẵn không với tới (số tiền dài, tên NCC, ghi chú), vì sàn chỉ biết trước LOẠI ô còn nội dung thật thì phải đo tại chỗ.<br>Rà luôn tiêu đề toàn bộ 74 cột của 7 bảng dòng bằng cách đo chuỗi ở `13px font-bold` + `px-2.5`: đúng **một** cột hụt — _Đơn giá (Sau VAT)_ khai 130 mà tiêu đề cần 131, nâng lên 140.<br>**Kèm yêu cầu thứ hai của đại ca:** cột _Số hóa đơn_ của bảng _Khoản chi phí nhập khẩu_ bỏ `compactHidden` để hiện sẵn. ⚠️ Phải **tăng `TABLE_STORAGE_KEY` lên `-v3`**: `hiddenColumns` được chụp lại ngay lần đầu người dùng đụng vào bảng, nên bỏ `compactHidden` không thôi thì chỉ người chưa từng mở bảng này mới thấy cột đó. Cái giá là bố cục đã chỉnh tay của **riêng bảng này** về mặc định một lần.<br>Cổng v2: typecheck **0 lỗi**, lint **0 lỗi / 32 cảnh báo** (đúng nền cũ), test **245 tệp / 2794 xanh + 2 expected fail**, trong đó **14 bài mới** ở `line-column-width.test.ts` — canh cả bố cục rác trong `localStorage` (bề rộng `0` / số âm) vì đó là dữ liệu người ngoài sửa được. | Không đụng CSDL, không đụng API. Người dùng bảng _Khoản chi phí_ mất bố cục cột đã chỉnh tay một lần (đổi `storageKey`) | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8) | `doc/erp/13-ke-hoach-man-con-lai-v2.md` §6.2 + §6.3 |
| **bao-CR-358-bo-ma-hanh-dong-nhat-ky** | 2026-09-10 | Nội bộ (CR-312 **P2**) | **[backend] Bộ mã hành động ĐÓNG cho nhật ký ba tầng (NT-4).** Gộp hai bảng đang khai chồng nhau — `ACTION_LABEL` của `modules/audit/controller.py` (nhãn) và `ACTION_GROUP_BY_ACTION` của `core/logging_codes.py` (nhóm) — vào một chỗ khai duy nhất `core/action_catalog.py`: mỗi mã một dòng `mã + nhãn tiếng Việt + nhóm`, **có nhãn là điều kiện để tồn tại**. Kèm test quét `ast` mọi lời gọi `record(...)` trong `app/` để mã mới không có nhãn thì đỏ ngay, và chốt cả **17 chỗ truyền `action` động** (biến / f-string / biểu thức điều kiện) — thứ mà quét chuỗi thuần bỏ lọt. **Tìm ra 12 mã chưa ai khai bao giờ** (`option_choose`, `option_unchoose`, `download_file`, `file_alert`, `unassign`, `draft`, `received`, `partial`, `open`, `in_progress`, `answered`, `done`/`survey_done`) — tất cả đều nằm ở những chỗ truyền động đó. Bài kiểm cũ trong `test_va_nhat_ky_thao_tac.py` thu về một ca, khỏi giữ hai bộ quét cạnh nhau. | Không đụng CSDL (`action` vẫn `VARCHAR`, xem QĐ-9), không đổi API, không đổi giao diện. Cửa vào cũ `audit.controller.ACTION_LABEL` giữ nguyên tên (nay là bí danh) nên 3 bài kiểm đang import không phải sửa | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8) | `nhat-ky-va-phien-dang-nhap.md` §10.1 |
| **bao-CR-357-bao-cao-gia-von-nhap-khau-v2** | 2026-09-10 | Đại ca | **[frontend-v2] Dời màn _Giá vốn nhập khẩu_ (bao-CR-347) sang `frontend-v2`.** Backend đã có sẵn trên nhánh `erp-v2`, đợt này thuần giao diện: tab thứ 10 của _Báo cáo mua hàng_ (hai cách xem — theo lô và theo dòng hàng), trang in A4 ngang `/print/import-landed-cost`, và lối vào _In Báo cáo giá vốn_ trong menu in của chi tiết ĐMH.<br>**Đã dựng:** `types/import-landed-cost.ts` (kiểu + `buildLandedCostParams`) · hai hàm gọi API trong `api/purchase-report-api.ts` · khóa cache `reportImportLandedCost` · hook `useImportLandedCost` · `components/import-landed-cost-tables.tsx` (bảng xoay theo lô + bảng theo dòng hàng + khối cảnh báo + khối chữ ký, **dùng chung cho cả tab lẫn trang in**) · `components/report-import-landed-cost-tab.tsx` · `pages/import-landed-cost-print-page.tsx` + `appRoutes.procurement.importLandedCostPrint` + đăng ký ở `app-router.tsx` · nút _In Báo cáo giá vốn_ ở `purchase-order-detail-page.tsx`.<br>⚠️ **`LandedCostParams` phải khai bằng `type`, KHÔNG phải `interface`.** Hàm dựng khóa cache nhận `Record<string, unknown>`, mà `interface` không tự có chữ ký chỉ mục ngầm — gõ `interface` là `tsc` đỏ ngay ở dòng gọi `queryKeys`, đúng bẫy đã ghi sẵn cho `ReportScope`.<br>⚠️ **Một bộ CSS cho hai nơi, nối bằng biến CSS có giá trị dự phòng.** `LANDED_COST_TABLE_STYLES` viết toàn `var(--lc-*, #màu)`: trang in **ăn giá trị dự phòng** (giấy không có chế độ tối, cũng không có bảng màu người dùng), còn trên màn hình thì bọc trong `.lc-scope` để ánh xạ về token giao diện (`--border` · `--row-head` · `--muted` · `--warning`). Không có lớp bọc đó thì bảng trong tab đứng im một màu khi đổi giao diện.<br>⚠️ **Tab này KHÔNG tự nạp lại khi gõ, phải bấm _Xem_** — khác mấy tab anh em. Ô mã đơn là chữ tự do, nạp theo từng ký tự thì gõ dở `PO-` đã bắn một lượt truy vấn rỗng. Đổi công ty thì vẫn nạp ngay vì `company_id` đi thẳng vào tham số.<br>⚠️ **Trang in gác `useCompanies` bằng `can('company','read')`** — người chỉ có quyền in mà mở trang là ăn 403 ngay lúc mount, đúng bẫy «tab mượn dữ liệu của phân hệ khác» đã ghi ở CLAUDE.md.<br>⚠️ **Đường in không có `:id`** như mấy bản in chứng từ: phạm vi báo cáo đi bằng query string (`codes` / `date_from` / `date_to` / `company_id` / `view`).<br>Giữ nguyên ba chỗ **cố ý bỏ trống** của bản v1, đừng tưởng là sót: _Tiền hàng (nguyên tệ)_ rỗng khi phạm vi có nhiều đồng tiền · cột TỔNG rỗng ở mấy dòng chỉ có nghĩa với từng đơn (ngày, NCC, tờ khai, tỷ giá) · ô _Giá vốn/ĐVT_ ở dòng tổng của bảng theo dòng hàng rỗng vì mỗi dòng một đơn vị tính.<br>Bảng ở đây **không dùng `DataTable`** — cột sinh theo dữ liệu (mỗi loại chi phí một cột/dòng) và đây là bảng XOAY, theo đúng tiền lệ `report-metric-table.tsx`; luật «màn danh sách phải dùng `DataTable`» không áp cho tab báo cáo.<br>Cổng v2: typecheck **0 lỗi**, lint **0 lỗi / 32 cảnh báo** (đúng nền cũ, không cảnh báo nào rơi vào tệp mới), test **244 tệp / 2780 xanh + 2 expected fail**, trong đó **8 bài mới** ở `import-landed-cost.test.ts` canh luật _mã đơn đè khoảng ngày_. | Không đụng CSDL, không đụng API | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8) | — |
| **bao-CR-356-mau-hang-tieu-de-bang-v2** | 2026-09-10 | Đại ca (nhìn màn Đơn mua hàng trên v2) | **[frontend-v2] Hàng tiêu đề bảng danh sách đang XÁM, trả về xanh nước biển như bản v1.** Bản v2 để `--row-head: #e6ebf2` (xám xanh nhạt) + `--row-head-foreground: #0f172a` (gần đen) — hai màu đó ghim từ đời còn viết cứng `bg-slate-200/80`, tức là ghim đúng cái xám cũ chứ không phải chọn màu. Bản v1 để chữ `--navy` trên nền `#f4f7fe`, ra xanh nước biển. Đổi cả CHỮ lẫn NỀN: `--row-head-foreground` → `#1b2559` (đúng `--navy`), `--row-head` → `#dbe6f5`.<br>⚠️ **Không bê thẳng `#f4f7fe` của v1 sang.** v1 không có vằn hàng, v2 thì có — `--row-stripe: #f0f4f9`. Dán `#f4f7fe` vào là hàng tiêu đề SÁNG hơn vằn hàng ngay dưới nó, bảng nhìn như mất tiêu đề. Nền mới phải đậm hơn `--row-stripe` mà vẫn ra xanh, nên chọn `#dbe6f5`.<br>⚠️ **Sửa HAI CHỖ hoặc test đỏ.** Token nằm ở `index.css` khối `:root` VÀ ở bản chép trong bảng màu DEGO của `theme-presets.ts`; `theme-presets.test.ts` đọc thẳng `index.css` rồi đối chiếu từng biến. Sửa một bên là đỏ ngay — đúng ý đồ của bài test đó.<br>Phạm vi cố ý hẹp: chỉ bảng màu **DEGO** (mặc định). Chế độ tối để nguyên (`#1e293b` / `#f1f5f9` — nền tối thì chữ navy không đọc được), 20 bảng màu còn lại vẫn tự suy ra từ `--card` + `--foreground` của chính chúng qua `DERIVED_TOKENS`. Ai đổi giao diện riêng thì tính sau, đúng như đại ca dặn.<br>Không đụng component nào: `DataTable` và `LinesTable` vốn đã ăn `bg-row-head` / `text-row-head-foreground`. | Không đụng CSDL, không đụng API | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8) | — |
| **bao-CR-350-nhan-su-hien-ma-trang-thai-tho** | 2026-09-10 | Đại ca (thấy trên màn Nhân sự lúc chạy thử) | **[frontend v1 — nhánh `main`] Màn Nhân sự hiện `OFFICIAL` tô ĐỎ thay vì «Chính thức» xanh.** Cột Trạng thái so `r.status === 'Chính thức'` — chữ tiếng Việt của đời dữ liệu cũ. Đợt **B-03** (nhánh `erp-v2`) đổi `tab_employee.status` sang mã tiếng Anh, nên bất kỳ CSDL nào đã chạy B-03 mà mở bằng giao diện nhánh này thì **không dòng nào khớp**: badge rơi hết về nhánh `err` (đỏ) và đem `status` thô đi hiện, CSS badge viết hoa thành `OFFICIAL`. Không có lỗi nào nổ ra — đó là kiểu hỏng tệ nhất, đọc ngược nghĩa (đỏ trông như «đã nghỉ việc» trong khi `official` = đang làm chính thức). Vá bằng bảng tra `EMPLOYEE_STATUS_BY_CODE` + `isEmployeeOfficial` đọc được cả hai đời, dùng ở cột danh sách và chip trang chi tiết; ưu tiên `status_label` nếu API có gửi kèm.<br>⚠️ **KHÔNG đổi `value` của `EMPLOYEE_STATUS` sang mã tiếng Anh.** Bộ đó vừa là option ô lọc — giá trị được gửi thẳng lên API — mà CSDL prod của nhánh `main` vẫn lưu tiếng Việt; đổi là lọc ra rỗng, cũng lặng lẽ như lỗi vừa vá.<br>⚠️ **Chỉ vá tầng HIỂN THỊ, ô chọn trong form để nguyên.** Đó là ô NHẬP: cho chọn `official` ở nhánh này là ghi vào CSDL một giá trị backend `main` không biết. Hệ quả còn lại, có chủ ý — mở CSDL đã chạy B-03 bằng giao diện này thì ô chọn vẫn hiện mã thô. | Không đụng CSDL, không đụng API. `frontend/src/config/cruds.tsx` | Đã deploy prod 10/09/2026 (0ec5646c, backup `procurement_truoc_cr347_349_20260910.sql.gz`) | — |
| **bao-CR-349-tat-email-thong-bao-theo-nguoi** | 2026-09-10 | Khách (ticket 37 prod — *"Ngưng cập nhật thông tin app trên mail pkngan.idagroup@gmail.com"*) | **[backend + frontend v1 + frontend-v2] Công tắc _Nhận email thông báo_ theo TỪNG NGƯỜI.** Chị Phạm Khánh Ngân (user 15, `pur_staff` + `pur_manager`) nhận **287 thư** từ 03/08 tới 10/09 — 13 đến 23 thư mỗi ngày vào một hộp Gmail cá nhân. Trước đó hệ thống **không có cách nào tắt cho một người**: `_send_workflow_emails` chỉ nhìn `EMAIL_WORKFLOW_ENABLED` trong `.env`, tức là muốn tắt cho chị ấy thì phải tắt luôn cho **28 người** đang nhận. Nay `tab_user.notify_email` (mặc định `1`) là tầng thứ hai, xét ngay trong vòng lặp người nhận.<br>⚠️ **Ô «Bật gửi email» ở Cấu hình hệ thống KHÔNG chặn được loại thư này** — luồng duyệt gọi `send_smtp_email(..., force=True)` để cố tình vượt qua nó. Ai vào tắt ô đó rồi báo «đã xử lý» là báo nhầm, thư vẫn chạy đều; ghi rõ vào tài liệu để lần sau không mất một buổi đi tìm.<br>⚠️ **Không dùng cách rẻ tiền là xóa `tab_user.email`** — đó là email ĐĂNG NHẬP, lại còn được đồng bộ ngược từ hồ sơ nhân sự, nên nó tự quay lại sau lần lưu hồ sơ kế tiếp và trong lúc đó thì gãy đường đăng nhập.<br>⚠️ **Người tắt thì bỏ qua hẳn, KHÔNG ghi `EmailLog`**: một dòng log «đã gửi» cho người không nhận được thư là mồi cho một cuộc đi tra vô ích.<br>⚠️ **Tắt email ≠ tắt thông báo.** Chuông trong app và Web Push chạy trước và độc lập — có test canh. Nhưng chị Ngân là **người duyệt** mà `tab_push_subscription` của chị ấy đang **rỗng**, nên khi bàn giao phải dặn bật Thông báo đẩy trước; card ở Trang cá nhân nói thẳng câu đó ngay cạnh nút tắt.<br>**Thư đặt lại mật khẩu và thư cấp tài khoản cố ý KHÔNG đi qua công tắc** — đó là đường vào hệ thống, tắt là người dùng không tự lấy lại được tài khoản. Hai test riêng canh chỗ này.<br>Bốn cửa: người dùng tự tắt ở **Trang cá nhân** (`PUT /api/auth/notify-email`, card mới `email-notification-card.tsx`), quản trị tắt hộ ở **hồ sơ Nhân sự** — thẻ «Tài khoản đăng nhập» (`employee-account-card.tsx`) — và ở **`/users/{id}`** (cùng gọi `PUT /api/users/{id}/notify-email`, quyền `user.write`, có ghi nhật ký), cộng nhãn **«Tắt email»** ở danh sách Phân quyền để trả lời ngay câu «sao người này không nhận được thư».<br>⚠️ **Cửa ở hồ sơ Nhân sự mới là cửa được dùng thật.** Ba cửa đầu làm xong mới nhận ra: người đi xử lý yêu cầu «ngưng gửi mail» là Nhân sự/quản trị, mà họ mở **hồ sơ nhân sự** chứ không nhớ tài khoản số mấy — bắt họ đi vòng qua màn Phân quyền là đặt công tắc ở chỗ không ai tìm. Cổng: **9/9 test mới** (`test_tat_email_thong_bao_cr349.py`) + 25/25 test cũ vùng user/thông báo xanh; typecheck container đúng 4 lỗi cũ.<br>**Đợt 2 — `frontend-v2` (10/09/2026).** Backend đã sẵn trên `erp-v2` từ lần trộn `main` → `erp-v2` (`ef0681d5`) nên đợt này thuần giao diện: dời **đủ bốn cửa** sang v2 — card `app/components/profile/email-notification-card.tsx` ở `/me`, thẻ «Tài khoản đăng nhập» của hồ sơ nhân sự (`hr/components/employee-account-card.tsx`), thẻ ở `/users/{id}` (`hr/pages/user-permission-detail-page.tsx`) và nhãn «Tắt email» ở bảng Phân quyền (`hr/components/user-account-table.tsx`); hai hàm gọi API ở `core/auth/auth-service.ts` (tự mình) và `hr/api/user-account-api.ts` + hook `useSetUserNotifyEmail`. Khác v1 ở ba chỗ, đều cố ý: (1) **`notify_email` khai `?` và luôn đọc `!== false`** — hệ chưa chạy migration `d2f45a8c9e10` thì API không gửi trường này, mà mặc định của cột là BẬT; đọc `=== true` là màn hình **bịa ra một người không nhận thư** trong khi họ vẫn nhận, rồi họ đi bấm «Bật» cho một công tắc vốn đang bật. Có test canh đúng ca `undefined`. (2) **Không giữ `useState` riêng** như bản v1 — bấm xong nạp lại `queryKeys.auth.me()` / `queryKeys.hr.all`, để bốn cửa cùng đọc một nguồn. (3) **Câu nhắc về chuông viết lại**: v2 **chưa có Web Push** (không có service worker lẫn màn đăng ký), nên chép nguyên câu «bật Thông báo đẩy ở thẻ bên dưới» của v1 là chỉ người dùng tới một công tắc không tồn tại — v2 nói thẳng «chỉ mở app mới biết có việc chờ ký». ⚠️ Nút trong `employee-account-card.tsx` **phải khai `type="button"`**: thẻ đó nằm TRONG `<form>` của trang chi tiết nhân sự. Thẻ ở `/users/{id}` **cố ý không khóa theo `isSelf`** — chốt hai người là luật của PHÂN QUYỀN, còn đây là tuỳ chọn nhận thư, tự tắt cho mình không nâng quyền cho ai. Cổng v2: typecheck 0 lỗi, lint 0 lỗi / 32 cảnh báo (đúng nền cũ), 243 tệp test / 2772 test xanh, trong đó 4 bài mới ở `email-notification-card.test.tsx`. | `tab_user` thêm cột `notify_email`; migration `d2f45a8c9e10`. ⚠️ Nối sau `a1c6f80b2d47` (CR-347) nên deploy CR-349 là kéo theo migration của CR-347 (đã kéo thật lúc deploy 10/09) | Đã deploy prod 10/09/2026 (0ec5646c, backup `procurement_truoc_cr347_349_20260910.sql.gz`) · đợt 2 (frontend-v2) xong local | [09-thong-bao-va-trang-ca-nhan](../tai-lieu-chuc-nang/09-thong-bao-va-trang-ca-nhan.md) Phần I mục G |
| **bao-CR-347-bao-cao-gia-von-nhap-khau** | 2026-09-10 | Khách hàng | **[backend + frontend v1] Báo cáo giá vốn lô hàng nhập khẩu — hai tab: theo lô và theo dòng hàng, bản in ký tay, Excel hai sheet.** Nối tiếp bao-CR-319 (đơn nhập khẩu): số chi phí đã nhập được rồi nhưng chưa có chỗ nào cộng ra **giá vốn một lô hàng**, kế toán vẫn gõ tay ra Excel. **(1) `tab_purchase_order.etd_date`** — ngày hàng rời cảng xuất, chỉ tiêu «Ngày hàng rời cảng (ETD)» trên báo cáo; để chuỗi `YYYY-MM-DD` cho đồng bộ với mọi cột ngày sẵn có của ĐMH. **(2) Ba mã chi phí mới** `12` Dịch vụ hỗ trợ nhập khẩu, vận chuyển · `13` Chi tiền hư container · `14` Lãi trả chậm — đánh **tiếp số**, không đánh lại từ đầu, vì mã cũ đã nằm dưới DB. **(3) API `GET /api/reports/import-landed-cost`** (+ `/export`) ở `modules/report/import_landed_cost.py`: lọc theo `po_ids` > `codes` > khoảng ngày đặt, chỉ nhận đơn `order_type = IMPORT`. **Một lời gọi trả CẢ HAI cách đọc** (`orders` + `items`) từ cùng một lần lọc, nên đổi tab trên màn hình không tải lại số liệu và màn hình / bản in / Excel dùng chung đúng một con số. **Giá vốn TÍNH BAY lúc xem, KHÔNG lưu cột nào** — chi phí còn sửa được tới lúc tất toán, lưu là đẻ ra một con số cũ nằm cạnh một con số đúng mà không ai biết cái nào thật. `price_per_kg` của dòng TỔNG tính lại từ tiền/khối lượng đã cộng, không phải trung bình các đơn. Gác cửa dùng lại `report.read` / `report.export`, dữ liệu NCC vẫn qua `_can_see_ncc`. **(4) Tab _Theo lô hàng_** — bảng **xoay dọc**: mỗi ĐƠN là một CỘT, mỗi chỉ tiêu là một DÒNG, đúng mẫu khách đang dùng, cột cuối là TỔNG. **(5) Tab _Chi tiết theo dòng hàng_** — mỗi mã hàng một dòng: số lượng, tiền hàng, chi phí chia về dòng, tổng giá vốn, **giá vốn mỗi đơn vị**. Chia bằng **đúng `allocate_import_costs`** của ĐMH (bao-CR-319 P4) chứ không tự cộng lại: cách chia khai trên từng khoản chi và bản in ĐMH nhập khẩu đang bày đúng số đó — hai nơi lệch nhau là kế toán mất buổi. Dòng TỔNG **bỏ trống ô giá vốn/ĐVT**: mỗi dòng một đơn vị tính, cộng lại rồi chia ra là số không đọc được. Cảnh báo lúc chia (thiếu kg, gõ tay lệch tổng) hiện thành dải nhắc **kèm mã đơn**, không chặn. **(6) Tên loại chi phí lấy nguyên danh mục `ImportCostType`**, KHÔNG gom nhóm lại theo mẫu giấy (đại ca chốt 10/09/2026 — tấm hình khách gửi chỉ là gợi ý bố cục): thu mua gõ chi phí theo danh mục đó nên báo cáo phải soi ngược về được. Bảng **chỉ bày loại CÓ PHÁT SINH** trong lần lọc, đánh số `2.1`, `2.2`… theo thứ tự danh mục — bảng đã rộng sẵn vì mỗi đơn một cột, thêm mươi dòng 0 đồng là đẩy phần đọc được ra khỏi tờ giấy. **(7) BỎ HẲN khái niệm chi phí Dự kiến khỏi giao diện** (đại ca chốt 10/09/2026): thu mua chỉ gõ chi phí khi có số thật. Cột `tab_po_import_cost.cost_status` (SMALLINT, `server_default='2'`) **vẫn giữ trong CSDL** làm tấm lưới — `is_actual_cost()` / `actual_costs()` còn lọc để dòng dự kiến sót lại từ đợt thử nghiệm không lọt vào công nợ — nhưng thẻ chi phí ĐMH, cụm thẻ số, báo cáo, bản in và Excel không còn chỗ nào nhắc tới nó. **(8) Xuất Excel hai sheet** `GIA VON THEO LO` + `THEO DONG HANG`. Sheet theo dòng hàng khai mỗi cột bằng bộ bốn `(tiêu đề, lấy số của dòng, định dạng, lấy số của dòng TỔNG)` để **dòng TỔNG không bao giờ trượt cột** khi thêm bớt loại chi phí. **(9) Bản in** `/print/import-landed-cost?codes=…&view=orders|items` **A4 NGANG**, ba ô ký **Người lập biểu · Kế toán · Quản lý duyệt** — khách chốt **duyệt bằng chữ ký trên giấy, KHÔNG dựng luồng duyệt trong phần mềm**. Mỗi lần in MỘT bảng: hai bảng xoay ngang dọc khác nhau, ghép chung một tờ là không đọc được. Bảng số dùng chung component với tab báo cáo để hai nơi không lệch. Vào từ menu In của chi tiết ĐMH nhập khẩu (_In Báo cáo giá vốn_). **(10) Tên chỉ tiêu gọi theo TRƯỜNG của đơn mua hàng, không chép mẫu giấy** (đại ca chốt 10/09/2026): bỏ «Lần nhận» (chi phí gắn theo CẢ ĐƠN, không tách theo lượt nhận nên ô đó luôn là 1), «Giá đô» → **Tiền hàng (nguyên tệ)** kèm một dòng **Đồng tiền**, «Công nợ NCC» → **Tiền hàng (vnd)**; thêm Ngày đặt hàng, Nhà cung cấp, Số tờ khai hải quan, Ngày tờ khai, Khối lượng (kg) — toàn trường có sẵn của ĐMH. **Tiền nguyên tệ bỏ trống khi lẫn đồng tiền**, lẫn ở HAI cấp: trong một đơn (từng dòng hàng khai được đồng tiền riêng — dữ liệu thật đang có đơn USD kèm một dòng VND) và giữa các đơn ở cột TỔNG; cột VNĐ vẫn cộng bình thường vì đã quy đổi qua tỷ giá của từng dòng. Test: 20/20 xanh `test_gia_von_nhap_khau_cr347.py`. Migration `a1c6f80b2d47`. | Không đổi khóa quyền — dùng lại `report.read` / `report.export`; dữ liệu NCC vẫn gác bằng `purchase_order.read` | Đã deploy prod 10/09/2026 (0ec5646c, backup `procurement_truoc_cr347_349_20260910.sql.gz`) | `doc/tai-lieu-chuc-nang/04-don-mua-hang.md` §A.26, §K.1, §K.5 · `doc/tai-lieu-chuc-nang/08-he-thong-bao-cao.md` tab _Giá vốn nhập khẩu_ |
| **bao-CR-346-nhat-ky-ghi-het-get-va-don-90-ngay** | 2026-09-10 | Nội bộ | **[backend] Rà soát bảo mật lớp nhật ký CR-312 P1 — vá 1 lỗ rò + 4 lỗ mù, kèm cơ chế dọn và bản sao ngoài máy.** Bảy phần. **(1) Che vết lỗi SQL** — `error_detail` lưu nguyên `traceback.format_exc()`, mà SQLAlchemy nhét NGUYÊN VĂN tham số câu lệnh vào lời báo lỗi: một cú 500 lúc tạo tài khoản là `[parameters: {'p': '$2b$12$...'}]` nằm vĩnh viễn trong bảng chỉ-thêm, ở đúng dòng mà `request_body` đã che thành `***`. `mask_error_detail()` cắt theo mốc, giữ `[SQL: ...]` (chỉ có chỗ giữ tham số, không có giá trị). **(2) ĐẢO luật lọc: ghi hết GET, bỏ QĐ-A.** Luật cũ không ghi lượt đọc nên tải toàn bộ danh sách nhân sự kèm số tài khoản ngân hàng về máy không để lại dòng nào; tệ hơn, `should_log_request()` quyết định TRƯỚC khi endpoint chạy nên một GET ăn 403 cũng mất. Nay chỉ bỏ `/api/notifications` + `/api/alerts` **theo phương thức GET** (4.427/7.440 lượt GET một ngày, 59,5% — nhưng `PATCH /api/notifications/{id}` vẫn ghi) và đường của chính nhật ký (NT-5). QĐ-A bỏ vì `/auth/refresh` chính là nơi refresh token bị cắp lộ mặt (BM-003); `should_skip_by_result()` giữ làm chỗ móc cho P3, luôn trả `False`. Đổi lại **GET thành công không lưu `response_body`** — chép thân trả về của lượt đọc là sao dữ liệu nhạy cảm sang một bảng không có `employee_sensitive` gác cửa. **(3) Dọn dòng GET quá 90 ngày** — `request_log.cleanup`, 03:40 hằng ngày, xóa theo lô 2.000 dòng (một câu `DELETE` quét vài trăm nghìn dòng giữ khóa đủ lâu để mọi lượt gọi API đứng chờ ghi nhật ký). Dòng GHI không bao giờ bị đụng. **Chỉ xóa khi `is_remote_storage_ready()` đúng** — xóa bản duy nhất của một dòng nhật ký là hủy chứng cứ. **(4) Kéo gói R2 từ P6 lên** — QĐ-C đã loại bốn bảng nhật ký khỏi sao lưu hằng đêm nhưng gói R2 xếp ở giai đoạn cuối, khoảng giữa hai mốc thì nhật ký có đúng một bản nằm trên chính cái máy kẻ tấn công đang đứng. `audit.archive` nay chép **đủ cột qua mapper** (bản cũ liệt kê tay 7 trường nên bản lưu trữ rụng sạch 11 cột ngữ cảnh của CR-312), thêm `tab_request_log`, kèm tệp `.sha256` với `gzip(mtime=0)`, đọc `yield_per` thay `.all()`, và **ném lỗi thay vì nuốt**. Chưa cấu hình R2 thì TỪ CHỐI chạy chứ không lùi về ghi `uploads/` — thư mục đó `main.py` gắn ra `/api/uploads` bằng `StaticFiles` không có lớp gác nào. **(5) `record(...)` cho `role/` + `user/`** — cả hai phân hệ trước đây có ĐÚNG KHÔNG lời gọi nào, nghĩa là `PUT /api/roles/{id}/permissions` ghi đè cả ma trận phân quyền mà không để lại dòng audit. Thêm 10 lời gọi, trong đó `describe_permission_change()` kể ra phần chênh ("Cấp thêm ... Thu hồi ... Đổi phạm vi ..."). **(6) Nhóm hành động** — `ACTION_GROUP_PERMISSION` trước đây có **không thành viên** (bộ lọc "Phân quyền" trên màn đọc luôn trả rỗng), nay 6 mã; `closed` hết rơi vào nhóm 0. **(7) `device_hash` + `referer`** (migration `94f0a2c4e43c`) — chuẩn hóa `User-Agent` về `chrome\|windows\|desktop` TRƯỚC khi băm, vì băm thô thì Chrome tự cập nhật là sáng thứ Hai cả công ty đổi dấu cùng lúc. Cố ý KHÔNG có cột `user_agent`: miền giá trị là tập đóng nên `device_label()` tra ngược 8 byte ra chữ. Test: 59/59 xanh `test_nhat_ky_lop_may_cr312.py` (thêm 39 bài), 39/39 xanh 3 tệp phân quyền/người dùng. | Không | **Hoàn tất — deploy dev 10/09 (`erp-v2` 337fa9bb) + deploy PROD 10/09 chiều (`main` e21023d1)**, migration `94f0a2c4e43c` ở cả hai | `doc/tai-lieu-ky-thuat/nhat-ky-va-phien-dang-nhap.md` §4.1, §4.1.1, §4.1.2, §6, §9, §10 · `so-ghi-nhan-loi-bao-mat.md` bản 1.2 (thêm **BM-008…BM-014**: bốn dòng CR này vá — rò `error_detail`, lượt đọc không dấu vết, `role/`+`user/` trắng audit, nhật ký một bản trên máy bị tấn công; ba dòng còn **mở** — token hết hạn ghi `user_id = 0`, `record()` tự commit sinh dấu vết ma, `CF-Connecting-IP` tin vô điều kiện) |
| **bao-CR-345-doi-ten-dinh-danh-tieng-viet-modules-v2** | 2026-09-10 | Nội bộ | **[frontend-v2] Dọn nốt tên định danh tiếng Việt trong `src/modules/`.** Tiếp bao-CR-322 (đợt đó chỉ dọn `src/shared/`): phần còn lại nằm trong mã của đồng nghiệp nên tách thành đợt riêng. Đổi **15 hàm** trong danh sách đã rà + toàn bộ biến cục bộ / prop / type tiếng Việt trong chính các tệp đó, tổng **11 tệp** thuộc 3 phân hệ. Đáng chú ý: `approval-node-form.tsx` (`dat`→`setField`, `moNangCao`→`advancedOpen`); `document-type-link-rules-card.tsx` (`luu`→`saveRules`, `xoa`→`removeRule`, `dangKhai`→`declaring`, `huong`→`direction`); `inline-viewable.ts` đổi cả **hàm xuất khẩu** `laAnh`→`isImage`, `laPdf`→`isPdf` và hai hằng `KIEU_ANH_XEM_DUOC`→`VIEWABLE_IMAGE_TYPES`, `KIEU_PDF`→`PDF_CONTENT_TYPE` (kéo theo một dòng import ở `attachment-viewer-dialog.tsx`); `gantt-row.tsx` (component `SoNgay`→`DayCount`, prop `xong`/`laDich`/`hienNhan`→`isDone`/`isLinkTarget`/`showLabel`, `tienDo`→`progress`); `task-date-row.tsx` (type `MocThoiGian`→`DateSlotKind`, `chon`→`pickDate`); các tệp test đổi hàm dựng dữ liệu (`ban`→`makeVersion`, `viec`→`makeTask`, `dong`→`makeActivity`, `nguoi`→`makeAssignee`, `ve`→`renderPicker`/`renderRow`, `doc`→`parseCubic`…). Sửa thêm `describe('soBanKeTiep')` ở `next-version-no.test.ts` — tên hàm đó không còn tồn tại, đổi thành `nextVersionNo`. **Chuỗi hiển thị, tên `it(...)` và comment tiếng Việt giữ nguyên**: bộ đổi tên bỏ qua vùng comment và mỗi tệp soi lại `git diff` từng dòng, vì regex `\bxong\b` nuốt luôn chữ "xong" trong câu tiếng Việt. Cổng: typecheck 0 lỗi, lint 0 lỗi / 32 cảnh báo (đúng nền cũ), 239 tệp test / 2748 test xanh. | Không | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8) | frontend-v2/.claude/rules/naming.md |
| **bao-CR-322-in-don-mua-hang-v2-doi-ten-doc-tien** | 2026-09-10 | Nội bộ | **[frontend-v2] Mở lối vào bản in _In Đơn mua hàng_ + bỏ tên hàm tiếng Việt.** Đính chính bản rà 10/09: mẫu _Đơn mua hàng_ **đã có** ở v2 — `purchase-order-print-page.tsx` gộp hai mẫu của v1 vào một trang, mẫu này là nhánh `goods` (`PurchaseOrderPrintGoodsForm`). Thứ thiếu là **lối vào**: v1 có 4 mục in, v2 chỉ 3, muốn xem mẫu này phải mở bản in rồi bấm công tắc. Sửa: (1) trang in nhận prop `defaultMode`, thêm route `/print/purchase-order-mh/:id` (giữ nguyên đường dẫn v1 để link đã lưu vẫn mở đúng mẫu) mở thẳng nhánh `goods`; (2) chi tiết ĐMH thêm nút **In Đơn mua hàng**, nút cũ đổi nhãn _In đơn_ -> **In Đơn đặt hàng** cho khỏi lẫn hai mẫu; (3) ô Ghi chú của mẫu này in thêm dòng đậm _Ngày cần hàng_ như v1 (trước chỉ in `note`). **Đổi tên hàm:** bỏ `modules/finance/utils/doc-tien.ts` (`parseMoney`) — trùng thuật toán với `shared/utils/number-to-vietnamese-words.ts`; bản in YCTT dùng chung `numberToVietnameseWords`, hai ca test riêng của bản cũ gộp sang tệp test giữ lại. Rà thêm tên định danh tiếng Việt trong `src/shared/`: đổi 15 chỗ (`ngayISO`/`khoang`/`tu`/`den`/`moc` ở `apply-client-filter.ts` và các hàm phụ trong tệp test). Cổng: typecheck 0 lỗi, lint 0 lỗi / 32 cảnh báo (đúng nền cũ), 239 tệp test / 2748 test xanh. | Không | **Đã deploy dev 10/09/2026** (`erp-v2` 7d98c3c8) | 05-don-mua-hang.md |
| **bao-CR-320-ycmh-lich-su-mua-hang-moi-trang-thai** | 2026-09-09 | Khách (ticket 36 prod) | **[YCMH] Nút tra cứu Lịch sử mua hàng hiện ở mọi trạng thái.** Trước: nút chỉ nằm trong nhánh `editable` (nháp / từ chối) của ô Mã hàng ở `PurchaseRequestDetail.tsx`, phiếu đã duyệt / đã điều phối là mất nút. Sửa: nhánh không sửa được cũng có nút; popup `PurchaseHistoryPickerModal` truyền `readOnly={!editable}` (cơ chế đã có sẵn từ ĐMH) — bỏ cột Chọn, không ghi đè dòng, chỉ tham khảo. Thuần frontend v1, không test mới, typecheck 4 lỗi cũ. Tài liệu: 03-yeu-cau-mua-hang.md mục tham chiếu giá cũ. Commit `6bba4ea4`, **deploy prod 09/09/2026** cùng đợt với CR-319. **Port `frontend-v2` 09/09/2026 (local, chưa commit):** `purchase-request-items-table.tsx` bỏ điều kiện `editing` ở nút lịch sử, `PurchaseHistoryDialog` nhận `readOnly={!editing}` (prop có sẵn), tiêu đề nút đổi *(chỉ xem)* khi phiếu khóa. | Không | Đã deploy prod (v1) · port v2 đã deploy dev | 03-yeu-cau-mua-hang.md |
| **bao-CR-321-dieu-khoan-in-dmh-theo-ncc** | 2026-09-09 | Khách (ticket 35 prod) | **Điều khoản bản in ĐMH theo từng NCC: số ngày kiểm tra hàng, số ngày thu hồi / đổi trả, thời gian nhận hóa đơn.** Khách chốt làm đủ 3 ô. Trước: mục 2 ("Chậm nhất 24h kể từ khi nhận hàng") và mục 5 (kiểm tra 15 ngày, thu hồi 07 ngày) chốt cứng trong `PrintPurchaseOrder.tsx`; mục 1 (thanh toán) đã theo NCC từ trước, không đụng. Thiết kế: 3 cột `inspection_days` / `return_days` (SmallInteger, 0 = chưa khai) + `invoice_deadline` (String 255, rỗng = chưa khai) trên CẢ `tab_supplier` (nguồn) và `tab_purchase_order` (bản chép, sửa riêng từng đơn lúc nháp, khóa sau duyệt như `payment_terms`; KHÔNG vào `ORDER_FIELDS_EDITABLE_AFTER_APPROVAL`). Chọn NCC trên ĐMH chép 3 ô xuống (NCC chưa khai thì giữ giá trị đơn); `copy_po` chép nguyên; `create_po` nhận 3 ô. Bản in đọc khóa mới `print_terms` do `resolve_print_terms(po, sup)` gộp đơn -> NCC -> mặc định (giữ đúng 15 · 07 · 24h cũ nên đơn / NCC cũ in y hệt); `*_label` hai chữ số để in "07". Migration `c9d3e7a1f5b6` (revises b7e2c4d9a1f3, server_default 0 / '') đã chạy local. Frontend v1: `SupplierDetail.tsx` (3 ô card tài chính + body lưu), `cruds.tsx` suppliers fields, `PurchaseOrderDetail.tsx` (state mặc định, `onPickSupplier`, `save()`, 3 ô đầu đơn dưới Hình thức thanh toán), `PrintPurchaseOrder.tsx`. Test mới `test_po_dieu_khoan_in_cr321.py` 14 pass (ba mức lùi, khoảng trắng, đối tượng chưa có cột, schema chặn ngoài 0–365, create/copy giữ điều khoản); `test_po_tien_te_cr319.py` vẫn pass; typecheck 4 lỗi cũ. Tài liệu 07 mục 14–16, 04 mục A.23–25 + E. Commit `f033195c` (kèm chỉnh cụm "Theo loại chi phí" dồn mép phải trên ĐMH nhập khẩu). **Deploy prod 09/09/2026 ~14:20**: backup `~/proc_backups/procurement_truoc_cr321_20260909.sql.gz` (1,2 MB, 59 bảng, prod trước = 6bba4ea4 / b7e2c4d9a1f3), `git reset --hard origin/main`, build api + web + celery-worker + celery-beat, migration c9d3e7a1f5b6 chạy tự động, 6 cột mới đủ 2 bảng, health 200. Rollback: `git reset --hard 6bba4ea4` + build lại + nạp dump. **Port `frontend-v2` 09/09/2026 (local, chưa commit):** `production/types/supplier.ts` + `supplier-crud.tsx` (3 ô ở nhóm tài chính, `percent` không dùng vì là số ngày / chữ), `purchase-order-detail.ts` + `purchase-order-draft.ts` + `purchase-order-api.ts` (3 ô đầu đơn, `PurchaseOrderPrintData.print_terms`), `purchase-order-info-card.tsx` (3 ô dưới Hình thức thanh toán, chép từ NCC lúc chọn), `utils/purchase-order-print-terms.ts` (`resolvePrintTerms` — cùng ba mức lùi đơn → NCC → mặc định 15 · 07 · 24h, có test 4 ca) và `purchase-order-print-order-form.tsx` đọc từ đó. Cổng `erp npm run check` xanh. | Có (`c9d3e7a1f5b6`) | Đã deploy prod (v1) · port v2 đã deploy dev | 04-don-mua-hang.md, 07-danh-muc.md |
| **bao-CR-319-don-hang-nhap-khau** | 2026-09-08 | Khách | **P1 + P2 + P3 XONG MÃ trên v1 (`main` + `frontend/`) — CHƯA commit, CHƯA deploy.** Kiểm chứng: 24/24 test mới (`test_po_tien_te_cr319.py` 10 + `test_po_chi_phi_nhap_khau_cr319.py` 14) xanh, 115/116 test cũ vùng ĐMH + công nợ + lịch sử mua hàng xanh (1 lỗi CÓ SẴN, không liên quan: `test_pr_line_no_po_cr074.py::test_moi_lap_don_nhap_thi_phieu_van_o_da_dieu_phoi`), typecheck `frontend/` giữ nguyên đúng 4 lỗi có sẵn (không phát sinh lỗi mới); hai migration `c5a81b3f6d24` (P1/P2) và `ed610675320b` (P3) đều chạy `upgrade → downgrade → upgrade` trên MySQL 8 tạm sạch, backfill `base_amount = amount` đúng, index `ix_tab_purchase_order_order_type` có thật, NCC `NSNN` *Ngân sách nhà nước* tạo đúng một lần; `alembic heads` một đầu duy nhất. Dữ liệu thử local: script `backend/scripts/seed_demo_import_po.py` dựng 2 đơn nhập khẩu mẫu (đơn CNY đã duyệt + tờ khai + 6 dòng chi phí + đã nhận hàng · đơn USD lẫn dòng VNĐ, 3 dòng chi phí khai ngay lúc lập), chạy lại nhiều lần vẫn ra đúng 2 đơn. Còn lại P4 + P5, và đợt port `frontend-v2` theo luật v1 trước. Đơn mua hàng NHẬP KHẨU: đơn ngoại tệ, thuế nhập khẩu nộp **ngân sách nhà nước** chứ không trả nhà cung cấp bán hàng, và một lô hàng gánh nhiều khoản chi phí (cước biển, nâng hạ, kiểm dịch, thuế) của **nhiều nhà cung cấp khác nhau**. **Ranh giới:** đúng là landed cost, nhưng hệ chưa có phân hệ hóa đơn nên **không tính giá vốn / giá bán / giá nhập kho, không lưu kết quả phân bổ**; chỉ lưu chi phí + xuất báo cáo. Vài đơn một năm nên **không làm custom-field engine**. **Phân bổ CÓ, nhưng chỉ để xem** — tính lúc in / lúc xem, không ghi xuống cột nào, không đẩy vào kho; cách chia khai ở `allocation_method` (theo giá trị · khối lượng · số lượng · chỉ định một mã), phần lệch do làm tròn dồn vào dòng hàng cuối để tổng luôn khớp. **P1 — nền tiền tệ:** thêm `currency` + `exchange_rate` cho `tab_purchase_order` và `tab_po_item`, thêm `base_amount` (quy đổi) + `weight_kg` + `dimension` cho dòng hàng. **Cấm đặt tên cột kiểu `amount_vnd`** — khóa cứng một loại tiền là sai hướng. Tiền tệ áp cho MỌI đơn, đơn thường mặc định VNĐ / tỷ giá 1 nên dữ liệu cũ không đổi số. **Bẫy đã tránh:** `POItemIn.currency` để mặc định **rỗng** chứ không phải `"VND"` — đặt sẵn VNĐ ở lớp schema thì dòng sinh tự động (tạo ĐMH từ YCMH, nhân bản đơn) sẽ mang cứng VNĐ vào đơn ngoại tệ; rỗng thì `_save_items` chép loại tiền + tỷ giá từ đơn xuống. Tỷ giá 0 luôn được hiểu thành 1 qua hàm `service.rate_of` — nhân với 0 là mất trắng số tiền mà không có lỗi nào nổi lên. Ngoài `recompute_effects` còn **hai chỗ cộng tiền ở phân hệ khác** cũng phải quy đổi: `dashboard/controller.py::item_amt` và `report/service.py::_amt` / `_recv_amt`. Quy đổi phải cắm đúng **ba chỗ tiền rời khỏi phân hệ ĐMH** trong `recompute_effects` (giá vào tồn kho · gốc+VAT công nợ hàng · `it.amount`), ẩn ở giao diện thôi là không đủ. **P2 — loại đơn:** `order_type` **SMALLINT + IntEnum** (1 trong nước mặc định · 2 nhập khẩu) theo luật R2/QĐ-11 cho cột mới, kèm `customs_decl_no` + `customs_decl_date`; danh sách ĐMH thêm cột đánh dấu đơn nhập khẩu + lọc theo loại đơn, màn chi tiết thêm ô chọn loại đơn và cụm tờ khai hải quan chỉ hiện với đơn nhập khẩu. **P3 — bảng chi phí + khóa VAT:** bảng PHẲNG `tab_po_import_cost`, **mỗi dòng chi phí mang NCC riêng của nó** (hãng tàu · đơn vị khai thuê · kho bãi · *Ngân sách nhà nước* cho các khoản thuế) chứ không lấy theo NCC bán hàng của đơn. `cost_type` + `allocation_method` là **SMALLINT + IntEnum** theo R2/QĐ-11; `amount` là tiền **nguyên tệ TRƯỚC thuế**, `base_amount` = `amount × (1+vat%) × tỷ giá` (đã gồm VAT, đã quy đổi). **Khóa VAT dòng hàng = 0 ở BACKEND** cho đơn nhập khẩu (`_save_items`), không chỉ ẩn trên giao diện: hóa đơn NCC nước ngoài không có thuế GTGT Việt Nam, thuế GTGT hàng nhập nộp ngân sách theo tờ khai và đã khai thành một dòng chi phí — để người dùng gõ VAT ở dòng hàng nữa là cộng thuế HAI LẦN đồng thời ghi nợ khoản thuế đó cho chính người bán, người không nhận đồng nào. Đơn trong nước KHÔNG đổi (có test chốt chặn hồi quy). **Bảng chi phí cố ý MỞ sau khi đơn đã duyệt** (`chan_sua_don_da_duyet` bỏ qua khóa `import_costs`) vì hóa đơn cước, tờ khai thuế, phí lưu bãi đều về sau ngày duyệt; các ô khác vẫn khóa như cũ. **Bẫy đã tránh:** (a) chi phí trả bằng VNĐ nằm trong đơn ngoại tệ **không mượn tỷ giá của đơn** — cùng đồng tiền với đơn thì theo tỷ giá đơn, khác thì để 1, không thì phồng lên 25.000 lần; (b) không gửi khóa `import_costs` (`None`) = *không đụng tới bảng*, gửi mảng rỗng = *xóa hết* — hai nghĩa khác nhau, nếu gộp thì mọi lần lưu dòng giao hàng đều quét sạch bảng chi phí; (c) `cost_type` lạ đẩy về *Chi phí khác*, chia *chỉ định một mã* mà bỏ trống mã hàng thì hạ về *theo giá trị* — không rơi vào cột theo kiểu im lặng; (d) migration đọc cột `tab_supplier` từ `information_schema` chứ không gõ cứng danh sách (mỗi môi trường một tập cột), và `downgrade` **cố ý giữ lại** NCC *Ngân sách nhà nước* vì nó có thể đã gắn vào công nợ thật. **Giao diện `frontend/`:** thẻ *Chi phí lô hàng nhập khẩu* chỉ hiện với đơn nhập khẩu, bảng phẳng sửa tại chỗ, chọn loại thuế thì tự điền NCC *Ngân sách nhà nước*, dải tổng 3 số (tiền hàng quy đổi · tổng chi phí · tổng giá trị lô) + gom theo loại chi phí và theo NCC; ô VAT dòng hàng khóa kèm câu giải thích. **Sửa luôn một lỗi P2 bỏ sót:** hàm `save()` của `PurchaseOrderDetail.tsx` **không gửi** `order_type` / `currency` / `exchange_rate` / `customs_decl_no` / `customs_decl_date` và `currency` / `exchange_rate` / `weight_kg` / `dimension` của dòng hàng — chọn *Nhập khẩu* trên màn hình rồi bấm Lưu vẫn ra đơn trong nước. **Dọn tên tiếng Việt không dấu (đại ca chỉ ra 08/09):** đổi hết định danh trong vùng ĐMH sang tiếng Anh theo luật đặt tên, **lấy đúng bộ tên nhánh `erp-v2` đã đổi từ trước** (kiểm bằng cách so `service.py` hai nhánh) để lần merge `main → erp-v2` không đụng nhau vì hai tên khác nhau cho cùng một hàm — `TRANG_THAI_DA_DUYET`→`APPROVED_STATUSES`, `TRUONG_DONG_SUA_SAU_DUYET`→`LINE_FIELDS_EDITABLE_AFTER_APPROVAL`, `TRUONG_DON_SUA_SAU_DUYET`→`ORDER_FIELDS_EDITABLE_AFTER_APPROVAL`, `NHAN_TRUONG_DONG`/`NHAN_TRUONG_DON`→`LINE_FIELD_LABELS`/`ORDER_FIELD_LABELS`, `_khac_nhau`→`_differs`, `chan_sua_don_da_duyet`→`block_edit_approved_order`, `TRUONG_BAT_BUOC_DONG`→`REQUIRED_LINE_FIELDS`, `thieu_truong_dong`→`missing_line_fields`, kèm các biến cục bộ trong `service.py` / `controller.py`, hàm `_them_ncc_ngan_sach` của migration `ed610675320b` và toàn bộ script `seed_demo_import_po.py`. Chuỗi hiển thị, câu báo lỗi và chú thích **giữ nguyên tiếng Việt**; **tên hàm test pytest giữ nguyên** vì đó là quy ước sẵn có của bộ test. Cập nhật luôn nơi gọi ở `controller.py`, ba tệp test, và các tài liệu còn trỏ tên cũ (`doc/erp/13`, `doc/erp/tai-lieu-ky-thuat/06a`, `CLAUDE.md`, `frontend-v2/.../purchase-order-detail.ts`). Chỗ duy nhất cố ý KHÔNG chép theo v2: biến `width` trong `missing_line_fields` bên v2 là dịch nhầm chữ *rỗng* thành *rộng*, bên `main` đặt `is_empty` — merge tới đó chọn bản `main`. Kiểm lại: 91/91 test của 4 tệp liên quan xanh, script dữ liệu mẫu chạy lại ra đúng số cũ. **P4 (08/09) — phân bổ chỉ để xem + bản in nhập khẩu 4 khối:** hàm thuần `service.allocate_import_costs(items, costs)` nhận/trả dict đúng dạng `_item`/`_import_cost`, gắn vào `_out` dưới khóa `import_cost_allocation` nên màn hình, bản in (`GET /{pid}/print` dùng lại `_out`) và YCTT sau này đọc cùng một con số; không lưu, không đẩy vào kho. Luật chia: theo giá trị (mặc định) · khối lượng (`weight_kg`) · số lượng (`qty_order`) · chỉ định mã (`allocation_target`, mọi dòng trùng mã chia nhau theo giá trị → SL → đều). Cơ sở chia cộng lại bằng 0 (chưa gõ kg, SL 0, mã không có trên đơn) thì LÙI về giá trị, giá trị cũng 0 thì chia đều, và ghi vào `warnings` + `effective_method` khác `allocation_method` để giao diện/bản in đánh dấu (*) chứ không im lặng đổi cách. Làm tròn 2 số lẻ, phần lệch DỒN VÀO DÒNG CUỐI trong số các dòng có nhận khoản đó nên tổng luôn khớp. Giao diện `frontend/`: panel "Chi phí theo dòng hàng" chỉ đọc trên `PurchaseOrderDetail.tsx` (chỉ đơn NK đã lưu), lồng NGƯỢC dòng hàng là CHA → mở ra từng khoản + cách chia + tỷ lệ, chân bảng là tổng toàn đơn; tính từ dữ liệu ĐÃ LƯU nên có dòng nhắc "sửa chi phí xong bấm Lưu". Trang in mới `PrintPurchaseOrderImport.tsx` (route `/print/purchase-order-import/:id`, nút "In Đơn nhập khẩu" trong dropdown In, chỉ hiện với đơn NK): A hàng hóa (nguyên tệ + quy đổi, cột kg, VAT 0) · B chi phí lồng 2 tầng theo loại kèm NCC · C phải trả theo từng NCC (NCC bán hàng + `by_supplier`) · D chi phí chia về dòng hàng. Hai bản in thường không đụng. Test mới `test_po_phan_bo_chi_phi_cr319.py` 12 ca (tỷ lệ, dồn lệch dòng cuối, 4 cách chia + 3 kiểu lùi, mã lạ, đơn trống, đi qua `_out`); 26/26 test CR-319 xanh; typecheck `frontend/` vẫn đúng 4 lỗi cũ. **P5 (09/09) — công nợ từng dòng chi phí + YCTT gom theo NCC + thẻ đã chi / còn phải chi:** dùng lại `tab_payable`, mỗi dòng chi phí thành MỘT khoản nợ `source_type = ref_type = "import_cost"`, `ref_id` = id dòng chi phí — không dựng luồng công nợ thứ hai nên màn Công nợ, tuổi nợ, YCTT, dashboard NCC, xuất Excel dùng lại y nguyên (chỉ thêm nhãn *Chi phí nhập khẩu* qua helper dùng chung `frontend/src/utils/payable.ts`, thay 7 chỗ gõ cứng goods/shipping). Luật đồng bộ `service.sync_import_cost_payables` (idempotent, gọi sau mỗi lần lưu bảng chi phí và mỗi lần đổi trạng thái): đơn `approved/partial/received/completed` mới sinh nợ; dòng 0 đồng / chưa khai NCC không thành nợ; `amount` = gốc trước VAT đã quy đổi, `vat` = tiền thuế, `total = base_amount`; `incur_date` = ngày HĐ của dòng (trống thì ngày đặt), `due_date` ưu tiên ô Hạn thanh toán của dòng rồi mới tới điều khoản NCC (`pay_service.upsert` nhận thêm `ref_type` + `due_date`); sửa dòng thì nợ đổi theo, bỏ NCC / xóa dòng thì gỡ nợ chưa chi, **dòng đã chi thì cấm xóa** (`block_delete_paid_import_cost`, 400 "đã chi"), hủy đơn giữ nợ đã chi gỡ nợ chưa chi (cùng cách với nợ hàng). `unpaid_total` header cộng cả nợ chi phí; báo cáo công nợ theo ngày cộng vào tổng nhưng không đếm vào cột hàng. Phía YCTT: `SOURCE_TYPES` nhận `import_cost`; `check_submit` và chi tiền không khớp được theo (NCC, loại, mã ĐMH, số HĐ) thì lùi về `payable_id` của dòng — nhiều dòng chi phí cùng ĐMH có thể chưa có số HĐ lúc lập phiếu, số HĐ gõ ngay trên phiếu vẫn bắt buộc lúc gửi duyệt. Giao diện `frontend/` màn ĐMH: bảng chi phí thêm cột tick (chỉ dòng đã có nợ còn phải chi) + nút *Tạo YCTT (n dòng đã tick)* + hai cột *Đã chi* / *Còn lại*; dải thẻ số thành 5 thẻ (tiền hàng · tổng chi phí NK · **đã chi** · **còn phải chi** · tổng giá trị lô, có % so tiền hàng); khối mới **"Thanh toán chi phí theo nhà cung cấp"** mỗi NCC một dòng Số khoản / Phải trả / Đã chi / Còn lại + nút Tạo YCTT riêng (gửi `unpaid_payable_ids`, chi đủ thì "Đã chi đủ") — đường chính vì YCTT chỉ nhận 1 NCC/phiếu, bấm là ra phiếu hợp lệ; popup Tạo YCTT của đơn thêm tab thứ ba *Chi phí lô hàng* (chỉ hiện khi có nợ loại này, tab mở sẵn = tab đầu có nợ). Cả ba đường về `/payment-requests/new?payables=...`, server tự tách mỗi NCC một phiếu. Nhân tiện dời hai cột *Cách phân bổ* / *Mã hàng chỉ định* lên ngay sau *Diễn giải* — trước nằm cuối bảng rộng 2.000px nên đại ca tưởng chưa có giao diện phân bổ. Test mới `test_po_cong_no_chi_phi_cr319.py` 7 ca (nháp không sinh nợ · duyệt sinh nợ đúng tiền/VAT/HĐ/hạn/NSNN/summary/unpaid_total · sửa-bỏ NCC-thêm lại-xóa · cấm xóa dòng đã chi · hủy đơn · tạo YCTT tách NCC + gửi duyệt chặn thiếu HĐ + chi tiền trừ đúng khoản · phiếu gõ tay nhận loại mới); 43/43 test CR-319 xanh, 79/79 test hồi quy vùng công nợ/YCTT/báo cáo xanh, typecheck `frontend/` vẫn đúng 4 lỗi cũ. Tài liệu: mục K mới (P3..P5) + B.29 + F.11 ở `04-don-mua-hang.md`, luồng 3 ở `06-ton-kho-cong-no.md`, `05-yeu-cau-thanh-toan.md` §4. **Còn nợ:** commit + deploy prod (theo lệnh), rồi merge `main → erp-v2` (cần bản gộp đầu Alembic cho `c5a81b3f6d24` + `ed610675320b` với `b1e4d7a03c58`) và đợt port `frontend-v2`. Chưa nhìn P4/P5 bằng mắt trên trình duyệt. **P4b (09/09) — cách chia 5 "Nhập tay" + đổi nhãn:** khách xem panel xong chốt: giữ nguyên bảng "Chi phí theo dòng hàng" (không popup, không thiết kế lại), chỉ thêm option *Nhập tay* vì thu mua thường gõ tay để cân số với chứng từ, giữ *Chỉ định một mã hàng* một mã, bỏ chữ "gánh" khỏi nhãn, và **sửa xong phải bấm Lưu của đơn** mới có hiệu lực (không nút lưu riêng). Làm: `AllocationMethod.MANUAL = 5`; cột mới `tab_po_import_cost.manual_allocation` TEXT JSON `{"<id dòng hàng>": số tiền VNĐ}` — cách chia DUY NHẤT phải lưu kết quả vì số do người gõ; migration `b7e2c4d9a1f3` (revises `ed610675320b`, head mới). Lưu (`_save_import_costs`): chỉ giữ khóa của dòng hàng còn tồn tại (`_save_items` chạy trước nên id đã có), tổng phải bằng `base_amount` dung sai 1 đ, lệch hoặc chưa gõ dòng nào thì **400** kèm câu "tổng đã nhập X, phải bằng Y (lệch Z)"; đổi sang cách khác thì xóa JSON. Xem (`allocate_import_costs`): khớp tổng thì dùng nguyên số gõ (`effective_method = 5`, tỷ lệ = số gõ / khoản, dòng không gõ không nhận), lệch (dòng bị xóa sau / số khoản đổi) thì lùi về giá trị + `warnings` như các cách khác; `parse_manual_allocation` nhận cả chuỗi JSON lẫn dict, bỏ giá trị hỏng. Giao diện `frontend/`: option *Nhập tay*; chọn xong panel tự mở mọi dòng, mỗi dòng hàng có thêm dòng con nền vàng với ô gõ số tiền (điền sẵn số đang chia từ dữ liệu đã lưu để chỉ sửa vài dòng), dải báo sống *đã nhập / phải bằng / lệch* và ô trạng thái ngay tại cột Mã hàng chỉ định; `save()` chặn sớm khi lệch; nút *Mở tất cả / Thu gọn*; đổi nhãn *Chi phí gánh*→*Chi phí phân bổ*, *Phần gánh (đ)*→*Số tiền (đ)*, *CP / tiền hàng*→*Tỷ lệ chi phí* (cả bản in NK); nới cột Diễn giải 190→320px (khách báo bị cắt chữ). Test: +6 ca `test_po_phan_bo_chi_phi_cr319.py` (khớp tổng, đọc JSON, lệch tổng lùi về giá trị, chưa gõ, dòng không gõ không nhận, parse bỏ rác) +4 ca `test_po_chi_phi_nhap_khau_cr319.py` (lưu JSON theo id + bỏ id lạ, lệch 400, chưa gõ 400, đổi cách xóa JSON) → 53/53 test CR-319 xanh; typecheck `frontend/` vẫn đúng 4 lỗi cũ; migration đã chạy local, `alembic heads` một đầu `b7e2c4d9a1f3`. Tài liệu: K.1 thêm dòng `manual_allocation`, K.2 thêm đoạn cách 5. **P4c (09/09, chiều) — popup chi tiết khoản chi phí + Diễn giải xuống dòng:** khách xem bảng chi phí 7 dòng, hỏi loại chi phí trùng được không (trả lời: cố ý, một dòng = một hóa đơn của một NCC), muốn có cây bút mở chi tiết như dòng hàng, và Diễn giải bị cắt chữ. Làm ở `PurchaseOrderDetail.tsx`: cột cuối đổi *Xóa*→*Hành động* (cây bút mọi trạng thái + thùng rác khi sửa được); ô Diễn giải và Ghi chú chuyển sang `TextAreaAuto` (`cell-textarea`, xuống dòng + cao theo nội dung); popup *Chi tiết chi phí #n* (state `editingCostIdx`, cùng khuôn popup dòng hàng, rộng 860) bày đủ trường theo form hai cột và ghi cùng `setCost` nên không có nút lưu riêng, vẫn qua Lưu của đơn; đầu popup nói rõ vì sao dòng chưa thành công nợ. Không đụng backend, không thêm test (thuần giao diện); typecheck vẫn đúng 4 lỗi cũ. Tài liệu: K.1 ghi loại trùng được + Diễn giải xuống dòng, K.2 thêm đoạn popup. **P5b (09/09, chiều) — đơn NK chỉ Hoàn thành khi trả đủ chi phí lô hàng:** khách hỏi đơn hoàn thành khi nào (trả lời: tiến độ dòng hàng chỉ xét nợ hàng, chi phí lô hàng không xét) rồi chốt "mức chặt, chỉ đơn nhập khẩu". Backend `service.block_complete_unpaid_import_costs` gọi trong `complete_po` sau kiểm dòng hàng: đơn `order_type=IMPORT` duyệt từng dòng chi phí có tiền, chưa thành nợ hoặc `total - paid_amount > 0,01` → 400 liệt kê từng khoản; đơn trong nước không đổi. Frontend: hộp xác nhận Hoàn thành nhắc số còn lại. Test +3 ca (chặn còn nợ / chặn dòng chưa chọn NCC / đơn trong nước và đơn không chi phí không bị chặn) → tệp `test_po_cong_no_chi_phi_cr319.py` 10 ca. Tài liệu: K.4 luật 8 + mục 15. Khách hỏi thêm YCTT dòng thuế không hóa đơn có bị gom: backend `_line_rows` chỉ gộp khi có số HĐ, dòng trống số HĐ giữ riêng; bản in `PrintPaymentRequest.tsx` gộp theo `số HĐ||ngày HĐ` nên hai dòng cùng trống cả hai sẽ dính thành một dòng in — chưa sửa, chờ khách. **Rà hồi quy đơn trong nước (09/09):** chạy 10 tệp test ĐMH/công nợ/YCTT cũ → lộ lỗi commit P1-P4 (d4aefd05, chưa lên prod) đổi tên `ten_dong`→`line_name` ở `_save_items` nhưng sót một chỗ trong f-string → đổi mã/tên/ĐVT dòng đã nhận hàng sẽ 500 thay vì 400; đã sửa, quét AST 4 tệp service không còn tên chưa khai, 130 ca đậu. **Ẩn VAT dòng hàng ở đơn NK (09/09, khách yêu cầu):** `PurchaseOrderDetail.tsx` ẩn cột VAT% + Đơn giá (Sau VAT) ở bảng dòng hàng (minWidth trừ 189), hai ô cùng tên ở popup dòng, cột VAT% ở bảng lần giao khi `isImport`; backend vẫn ép 0; typecheck 4 lỗi cũ. **Commit `d4aefd05` (P1-P4) + `f499eaa3` (P4b-P5b); DEPLOY PROD 09/09/2026 11:47 (VN):** sao lưu trước ở VPS `~/proc_backups/procurement_truoc_cr319_20260909_1146.sql.gz` (58/58 bảng, alembic `a7c3e91f5b28`), reset `origin/main` = `6bba4ea4`, build api + web + celery-worker + celery-beat với `-f docker-compose.production.yml`; 3 migration chạy sạch, `alembic current` = `b7e2c4d9a1f3`, health 200, 140 ĐMH cũ đều nhận `order_type=1` / VND / tỷ giá 1. Lùi lại nếu cần: `git reset --hard 409da792` + build lại, rồi nạp dump trên. **Sau deploy (09/09 chiều):** khách xem màn thật, cụm tổng *Theo loại chi phí* dưới bảng "Thanh toán chi phí theo NCC" dàn hết bề ngang (grid auto-fit chỉ còn 1 cụm) nên nhãn bên trái, số tận bên phải; đổi sang flex dồn về mép phải, mỗi cụm rộng 380px — CHƯA commit. **Merge `main → erp-v2` 09/09/2026: commit `14358280` (chưa push)** — gộp Alembic bằng bản đầu rỗng `d7f2a9c4e1b8` (revises `b1e4d7a03c58` + `c9d3e7a1f5b6`), `alembic heads` một đầu; DB local `dego-erp` lệch version so với tệp migration nên phải `alembic stamp --purge` rồi `upgrade head`. **Port `frontend-v2` 09/09/2026 (local, chưa commit, chưa deploy dev):** P1/P2 — `purchase-order-detail.ts` (kiểu `PurchaseOrderImportCost` / `ImportCostSummary` / `ImportCostAllocation`, hằng `ORDER_TYPE_*` / `CURRENCY_OPTIONS` / `IMPORT_COST_TYPE_OPTIONS` / `ALLOCATION_METHOD_*`, `isImportOrder`), `purchase-document.ts` (`order_type` ở dòng danh sách), `purchase-order-list-page.tsx` (cột *Loại đơn* + lọc `order_type` trên URL), `purchase-order-info-card.tsx` (ô Loại đơn · Loại tiền · Tỷ giá · cụm tờ khai chỉ hiện đơn NK), `purchase-order-items-table.tsx` + `purchase-order-line-dialog.tsx` (cột KL kg · Quy cách · Quy đổi VNĐ; VAT dòng + Đơn giá sau VAT ẩn ở đơn NK, khớp bản v1 sau 09/09), `purchase-order-draft.ts` (`createEmptyImportCost`, payload gửi đủ `order_type` / `currency` / `exchange_rate` / tờ khai / `import_costs`, không lặp lỗi P2 của v1), `utils/purchase-order-import-cost.ts` (tính tạm khi chưa lưu: `effectiveExchangeRate` · `lineBaseAmount` · `costBaseAmount` · `sumCostBase`, có test). P3/P4/P5 — thẻ mới `purchase-order-import-costs-card.tsx` (bảng chi phí sửa tại chỗ, chọn loại thuế tự điền NCC *Ngân sách nhà nước*, cách chia gồm *Nhập tay* với ô gõ theo dòng + dải lệch tổng, 5 thẻ số, khối *Thanh toán chi phí theo NCC* với nút Tạo YCTT theo `unpaid_payable_ids`, panel phân bổ chỉ đọc đánh dấu (*) khi lùi cách chia), `purchase-order-payment-dialog.tsx` (tab thứ ba *Chi phí nhập khẩu*, tiêu đề cột NCC đổi theo tab), `finance/types/payable.ts` + `payment-api` + `finance-dashboard-page.tsx` (nhãn `import_cost` qua `PAYABLE_SOURCE_LABELS`, hết chỗ gõ cứng goods/shipping), `purchase-order-detail-page.tsx` (nút *In Đơn nhập khẩu* chỉ đơn NK). Bản in NK: route `/print/purchase-order-import/:id` (`app-routes.ts` + `app-router.tsx`), trang mới `purchase-order-import-print-page.tsx` 4 khối A/B/C/D + 3 ô ký đúng bản v1, A4 dọc, khối B gom theo loại chi phí sắp giảm dần, khối C dòng 1 = NCC bán hàng (đã trả = Σ `paid_total` dòng), khối D lồng dòng hàng → khoản, `(*)` + chú thích khi `effective_method` khác `allocation_method`, danh sách `warnings`; test `purchase-order-import-print-page.test.tsx` 3 ca (đủ 4 khối + dấu (*) + tỷ giá/tờ khai · lỗi tải · đơn chưa có chi phí). Cổng `erp npm run check` xanh (typecheck 0, lint 0 lỗi, 296 test vùng thu mua/tài chính/sản xuất). Dữ liệu thử local: `seed_demo_import_po.py` chạy lại ra 3 đơn (PO00360 CNY đã nhận hàng, PO00361 USD, PO00362 USD chưa phân bổ). **Đợt sửa giao diện v2 theo phản hồi khách 09/09 (local, chưa commit):** khách mở đơn NK trên `deverp` nêu 5 điểm — (1) chữ trong bảng chi phí khác cỡ so với bảng dòng hàng: bỏ `h-8 text-xs` ở mọi ô để thừa hưởng cỡ chuẩn của `LinesTable` (13,5px thân / 13px đậm tiêu đề); (2)+(3)+(4) thẻ chi phí v2 thiếu gần hết phần v1: viết lại `purchase-order-import-costs-card.tsx` cho đủ — panel *Chi phí theo dòng hàng* (lồng ngược dòng hàng là cha, mở ra từng khoản + cách chia + tỷ lệ, có ô gõ tay khi chọn *Nhập tay*), popup *Chi tiết chi phí #n* mở bằng cây bút, cột tick + nút *Tạo YCTT*, khối *Thanh toán chi phí theo NCC*, 5 thẻ số, hai cụm tổng theo loại / theo NCC; bảng dòng hàng thêm lại cột *Phân loại* của v1; (5) số ngoại tệ hiện sai đơn: thêm `formatMoneyWithCurrency` ở `shared/utils/format-money.ts` (VNĐ làm tròn + *đ*, ngoại tệ giữ số lẻ + mã tiền) và `displayLineBaseAmount` / `isMissingExchangeRate` để dòng chưa có tỷ giá hiện *Chưa có tỷ giá* thay vì *0 đ*. Thêm hai phản hồi rời: nền trường bị khóa quá đậm → `--locked` nhạt lại (`#e4eaf1`→`#edf1f6` sáng, `#2b3a4f`→`#27364b` tối, chỉnh cả `index.css` lẫn bảng màu DEGO trong `theme-presets.ts` vì có test gương hai bên); không bôi đen chuột để chép TÊN trường được → shadcn `Label` mang sẵn `select-none` (`user-select: none`), đã bỏ lớp đó kèm chú thích. **Thứ tự cột mặc định:** `useTableLayout` ưu tiên bố cục đã lưu trong `localStorage` và nối cột lạ vào CUỐI, nên máy khách còn bố cục cũ thì cột mới (*Chọn*, *Mã hàng chỉ định*, *Đã chi*, *Còn lại*, *Ghi chú*, *Phân loại*, *Quy đổi VNĐ*) rơi hết xuống cuối bảng — nhìn như bị loạn. Đổi khóa lưu sang `purchase-order-items-v2` và `purchase-order-import-costs-v2`, đồng thời xếp lại cột theo cụm đọc: nhận diện khoản (ghim *#* · *Loại chi phí*) → tiền (tiền tệ · tỷ giá · số tiền · VAT · quy đổi · đã chi · còn lại) → phân bổ → hóa đơn & hạn trả → ghi chú → hành động. Cổng `erp npm run check` xanh (typecheck 0, lint 0 lỗi / 32 cảnh báo cũ, toàn bộ test). **Đợt sửa giao diện v2 vòng 2 (09/09, chiều muộn — 7 điểm khách nêu, local, chưa commit):** (1) nền ô khóa vẫn nặng → `--locked` xuống nấc cuối `#f1f5f9` (sáng) / `#243348` (tối); nhạt hơn nữa là không phân biệt được với ô trắng. (2) tiêu đề cột bị cắt thành `…`: cột tick nới 44→60px (44px vừa ô tick nhưng chữ *Chọn* thành *Ch…*), cột *Loại chi phí* 200px, *Diễn giải* 300px, *Số tiền* 145px. (3) khối *Thanh toán chi phí theo NCC* và cụm *Theo loại chi phí* nằm dàn ngang trông lệt bệt → gói cả hai vào lưới `xl:grid-cols-[minmax(0,1.6fr)_minmax(320px,1fr)]`: bảng NCC bên trái ăn hết phần thừa (cột NCC `w-full`, các cột số cố định 64/120/110/120/104px, thêm hàng chân *Tổng cộng* cộng lại từ chính các dòng đang bày), hai cụm tổng xếp thành thẻ dọc bên phải có viền + đầu thẻ nền mờ + dải tổng dưới chân. (4) nhãn trong ô CHỌN bị cắt: shadcn `SelectTrigger` mang sẵn `whitespace-nowrap` + `line-clamp-1` + `h-9`, phải đè cùng tiền tố biến thể (`WRAPPING_SELECT_TRIGGER`) mới cho xuống dòng — áp cho *Loại chi phí*, *Cách phân bổ*, *Mã hàng chỉ định*. (5) **bỏ hẳn dấu `-` làm giá trị rỗng** (khách: rỗng thì để trống): ô tick, tỷ giá đơn VNĐ, *Đã chi* khi chưa thành nợ, mục tiêu phân bổ, diễn giải, NCC, khối lượng, nhãn cách chia trong thẻ chi phí; bên bản in NK bỏ nốt ở diễn giải, VAT, `percentOf` và cột kg. `ReadOnlyValue` vốn đã coi `—`/`-` là rỗng nên không phải sửa nơi gọi. (6) bản in NK khối B: thêm `<colgroup>` ép bề rộng 32/24/15/13/6/10% (trước để trình duyệt tự chia nên cột NCC phình bằng cột diễn giải) và **bỏ cột *Cách chia*** — khối D vẫn giữ cột đó vì ở đó nó là thông tin chính. Nút *In Đơn nhập khẩu* riêng thì **giữ nguyên theo lệnh khách** ("tạm thời để nó đi"). (7) sọc ngựa vằn quá nhạt → `--row-stripe` đậm lên `#f0f4f9` (sáng); bản tối trước đó bằng đúng `--card` nên **không có sọc**, đổi sang `#243044`. Bốn mã màu đều phải chép song song vào `theme-presets.ts` vì `theme-presets.test.ts` soi gương với `index.css`. (8) khách báo thanh lọc màn **danh sách ĐMH** vỡ bố cục: cụm lọc bọc trong một thẻ `md:flex` riêng nên với thanh công cụ nó là MỘT ô — thêm ô lọc *Loại đơn* là cả cụm rớt nguyên khối xuống dòng dưới, để lại khoảng trống dài bên phải ô tìm kiếm rồi tự xuống dòng thêm lần nữa bên trong (thanh lọc thành 3 dòng, dòng cuối chỉ có 2 nút). Đổi thẻ bọc sang `hidden md:contents` để các ô lọc thành ô trực tiếp của thanh công cụ, xếp kín từng dòng. Ba màn cùng khuôn (`purchase-request-list-page` · `survey-list-page` · `survey-request-list-page`) **đổi luôn cho đồng bộ** dù hiện còn vừa một dòng — để lần sau thêm ô lọc không vỡ lại y hệt. Bốn màn này là toàn bộ nơi dùng khuôn đó. (9) khách hỏi "Mặc định bản in" là gì → bỏ hẳn cụm chữ đó ở ba ô điều khoản in của bao-CR-321 trên màn chi tiết ĐMH (`purchase-order-info-card.tsx`): ô trống nay bày THẲNG con số / câu chữ bản in sẽ in ra (*15 ngày* · *7 ngày* · *Chậm nhất 24h kể từ khi nhận hàng*) thay vì *Mặc định bản in (15 ngày)*. Bản v1 trên `main` KHÔNG có cụm chữ này — bên đó ba ô là `<input>` với placeholder *Trống = 15 ngày*, cách diễn đạt khác nên **không sửa**. Kiểm lại: typecheck 0 lỗi, lint 0 lỗi / 32 cảnh báo cũ, 591 test vùng thu mua + giao diện + bảng màu xanh. **Đã commit + deploy dev 09/09/2026:** cả đợt port v2 (kèm CR-317/318/321 và hai vòng sửa giao diện) nằm trong commit `c8a0380f`, gộp với hai commit của đồng nghiệp thành `d2f0e603`, dev chạy `deverp` HTTP 200. Cổng trước khi đẩy: typecheck 0 lỗi, lint 0 lỗi / 32 cảnh báo cũ, 240 tệp test / 2753 test xanh (số sau khi gộp nhánh). **Còn nợ:** nhìn bản in + thẻ chi phí bằng mắt trên `deverp` (cần đăng nhập). | main → erp-v2 | Đã deploy prod (v1) · port v2 đã deploy dev | Bộ nhớ thiết kế `don-hang-nhap-khau` |
| **bao-CR-318-duong-ve-ycbg-tu-ycmh** | 2026-09-08 | Khách | **ĐỢT 1 (prod) XONG — commit `409da792`, đã deploy prod 08/09/2026. 13/13 test backend xanh (chung tệp với CR-317), typecheck `frontend/` không phát sinh lỗi mới. Đối chiếu trên prod sau deploy: phiếu 113 `PYC08092601` trả về YCBG `YCBG03092602` (id 2736). ĐỢT 2 (port `frontend-v2` + merge `main`→`erp-v2` + deploy dev) CHƯA LÀM — xem mục việc còn nợ cuối dòng.** Màn Yêu cầu mua hàng KHÔNG có đường nào quay về Yêu cầu báo giá đã sinh ra nó, trong khi chiều ĐMH → YCMH thì có sẵn (`pr_code`). Mã YCBG hiện có xuất hiện trên phiếu nhưng chỉ là câu chữ trong ô *Nội dung* ("Từ Yêu cầu báo giá YCBG..."), bấm không ra. **Liên kết thật vốn đã có trong CSDL, chỉ là chưa ai đọc lên:** `tab_survey_request_pr` (mỗi lần chốt phương án tạo YCMH ghi 1 dòng) — trên prod 08/09 có 2 dòng. Thêm hàm `_source_survey_request` trong `purchase_request/controller.py`: tra bảng liên kết trước, không có thì lùi về `tab_survey_request_line.pr_id` (phiếu đời trước khi có bảng liên kết), YCBG bị xóa thì trả rỗng chứ không bày mã cụt. Trả thêm `survey_request_id` + `survey_request_code` ở `_out`; màn chi tiết YCMH hiện thêm ô **Từ yêu cầu báo giá** ngay dưới *Ngày tạo*, bấm được khi có quyền `survey_request.read`, không thì chỉ hiện mã. Phiếu lập tay không hiện ô này. **Không rò rỉ dữ liệu:** mã YCBG vốn đã nằm sẵn trong ô Nội dung của chính phiếu. **CÒN NỢ — đợt 2 `frontend-v2` (luật v1 trước):** backend đã trả sẵn `survey_request_id` + `survey_request_code`, chỉ cần thêm ô **Từ yêu cầu báo giá** vào `frontend-v2/src/modules/procurement/pages/purchase-request-detail-page.tsx` giống bản `frontend/`. **ĐỢT 2 XONG local 09/09/2026 (chưa commit):** merge `main → erp-v2` tại `14358280`; `purchase-request-detail.ts` thêm `survey_request_id` / `survey_request_code`; ô *Từ yêu cầu báo giá* đặt ở `purchase-request-info-card.tsx` (không phải trang, vì thẻ *Thông tin chung* là nơi giữ thứ tự nhãn của bản v1) ngay dưới *Ngày tạo*, thành `Link` sang `surveyRequestDetail` khi `can('survey_request','read')`, không thì chỉ hiện mã; phiếu lập tay không hiện. Còn deploy dev. | main → erp-v2 | Xong đợt 1 (prod) · đợt 2 đã deploy dev | `test/backend/test_lien_ket_ycmh_cr317_318.py` |
| **bao-CR-317-tick-bao-gia-dinh-kem-ban-in** | 2026-09-08 | Khách | **ĐỢT 1 (prod) XONG — commit `409da792`, đã deploy prod 08/09/2026. 13/13 test backend xanh, typecheck `frontend/` không phát sinh lỗi mới. Đối chiếu trên prod sau deploy: phiếu 113 `PYC08092601` trả `has_quote_file = True`. ĐỢT 2 (port `frontend-v2` + merge `main`→`erp-v2` + deploy dev) CHƯA LÀM — xem mục việc còn nợ cuối dòng.** Bản in *Phiếu đề xuất mua hàng hóa/dịch vụ* luôn tick **Báo giá đính kèm: Không** dù phiếu có file báo giá thật (khách báo ở phiếu id 113 = `PYC08092601`, có 2 tệp loại *Báo giá*). **Gốc lỗi:** bản in soi cột `quote_file_url` — ô tải đúng 1 file từ thời đầu, nay không màn nào ghi vào nữa vì chứng từ đã dời hết sang khối đính kèm (`tab_file_link`, loại `quotation`). Đo trên prod 08/09: **0/116 phiếu còn giá trị ở cột cũ** — tức ô tick này sai với MỌI phiếu có báo giá, không riêng phiếu 113. Thêm hàm `_has_quote_file` đếm theo ba nguồn: cột cũ `quote_file_url` (giữ cho phiếu đời đầu) · file loại `quotation` trên entity `purchase_request` (đường đi hiện tại) · entity cũ `purchase_request_quote`. Backend trả thêm `has_quote_file` ở `_out` nên **cả hai lối in đều đúng**: in từ phiếu, và in từ đơn mua hàng (bao-CR-314 đi chung `pr_out`). File loại *Khác* KHÔNG tính là báo giá. **CÒN NỢ — đợt 2 `frontend-v2` (luật v1 trước):** `frontend-v2/src/modules/procurement/pages/purchase-request-print-page.tsx` dòng 171 dính **đúng lỗi này** (vẫn soi mỗi `quote_file_url`), backend đã trả sẵn `has_quote_file` nên chỉ cần đổi chỗ đọc. **ĐỢT 2 XONG local 09/09/2026 (chưa commit):** `purchase-request-detail.ts` thêm `has_quote_file?`; `purchase-request-print-page.tsx` đọc `has_quote_file ?? Boolean(quote_file_url)` — backend cũ chưa trả khóa mới thì vẫn lùi về cột cũ chứ không tick sai. Còn deploy dev. | main → erp-v2 | Xong đợt 1 (prod) · đợt 2 đã deploy dev | `test/backend/test_lien_ket_ycmh_cr317_318.py` |
| **bao-CR-316-tach-cot-ngay-tiep-nhan** | 2026-09-08 | Bảo | **XONG — đã deploy prod 08/09/2026 (commit `1fdbada8`), migration `a7c3e91f5b28` đã chạy. 15/15 test backend xanh, cổng `erp npm run check` xanh. Backfill thực tế trên prod: 116 phiếu → 101 phiếu có Ngày tiếp nhận, 52 phiếu lấy lại được ngày lập gốc; soát lại không còn phiếu đã điều phối nào thiếu Ngày tiếp nhận. ĐợT 2 (port tay sang `erp-v2`: backend + `frontend-v2`, KHÔNG merge `main`→`erp-v2` — đại ca chốt để xong CR-319 rồi merge một thể) XONG 08/09/2026, commit `d9603ac4` + gộp code đồng nghiệp (18 commit HRM/nghỉ phép/đặt xe) đẩy lên `origin/erp-v2` tại `9490ea8b`; bản gộp đầu `b1e4d7a03c58` đổi sang gộp `(4f5033c40f3c, a7c3e91f5b28)` để Alembic còn một head.** Tách *Ngày tiếp nhận* ra cột riêng `received_date` để `request_date` trở lại đúng một nghĩa *ngày lập phiếu*. Gốc lỗi: bao-CR-293 cho `dispatch_pr` ghi đè thẳng `request_date`, nên một cột mang hai nghĩa và ngày lập gốc mất hẳn. Nay `dispatch_pr` chỉ ghi `received_date`; mọi chỗ tính hạn đi qua hàm mới `sla_base_date(pr)` (có Ngày tiếp nhận thì lấy nó, chưa có thì lùi về Ngày lập). Ô lọc trên danh sách tách thành hai (*Ngày lập phiếu* / *Ngày tiếp nhận*, rỗng = chưa tiếp nhận) ở cả `frontend/` lẫn `frontend-v2/`; cột *Ngày tạo* của danh sách v2 sắp theo `created_at` cho khớp con số đang bày. **Migration `a7c3e91f5b28` có backfill**: dựng lại ngày lập gốc từ ghi chú nhật ký `dispatched` của CR-293 (điều phối nhiều lần thì lần ĐẦU cho ngày lập, lần CUỐI cho ngày tiếp nhận); phiếu không có dấu vết nhật ký nhưng trạng thái đã qua điều phối thì chép ngày hiện có sang. Thử trên bản dump dev: 69 phiếu → 35 phiếu có Ngày tiếp nhận, 5 phiếu lấy lại được ngày lập gốc khác hẳn. Nhánh `erp-v2` thêm bản gộp đầu rỗng `b1e4d7a03c58` (KHÔNG cherry-pick sang `main`). | main → erp-v2 | Xong đợt 1 (prod) | `test/backend/test_tach_ngay_tiep_nhan_cr316.py`, `test/backend/test_ngay_tiep_nhan_ticket20.py` |
| **bao-CR-315-ngay-tiep-nhan-chua-dieu-phoi** | 2026-09-08 | Bảo (tự rà, không có phiếu hỗ trợ) | **XONG — đã deploy prod 08/09/2026 (commit `0be014c3`).** Màn *Tạo Yêu cầu Thu mua mới* đã điền sẵn ngày hôm nay vào ô **Ngày tiếp nhận**, trong khi chính nhãn ô đó ghi *(tự điền khi thu mua duyệt điều phối)* — người lập phiếu nhìn vào tưởng thu mua đã nhận việc rồi. **Nguyên nhân:** ô này đọc cột `request_date`, mà cột đó mang HAI nghĩa theo thời gian — lúc tạo phiếu là *ngày lập*, tới bước điều phối thì `dispatch_pr` ghi đè bằng ngày duyệt và từ đó mới là *Ngày tiếp nhận* (bao-CR-293). Giao diện bày nghĩa thứ hai ra ngay từ nghĩa thứ nhất. **Cách sửa (chỉ giao diện, không đụng dữ liệu):** phiếu chưa qua điều phối thì ô hiện chữ **"Chưa tiếp nhận"** thay vì một ngày; từ `dispatched` trở đi mới hiện ngày thật. **Cố ý KHÔNG bỏ trống cột dưới database:** `request_date` vẫn là mốc TẠM để tính *ngày QĐ có hàng* và cờ *Đơn gấp* ngay lúc lập phiếu (`regulatedDate` · `urgentLinesPR` · `apply_auto_urgent`), xóa đi là mất tính năng cảnh báo trễ hạn ngay từ đầu. Công tắc điều phối TẮT không phải ngoại lệ — lúc đó bước duyệt gọi thẳng `dispatch_pr`, phiếu vẫn sang `dispatched` và vẫn được điền ngày. Danh sách YCMH gọi cột này là *Ngày lập* nên không sửa. **Đã đồng bộ sang `erp-v2`:** `frontend-v2` mắc ĐÚNG lỗi này (`purchase-request-info-card.tsx` in thẳng `request_date` dưới nhãn *Ngày tiếp nhận*), nên bản v2 không chỉ merge mà phải sửa thật — luật gom vào một hàm dùng chung `isDispatched()` ở `modules/procurement/types/purchase-request-detail.ts`, có test kèm. | main → erp-v2 | Xong | — |
| **bao-CR-314-in-ycmh-tu-dmh** | 2026-09-08 | Khách (phiếu hỗ trợ 31) | **XONG — đã deploy prod 08/09/2026 (commit `4ea4788f`), 7/7 test xanh.** In phiếu yêu cầu mua hàng NGAY TRÊN đơn mua hàng, đủ cả hai mẫu sẵn có (*Mẫu thường* nội bộ · *Mẫu thuế*), nhưng **chỉ in những dòng hàng có trên đơn** — số lượng và mọi thông số khác vẫn lấy từ phiếu yêu cầu. **Vì sao không mở lối cũ:** một phiếu YCMH chia cho nhiều NSTM phụ trách (`tab_purchase_request_item.assignee`), phạm vi dữ liệu của người cầm đơn thường KHÔNG với tới cả phiếu, nên bấm sang `/api/purchase-requests/{id}` là 403 đúng những người cần in. Cổng đặt ở ĐƠN: API mới `GET /api/purchase-orders/{id}/purchase-request`, gác `purchase_order.print` + phạm vi của chính đơn. **Ghép dòng bằng `product_code`** vì giữa dòng ĐMH và dòng YCMH không có khóa ngoại — cùng cầu nối mà `pr_expected_map` đang dùng; mã hàng là DUY NHẤT trên YCMH nên tra ngược trúng một dòng, ĐMH được phép trùng mã (bao-CR-308) thì các dòng trùng cùng chiếu về một dòng nguồn, in một lần. Tổng tiền tự khớp vì `_out` của YCMH **tính lại từ danh sách dòng**, không đọc cột lưu sẵn. **Hai điều KHÁCH ĐÃ CHỐT, đừng tự đổi:** (1) *Mẫu thuế* vẫn để trống người đề xuất / chức vụ / bộ phận / chữ ký y như cũ; (2) bản in **không** thêm dòng chữ nào báo đây là bản trích — vẫn mang mã phiếu gốc và cụm chữ ký duyệt, chỉ danh sách hàng là một phần. Dòng ĐMH không đối chiếu được (thiếu mã hàng, hoặc mã không có trên phiếu) **không im lặng biến mất**: đếm ở `po_lines_unmatched` và báo trên thanh công cụ `no-print`, không in vào tờ giấy. `frontend/` đang đóng băng (D-026) nhưng đây là bản chạy thật nên làm ở đó. **Đã port sang `frontend-v2`** (08/09/2026): cùng trang `purchase-request-print-page.tsx` nhận thêm cờ `fromPo`, route `/print/purchase-request-from-po/:id` (tham số là id ĐƠN), nút *In Phiếu yêu cầu* trên chi tiết ĐMH chỉ hiện khi đơn có `pr_code`, và cảnh báo `po_lines_unmatched` nằm ở thanh công cụ. Hai hook nguồn dữ liệu gọi song song, hook không dùng nhận `0` để `enabled` tự tắt — không được gọi hook trong nhánh `if`. | main → erp-v2 | Xong | — |
| **bao-CR-313-va-lo-hong-nhat-ky-va-tan-suat** | 2026-09-07 · **bắt đầu làm 2026-09-08** · **xong + deploy 2026-09-09** | Bảo | **XONG — đã deploy prod (`main` f023c747 + 2de0b2d4) và dev (`erp-v2` 682010c3 + abff1298), BM-001 đã đóng.** Phát hiện khi bắt tay: `erp-v2` ĐÃ có `_guard` cho `/api/audit-logs` từ 05/09/2026 (commit d413481f, minhduoc-tran) nên BM-001 chỉ còn mở trên `main`/prod; việc là **chuyển `_guard` sang `main`** (kèm `core/entity_models.py` + `scope_condition` mà `main` chưa có) chứ không viết lại. Vá ba lỗ hổng cùng vùng mã, ghi ở `so-ghi-nhan-loi-bao-mat.md`: (1) **BM-001** `/api/audit-logs` chỉ gác bằng `get_current_user` — không `require`, không `apply_scope`, `entity` do người gọi truyền thẳng và `entity_id` được phép bỏ trống, nên **bất kỳ tài khoản đăng nhập nào cũng đọc được nhật ký của mọi phân hệ**; đo trên prod 07/09: 18 entity, riêng `entity=auth` 341 dòng chứa IP nhà riêng + email tài khoản + lịch sử gõ sai mật khẩu. (2) **BM-003** `/api/auth/refresh` không ghi dấu vết và không lấy IP → refresh token bị cắp tự gia hạn im lặng suốt 7 ngày. (3) **BM-004** limiter khai `get_remote_address` mà uvicorn chạy không `--proxy-headers` sau nginx → mọi lượt gọi mang IP container, `LOGIN_RATE_LIMIT=10/minute` thành **10 lượt/phút cho cả công ty**: vừa yếu chống dò (14.400 lượt/ngày) vừa chặn nhầm người thật lúc cao điểm. Hai chỗ dễ vỡ: siết quyền đọc nhật ký làm dòng thời gian của lớp CRUD **im lặng trống** thay vì báo 403 (phải rà cả `frontend/` lẫn `frontend-v2/`); sửa limiter mà nginx cho client tự đặt `X-Forwarded-For` thì **tệ hơn hiện tại**. **Tiến độ 08/09/2026: mã xong trên `main`, 16/16 test xanh, chưa commit.** Tệp: mới `core/client_ip.py` (IP thật: `CF-Connecting-IP` → phần tử CUỐI XFF → `client.host`; đã soi nginx nối XFF nên phần tử đầu giả được, cloudflared là container không mở cổng), mới `core/entity_models.py` (9 entity của `SCOPE_FIELDS` main), `core/scoping.py` tách `scope_condition`, `core/limiter.py` đổi `key_func`, `auth/controller.py` refresh ghi `refresh`/`refresh_failed` kèm IP, `audit/controller.py` chép `_guard` từ erp-v2 + ba chốt khác bản erp-v2: entity lạ 403 (không 400), khóa `setting` đọc được mọi entity kể cả `auth`, alias `faq`→`help_article`. Test mới `test_va_nhat_ky_cr313.py` (8) + `test_client_ip_cr313.py` (7). **Deploy 09/09/2026, KHÔNG kèm migration nào** (`alembic current` prod trước/sau đều là `c9d3e7a1f5b6 (head)`, nên lùi lại chỉ là `git reset` + build lại). Chia làm hai commit riêng trên `main` để lùi được từng mẩu: `2de0b2d4` gác nhật ký (`audit/controller.py` + `core/entity_models.py` + `scope_condition`), `f023c747` IP thật + dấu vết gia hạn phiên. Sang `erp-v2`: cherry-pick `abff1298` (mẩu refresh+IP) rồi commit riêng `682010c3` cho ba chốt — **cố ý KHÔNG chép đè `list_logs`/`ACTION_LABEL` của `main`**, vì bản `erp-v2` có thêm bộ lọc, phân trang và các nhãn `returned`/`withdrawn`/`option_add`/`option_remove` mà `main` không có. **Đo trên hệ thật sau deploy** (thay cho "chắc là chạy"): prod — gọi `_guard` với **cả 233 tài khoản đang hoạt động**, 230 ăn 403 ở `entity=auth` lẫn lối duyệt toàn hệ, 3 tài khoản quản trị qua được, và sáu Yêu cầu báo giá gần nhất vẫn trả 1–17 dòng lịch sử cho chính người tạo (vế đối chứng: không giết dòng thời gian của người thường); hai dòng `refresh` đầu tiên mang IP công cộng thật 118.71.139.127 / 27.64.133.181 chứ không phải `172.x` → `CF-Connecting-IP` tới được api. Dev — curl với header giả `X-Forwarded-For: 6.6.6.6` + `X-Real-IP: 7.7.7.7` và mật khẩu sai: dòng `login_failed` ghi `IP 180.93.2.176` (IP công khai thật của VPS), tức client **không** tự khai IP được. Ba chỗ làm khác hướng vá ghi trong sổ, đều đã ghi rõ lý do ở đó: entity lạ trả **403 chứ không 400** (400 biến endpoint thành máy dò danh sách entity), **không cấm** bỏ trống `entity_id` mà lọc theo tập id trong phạm vi (cấm là giết lối lọc theo loại chứng từ của quản trị), và **không bật `--proxy-headers`** (nó cũng tin XFF mà nginx cho client nối thêm) — đã sửa lại hai chỗ tài liệu `nhat-ky-va-phien-dang-nhap.md` còn ghi "PHẢI chạy `--proxy-headers`". | main + erp-v2 | Xong | `doc/tai-lieu-ky-thuat/so-ghi-nhan-loi-bao-mat.md` |
| **bao-CR-312-nhat-ky-thay-doi-va-phien-dang-nhap** | 2026-09-07 · **thiết kế lại bản 2.3 ngày 2026-09-08 · bản 2.4 ngày 2026-09-09** | Bảo | **P1 XONG MÃ 09/09/2026 trên `erp-v2` — chưa commit, chưa deploy.** Middleware `core/request_middleware.py` + bảng `tab_request_log` + **11** cột ngữ cảnh trên `tab_audit_log`, migration `f4d37c7600d0` (thiết kế ghi 10 cột; thực tế 11 vì `parent_entity`/`parent_id` là hai cột). Luật *ghi hay không* gom một chỗ ở `core/logging_policy.py`, bộ mã số ở `core/logging_codes.py`, ngữ cảnh chảy bằng **ContextVar** (`core/request_context.py`) nên **213 lời gọi `record(...)` trong 54 tệp không sửa một chữ**. QĐ-A và QĐ-B đúng ngay từ migration đầu như đã chốt. Kiểm chứng: **18/18 test mới xanh** (`test/backend/test_nhat_ky_lop_may_cr312.py`) và bấm tay trên stack local — `DELETE /api/suppliers/1` bị chặn nay để lại dòng `403` kèm `route` + `user_id` (trước đó **không nằm ở đâu cả**, đúng lỗ P1 sinh ra để bịt); `POST /api/auth/refresh` thành công **không** đẻ dòng còn thất bại thì có (QĐ-A chạy đúng); mật khẩu và refresh token trong body ghi ra `***`; tệp xuất Excel 24.237 B đi qua middleware nguyên vẹn (không đệm thứ không phải JSON). **Hai chỗ cố ý làm khác bản thiết kế, đã ghi ⚠️ vào §4.1 của tài liệu:** (1) body **multipart không đọc gì cả**, chỉ ghi `content_type` + cỡ — bóc tên tệp thì phải nuốt trọn tệp 20 MB vào RAM ở tầng chạy trước mọi request, mà tên tệp đã có ở `tab_file` và ở dòng audit cùng `request_id`; (2) nhánh *gia hạn thành công nhưng ĐỔI IP thì ghi* của QĐ-A **hoãn sang P3** vì nó cần `tab_login_session.last_seen_ip` chưa tồn tại — nghĩa là từ nay tới P3, refresh token bị cắp dùng từ máy khác vẫn không để lại dòng nào, lỗ đã biết và cố ý. **Bản 2.4 (09/09): đo lại dung lượng bằng SỐ THẬT trên prod thay cho ước lượng — 124 dòng audit/ngày (không phải 112), 265 lượt non-GET/ngày, 5,6 lượt đăng nhập/ngày (ước 40), phiếu YCMH trung bình 1,9 dòng, cả CSDL prod chỉ 18,7 MB. Tổng còn ~90 MB/năm (~120 MB ổn định ở 16 tháng) thay vì 180 MB, vẫn dưới khung đã trình khách nên không phải duyệt lại. Phép đo lòi ra ba chỗ lãng phí, chốt luôn: QĐ-A gia hạn phiên THÀNH CÔNG không ghi dòng nào (126/265 lượt non-GET mỗi ngày là `/auth/refresh` — cắt 46.000 dòng rác/năm, vẫn đóng BM-003 bằng nhánh đổi IP + thất bại; P3 phải quay lại bỏ dòng audit `refresh` mà CR-313 đang ghi) và QĐ-B `request_id` lưu `BINARY(16)` ở cả ba bảng (CHAR(36) utf8mb4 tốn ~26 MB/năm chỉ để lưu khóa nối, hiện ra ngoài vẫn là chuỗi 36 ký tự). QĐ-C: bốn bảng nhật ký **KHÔNG** nằm trong sao lưu hằng đêm — nay mỗi bản dump nén 1,09 MB × 30 bản = 33 MB trên R2, thêm nhật ký vào thành 250-450 MB, mà nhật ký đã có đường lưu trữ riêng, giữ cả hai là lưu hai lần cùng một thứ. Cách làm bắt buộc là **dump HAI lượt nối lại**: lượt 1 `--ignore-table` bốn bảng, lượt 2 `--no-data` đúng bốn bảng đó — chỉ `--ignore-table` một lượt thì mất luôn `CREATE TABLE`, mà `alembic_version` đang ở head nên `upgrade head` không dựng lại, phục hồi xong hệ thống chết ở truy vấn đầu tiên chạm nhật ký. Đánh đổi đã chấp nhận: phục hồi từ sao lưu ra hệ thống trắng nhật ký, muốn có lại phải nạp từ gói trên R2. **Kèm theo, nhịp đóng gói đổi từ mỗi năm sang mỗi tháng** (`log-<năm>-<tháng>`) vì nếu không, năm đang chạy chỉ còn một bản duy nhất trong DB — mất CSDL giữa năm là mất trắng nhật ký cả năm; phân vùng vẫn theo năm, một năm chỉ được `DROP PARTITION` khi đủ 12 gói tháng đã kiểm `.sha256`. Cam kết với khách ở Q2 không đổi. Cả ba QĐ làm ở P1 (A, B — phải đúng ngay từ migration đầu) và P6 (C).** **12 CÂU HỎI ĐÃ CHỐT 08/09/2026 theo đề xuất, riêng Q2 khách đổi: giữ 16 tháng cho cả bốn bảng + đóng gói lưu trữ riêng lên R2 (phân vùng theo năm, gói xong mới `DROP PARTITION`, nhật ký giữ lâu hơn sao lưu) — §9/§11; bản 2.4 đổi nhịp gói từ `log-<năm>` sang `log-<năm>-<tháng>` theo QĐ-C, cam kết với khách không đổi. Chưa gõ mã P1 trở đi.** Bản 2.2 thêm §8 **giao diện gom một chỗ**: tách bảng là chuyện lưu trữ (lọc theo trường · tách quyền · dọn riêng), còn màn `/system/logs` chỉ có MỘT — mỗi dòng = một `request_id`, ngăn chi tiết 4 tab *Tổng quan · Request · Thay đổi · Phiên*, theo dõi trực tiếp 5 giây, bốn lối debug (mã phiếu · chỉ lỗi · người/IP · trường); việc nền Celery/script cũng thành một dòng (`source`), lỗi 500 lưu **traceback** (`error_detail`) để đọc trên màn, `request_id` in vào log uvicorn để nối sang container; API 2 endpoint đúng 3 truy vấn/trang. Phiên hiện ở **ba chỗ, một bảng**: Quản trị `/system/sessions` (đá · đăng xuất mọi thiết bị) · Trang cá nhân *Thiết bị của tôi* (`own`) · **hồ sơ Nhân sự tab *Tài khoản & thiết bị*** (phiên + lịch sử đăng nhập 90 ngày = phiên thành công hợp `login_failed`; một nút *Khóa + đăng xuất mọi thiết bị* cho nghỉ việc, HR **không** đá lẻ). §6 có ba bản ghi mẫu dạng JSON. Thêm Q11 (tab HR) + Q12 (traceback vào DB). Bản 1.0 chỉ thêm hai bảng bên cạnh nhật ký cũ; **bản 2.x thiết kế lại chính dòng nhật ký**, vì đo lại trên prod thì thứ thiếu là bản thân một dòng: **70% số dòng có `message` rỗng** (1.769 dòng `update` trơ trọi), **27% hiện mã tiếng Anh trần** cho người đọc (28 `action` trong DB / 13 nhãn trong `ACTION_LABEL`), không dòng nào có endpoint · input/output · IP · phiên · giá trị trước-sau, và lượt bị 403 không để lại dấu. **Thiết kế: BA lớp nối bằng một `request_id`** + một bảng phiên. (1) `tab_request_log` **mới**, do middleware bọc NGOÀI endpoint ghi tự động cho mọi request không phải GET: method · path · **route mẫu** · body vào đã che · `http_status` · body ra khi lỗi · `duration_ms` — trả lời "gọi endpoint nào, gửi gì, nhận gì, có bị chặn không". (2) `tab_audit_log` thêm 10 cột (`actor_kind`, `on_behalf_of`, `session_id`, `request_id`, `ip`, `doc_code`, `parent_entity`/`parent_id`, `action_group`, `changed_fields`, `change_count`) — ngữ cảnh do **ContextVar** điền nên **213 lời gọi `record(...)` không phải sửa một chữ**. (3) `tab_change_log` **một dòng mỗi TRƯỜNG** cho thao tác sửa (lọc được `WHERE field='unit_price'`, ~65 MB/năm thay vì ~550 MB/năm nếu chụp nguyên bản ghi), `snapshot_json` cho thêm/xóa. (4) `tab_login_session` + `jti` + thiết bị/OS/trình duyệt bóc từ user-agent + **`tab_user.token_version`** — tăng 1 là mọi token cũ chết ngay, cho bốn thao tác **đá một thiết bị · đăng xuất mọi thiết bị · bắt đăng nhập lại · chặn tài khoản** (BM-002). `ACTION_CATALOG` **bộ mã đóng, có nhãn tiếng Việt là điều kiện tồn tại** (bỏ cơ chế "nhớ cập nhật `dict` nhãn" đang lệch prod 13 / erp-v2 ~40). `action` **cố ý giữ VARCHAR** như Thu mua (QĐ-9) vì đã có 4.201 dòng thật; mọi cột mới SMALLINT + IntEnum đúng R2. **Đọc tách bốn cửa** để không lặp BM-001: dòng thời gian một phiếu theo quyền của chính chứng từ · tra cứu toàn hệ cần khóa mới `audit` · trước/sau + body vào cần khóa `change_log` (có thể chứa tên NCC) · phiên cần `login_session` phạm vi `own`. ENTITIES **55 → 58** (bản 2.3 ghi 53 → 56, số cũ trước khi có `employee_sensitive` của duoc-CR-314 và `job_position` của duoc-CR-320 — đếm lại trước khi gõ mã), khai đủ `SCOPE_FIELDS` kẻo `test_pham_vi_khai_du_b07.py` đỏ. Che dữ liệu nhạy cảm khai MỘT chỗ dùng cho cả ba lớp; bốn bẫy đã ghi (record tự commit · không tự ghi chính mình · nhập liệu hàng loạt phải gộp · body vào đọc một lần phải trả lại cho endpoint). Dung lượng ~90 MB/năm (số đo bản 2.4; bản 2.0 ước 180 MB), hạn giữ **16 tháng cho cả bốn bảng** theo Q2 khách chốt — ba hạn 6/12/24 tháng của bản 2.0 đã bỏ. Chia **P0..P6**; P0 tách thành **bao-CR-313** làm trước; **P1 (middleware + request_log) là phần đáng làm nhất so với công bỏ ra**. **P1 deploy dev 09/09/2026** (commit `2eba1274`, migration `f4d37c7600d0` chạy trên `procurement_dev`, backup trước migration ở `~/proc_backups/procurement_dev_truoc_cr312_20260909.sql.gz`): bảng `tab_request_log` có thật, `alembic current` một đầu `f4d37c7600d0`, log `api` sạch. Bảng **rỗng ngay sau deploy là ĐÚNG**, không phải middleware chết — luật ở `logging_policy.py` chỉ ghi POST/PUT/PATCH/DELETE cộng ba loại GET (`/export`, `/print`, đuôi `/view`), còn `/api/health` nằm trong `SKIP_PATH_PREFIXES`. **Rà soát mã P1 sau khi deploy (10/09/2026) ra 7 điểm, HAI điểm nặng đã vá ngay tại chỗ — cả hai đều là LỘ BÍ MẬT vào một bảng chỉ-thêm:** (1) `mask_payload` khớp tên khóa CHÍNH XÁC nên `PUT /api/settings` ghi thẳng `smtp_password`, `r2_secret_access_key`, `r2_access_key_id` ra `request_body` — nay mọi khóa CHỨA một mảnh trong `SENSITIVE_KEY_MARKERS` đều bị che, luật gom vào một hàm `is_sensitive_key()` dùng chung cho cả tên khóa lẫn tên cột (P4), cố ý không có mảnh `key` trần kẻo nuốt `keyword`/`product_key`; (2) thân lỗi **422** giữ nguyên văn theo Q9, mà Pydantic v2 gắn vào mỗi mục `exc.errors()` một khóa `input` = **đúng thứ người dùng vừa gõ** — gõ mật khẩu sai kiểu là mật khẩu nguyên văn nằm trong `response_body` trong khi `request_body` của cùng dòng đã che `***`; che-theo-tên-khóa không đỡ được vì khóa tên `input`. Thêm lớp thứ hai `redact_raw_inputs()` bỏ HẲN giá trị đó, thay bằng `{_omitted, type, size}` — `loc` + `type` + `msg` vẫn trả lời đủ *"hỏng ở ô nào, vì sao"*, và đó cũng là mảnh duy nhất không có trần kích thước (`json_invalid` nhét cả thân request vào đó). Người gọi vẫn nhận thân thật, chỉ bản LƯU LẠI bị lược. Kiểm chứng: **20/20 test** (thêm 2 ca canh) và bấm tay trên stack local đúng hai đường lộ đó. Năm điểm còn lại chưa vá, đã ghi ra để xử sau: `_write` chạy I/O CSDL **chặn** trên vòng lặp asyncio (nên bọc `run_in_threadpool`), `_route_pattern` tra ngược theo GIÁ TRỊ nên đụng nhau khi hai tham số cùng giá trị và ghi đè cả đoạn tĩnh, `"/export"` khớp cả `/api/exports`, **mã sự cố** hiện cho người dùng sinh ở `main.py` (ngoài middleware) nên không bao giờ vào được `tab_request_log` — mọi dòng 500 đều mang `error_code='internal_error'`, và trần thân trả về 256 KB rộng gấp 4 lần trần thân request 64 KB mà `_summarize_response` không cắt. Gốc rễ: ticket YCBG05092603 — xem bao-CR-311. | erp-v2 + main | Đang làm — **P0 · P1 · P1b đã deploy CẢ dev lẫn PROD** (prod tới `main` e21023d1 ngày 10/09) · **P2 + P3a đã deploy DEV 10/09 (`erp-v2` 7d98c3c8, head `a7c5e91d3b04`) — prod dừng ở hết P1b** · P3b/P4/P5/P6 chưa gõ | `doc/tai-lieu-ky-thuat/nhat-ky-va-phien-dang-nhap.md` |
| **bao-CR-311-nhat-ky-gan-phuong-an-ycbg** | 2026-09-07 | Bảo | Ghi dấu vết khi **gắn / gỡ / chốt / bỏ chốt** phương án trên Yêu cầu báo giá — bốn hành động mới `add_option` · `del_option` · `choose_option` · `unchoose_option`, kèm nhãn tiếng Việt trong `ACTION_LABEL`. **Vì sao:** ticket YCBG05092603 (phiếu khảo sát Chai hiện kết quả Thùng) truy ra được thủ phạm, nhưng phải đọc cột `created_by` của chính dòng `tab_survey_request_option` trong database — ba thao tác này chạy từ ngày đầu mà **không ghi một dòng nhật ký nào**, người dùng mở lịch sử phiếu ra chỉ thấy trống. **Ba quyết định:** (1) **KHÔNG ghi tên NCC vào dấu vết** — nhật ký phiếu đọc bằng `survey_request.read` mà người YÊU CẦU có khóa đó, cả cơ chế phương án sinh ra là để giấu NCC với chính họ; chỉ ghi tên sản phẩm (vốn đã hiện trên thẻ phương án) + nhãn `Option N — ID x`. (2) Ghi kèm **phân loại của phiếu khảo sát nguồn** để soi lệch phân loại ngay trên dòng nhật ký — đúng thứ ticket cần mà không có. (3) Đường **đồng bộ tự động** truyền `audit=False` vì `sync_options_from_surveys` đã ghi một dòng `sync_options` tổng kết cả lượt; ghi thêm từng phương án là một thao tác hiện hai lần. Kèm theo: `delete_option` nhận thêm `user_id` (mặc định 0 = hệ thống) và luồng hủy duyệt phiếu khảo sát (`survey/service._purge_yc_options`) truyền người thao tác xuống, trước đây gỡ phương án hàng loạt mà không ai đứng tên. **Chỉ vá được từ nay về sau — thao tác cũ không dựng lại được.** Muốn lấp chỗ trống đó ở mức nền thì xem bao-CR-312. Test: `test_nhat_ky_phuong_an_cr311.py` (7 ca, có ca canh KHÔNG lộ tên NCC và ca canh không ghi đôi). | erp-v2 | Hoàn tất | — |
| **bao-CR-310-xu-ly-phuong-an-tren-ycmh** | 2026-09-07 | Khách | Cho XỬ LÝ KHẢO SÁT ngay trên Yêu cầu mua hàng: NSTM gắn phương án (NCC + giá) lên từng dòng YCMH, chốt rồi lên thẳng ĐMH — thay cho hướng gộp YCMH vào YCBG của cụm bao-CR-277..291 (đã khai tử, xem ghi chú ở tệp chung). YCBG giữ nguyên không đụng. **Chia 4 đợt, bảng đợt ở mục H.8 của tài liệu chức năng.** **Đợt 1 XONG** (nền + API): bảng `tab_purchase_request_item_option` (migration `6835fb9cfecd`) — bảng RIÊNG, cố ý không nhét chung `tab_survey_request_option` để khỏi đẻ cột nghĩa kép; snapshot giá tại thời điểm gắn nên phiếu khảo sát sửa giá sau đó không làm đổi phương án đã chốt; **không** thêm cột "đã chốt" lên dòng YCMH mà suy từ `is_chosen`. 6 API dưới `/{pid}/items/{item_id}/options` (liệt kê · gắn từ kho khảo sát · nhập tay · sửa · xóa · chốt), cổng mở khi phiếu ở `dispatched`/`processing`/`purchasing`/`purchased`. **Ba quyết định phân quyền:** (1) **NSTM gắn, NGƯỜI YÊU CẦU chốt** — NSTM chỉ biết giá bao nhiêu, người yêu cầu mới biết giá đó còn đáng mua không; (2) ai có `purchase_request.approve` (quản lý / admin thu mua) chốt thẳng được cho hàng gấp — khóa này chia đúng ranh giới sẵn có, `pur_staff` không có nên không cần khóa quyền mới; (3) nhập tay chỉ đòi `supplier.read` chứ KHÔNG đòi `supplier.write` — `pur_staff` không có `write`, đòi `write` là khóa chết đúng người dùng nó, mà `write` còn là quyền sửa cả danh mục NCC nên rộng quá mức. Vì thế `choose` gác bằng `read` (người yêu cầu thường chỉ có `read` sau khi phiếu đã duyệt) rồi để `ensure_can_choose` phân xử. Ẩn NCC bám luật cụm `pur` của Task 4. **Kèm theo: BỎ bắt buộc Mã hàng trên dòng YCMH** (`required-fields.ts`) — giống phiếu khảo sát vốn có cả loại có mã lẫn không mã, người yêu cầu phải mua được thứ chưa nằm trong danh mục mà không phải chờ mở mã. Đánh đổi phải biết: `sync_from_purchase_orders` nối ĐMH về YCMH bằng **chuỗi `product_code`** nên dòng KHÔNG MÃ không được cộng tiến độ `qty_ordered`/`qty_received` tự động, NSTM phải tự đặt `line_status` — muốn hết thì phải nối bằng khóa dòng, xem N-004. Luật cũ vẫn còn ở bản v1 (`PurchaseRequestDetail.tsx`) vì `frontend/` đóng băng, chờ khách quyết. Kịch bản nghiệm thu + dữ liệu demo: `backend/scripts/demo_cr310.py` (CHỈ local). Test: `test_ycmh_phuong_an_cr310.py` (21 ca) + `required-fields.test.ts`. **Còn lại: đợt 2** gom phương án đã chốt theo NCC → sinh N đơn mua hàng, **đợt 3** màn *Xử lý phương án* ở frontend-v2 (hiện chưa có một dòng giao diện nào — nên đợt 3 sẽ làm TRƯỚC đợt 2), **đợt 4** hai bản in (bản A cho người yêu cầu: dùng lại phiếu đề xuất mục F, in CHUNG MỘT BẢNG dù nhiều NCC, không cột NCC theo dòng; bản B cho thu mua: tick NCC → in nháp đơn, mỗi NCC một trang) + HDSD. | erp-v2 | Đang làm — xong đợt 1 | `doc/tai-lieu-chuc-nang/03-yeu-cau-mua-hang.md` mục H |

---

## Nhật ký deploy môi trường thật

Chỉ ghi deploy của các CR trong tệp này. Deploy cũ nằm ở cuối `change-log.md`.

| Ngày | Môi trường | Nội dung | Ghi chú |
|---|---|---|---|
| 2026-09-11 | **prod + dev** | **bao-CR-372** — YCMH hiện phân loại ngoài danh mục thay vì bỏ trắng ô. `main` c417bf64 → **fa8d4262**; `erp-v2` 170744f5 → **83eab2ce** (gộp `main`). Chỉ frontend nên **chỉ build lại `web`**, không đụng `api`/`celery`. **Không có migration**, không đụng CSDL nên không cần sao lưu trước. | ⚠️ `up -d --build web` sẽ kéo theo `api` qua `depends_on`; muốn chạm đúng `web` thì `build web` rồi `up -d --no-deps web`. Kiểm sau deploy: chuỗi `(ngoài danh mục)` có trong gói JS của cả `procurement-web` lẫn `procurement-web-dev`. Bản **v2 chưa vá** — popup chi tiết dòng ở `frontend-v2` còn là ô nhập chữ tự do |
| 2026-09-11 (chiều) | **dev** (deverp + devthumua) | **Gộp `main` → `erp-v2`** — 15 commit prod về nhánh dev: bao-CR-346/363/364/365/366/367/368. `erp-v2` bf6daece → **4f112e9b** (hai commit gộp: `f26953c1` gộp `main`, `4f112e9b` gộp tiếp duoc-CR-370 của đồng nghiệp). Build lại `api` + `celery-worker` + `erp` + `web`. **Không có migration** — tệp `f4d37c7600d0` đụng độ ở `down_revision` và đã giữ bản `erp-v2` (`d7f2a9c4e1b8`) đúng như N-010 dặn trước; `alembic current` = `bee157de2ec8 (head)`. Kiểm sau deploy: 8/8 container `running`; `grep -c "bao-CR-368"` trong `service.py` của container `api` = 4; devthumua / deverp / `/api/health` đều 200; log `api` sạch. | 17 chỗ đụng độ: 10 tệp giữ bản `erp-v2` (đều là thứ `main` cherry-pick từ chính nó rồi bị bỏ lại phía sau), `employee/service.py` ghép tay để giữ **cả** duoc-CR-314/320 **lẫn** bao-CR-368 (chốt `commit` đặt DƯỚI hai việc chỉ `flush`), `employee/controller.py` thêm chặn trùng email cho đường CSV và đổi tên biến sang tiếng Anh, bốn tệp tài liệu giữ đủ hai phía. Test: 138 bài backend xanh, `tsc` của `frontend/` đúng 4 lỗi nền cũ |
| 2026-09-11 (13:45) | **prod** | **bao-CR-368** — chặn hai hồ sơ nhân sự dùng chung một email + gom lần lưu về một giao dịch. `main` a2660b84 → **870a721f**. Chỉ backend: build lại `api` + `celery-worker` + `celery-beat` (`web` **không** đụng tới nên không ai bị ngắt phiên). **Không có migration.** Trước đó đã sửa dữ liệu: xoá NSU238 (ID 286) — bản sao `proc_backups/procurement_truoc_cr368_20260911_1340.sql.gz`. Kiểm sau deploy: 7/7 container `Up`; `grep -c "bao-CR-368"` trong `service.py` của container `api` = 3; log `api` có phiên đăng nhập Google thật và `GET /api/employees` trả 200. | Rà lại prod sau khi xoá: **0 cặp nhân sự trùng email** trên 235 hồ sơ. Luật mới chỉ gác đường API — `seed.py`/`seed_prod.py` dựng `Employee` thẳng từ model nên không đi qua đây; nếu sau này seed sinh trùng email thì vẫn lọt |
| 2026-09-11 | **prod** | **bao-CR-367** — Ngày hóa đơn trên ĐMH không còn tự nhảy theo ngày lưu (ticket TK10092603). `main` ff44557e → **338def8b**. Build lại `web` + `api` + `celery-worker` + `celery-beat`. **Không có migration**, không đụng CSDL nên không cần sao lưu trước. Kiểm sau deploy: bình luận `bao-CR-367` có trong `service.py` của container `api`; bundle của `web` không còn đoạn tự điền `invoice_date: new Date()`. | Dữ liệu cũ đã hỏng **không khôi phục tự động** — khách chốt để thu mua nhập lại tay (N-017). Bản v2 vốn không dính lỗi payload, còn nợ gỡ đoạn tự điền ngày hôm nay ở `purchase-order-deliveries-table.tsx` cho đồng bộ |
| 2026-09-10 (17:55) | **dev** (deverp + devthumua) | **bao-CR-363** — mỗi mốc trạng thái một màu, cả bộ huy hiệu tô đặc. `erp-v2` 1c4544f8 → **330c9d83**: bản v2 (`2e73d73d` — 5 tông mới + `STATUS_TONE` + `PROGRESS_CLASS` + 6 test) cộng bản v1 cherry-pick từ `main` (`306f9bda` · `b8a5f156` · `bac8fd8e`). Build lại **`erp`** và **`web`**. | Không có migration, không đụng backend — **không sao lưu DB**. Dùng `build` rồi `up -d --no-deps erp web` nên `api` không bị dựng lại. ⚠️ **Không merge `main` vào `erp-v2` được** trong đợt này: merge ra **8 xung đột add/add** (`request_middleware.py`, migration `f4d37c7600d0`, `audit/controller.py`, `role/service.py`, `user/service.py`, 3 tệp doc) vì CR-312 P1 + CR-346 vốn là mã cherry-pick **ngược** từ `erp-v2` sang `main`, hai bên thêm cùng nội dung bằng hai commit khác nhau. Cherry-pick riêng 3 commit của CR-363 thì chỉ vướng `change-log-bao.md`, giữ bản `erp-v2`. **Đo sau deploy:** `deverp=200` · `devthumua=200` · `api=200` · trong ảnh `web`: `badge.done{background:#14532d;color:#fff}` · trong ảnh `erp`: `status-tone-*.js` có `bg-green-900` · `bg-violet-600` · `bg-amber-700`. Cổng `npm run check` xanh trước khi đẩy (typecheck 0 lỗi, lint 0 lỗi, 246 tệp test / 2805 test). |
| 2026-09-10 (17:35) | **prod** | **bao-CR-363 đợt 2** — cả bộ pill tô đặc, chữ trắng (khách báo bản pastel *"nhợt nhạt"*). `main` 08ede4c6 → **3e107b42**. Mười lớp pill đổi nền, mỗi màu chọn ở bậc đậm cho chữ trắng đạt tương phản ≥ 4,5:1; *Hoàn thành* xuống `#14532d` để tách khỏi `ok #15803d` đứng ngay trước nó. | Không có migration, không đụng backend — **không sao lưu DB**. Lần này tách hai bước đúng như ghi chú của đợt 1: `build web` rồi `up -d --no-deps web` → **`api` không bị dựng lại** (`Up 33 minutes` sau khi deploy xong), không ai bị ngắt. **Đo sau deploy:** `web=200` · `api=200` · trang chủ trả về đúng bộ `index-Dz3-LjZA.js` + `index-Ct8I0WsT.css` của ảnh vừa dựng · trong ảnh: `badge.done{background:#14532d;color:#fff}` và `badge.violet{background:#7c3aed;color:#fff}`. |
| 2026-09-10 (16:50) | **prod** | **bao-CR-363** — mỗi mốc trạng thái một màu. `main` e21023d1 → **c62ea8c9** (5 lớp pill mới + xếp lại `PR_STATUS`/`PO_STATUS`/`SR_STATUS`) → **39e07ca5** (pill *Hoàn thành* đổi từ `#16a34a` sang `#15803d` cho đủ tương phản với chữ trắng 10.5px). Build lại **`web`** bằng `-f docker-compose.production.yml`. | Không có migration, không đụng backend — **không sao lưu DB** vì không có gì ghi xuống. ⚠️ `docker compose ... up -d --build web` **kéo theo cả `api`** (do `depends_on`), nên `api` bị dựng lại và chạy lại `start.prod.sh`: log xác nhận `alembic upgrade head` không có migration mới và `seed_prod` không ghi đè gì. Muốn chỉ đụng `web` thì `build web` rồi `up -d --no-deps web`. **Đo sau deploy:** `web=200` · `api=200` · trang chủ trả về đúng bộ `index-Bu0cDF3T.js` + `index-BHAvgkCV.css` của ảnh vừa dựng · trong ảnh: `badge.done{background:#15803d;color:#fff}` và `Đã điều phối",cls:"violet"`. |
| 2026-09-10 (16:30) | **dev** (deverp + devthumua) | **Gộp 8 CR một đợt** — bao-CR-322/345/356/357 (frontend-v2: lối vào bản in ĐMH, tên định danh tiếng Anh, hàng tiêu đề xanh, giá vốn nhập khẩu) · **bao-CR-359** (sàn bề rộng cột chứa ô nhập) · **bao-CR-358 + bao-CR-360** (CR-312 P2 + P3a: bộ mã hành động đóng, phiên đăng nhập máy chủ). `erp-v2` 337fa9bb → **7d98c3c8**, kèm **7 commit frontend-v2 của đồng nghiệp** (giao diện khổ hẹp + tổng quan Dự án) merge sạch. Migration `94f0a2c4e43c` → `e4b7c2a1d905` (gộp head) → **`a7c5e91d3b04`**. Build lại `api` + `celery-worker` + `celery-beat` + `erp` bằng **`-f docker-compose.dev.yml`**. Sao lưu trước: `~/proc_backups/procurement_dev_truoc_cr358_360_20260910.sql.gz` (1,3 MB). | Xác minh sau deploy: `alembic current` = `a7c5e91d3b04 (head)` một đầu · `tab_login_session` có thật, `tab_user.token_version` kiểu `smallint` · deverp `/` 200, `/api/health` 200 ở cả hai domain, `/api/auth/me` không vé trả đúng phong bì 401 · `celery@… ready` + `beat: Starting` · `tab_request_log` vẫn ghi sau khi deploy. ⚠️ **Đợt này ĐĂNG XUẤT TOÀN BỘ người dùng dev một lần** (vé cũ không có `jti`) — đại ca chốt *"cho mọi người vào lại thôi"* nên deploy thẳng trong giờ. ⚠️ **Đính chính: dev CÓ `celery-beat`** (`procurement-tool-dev-celery-beat-1`), câu "dev không có beat" ở các dòng trước là sai — chỉ `devthumua` dùng chung DB mới không thêm beat. |
| 2026-09-10 (chiều) | **prod** (thumua) | **bao-CR-312 P1 + bao-CR-346 (P1b)** — trả nợ kỹ thuật **N-010**, đưa lớp nhật ký lên hệ thật. `main` 0ec5646c → **2daf6ffb** (P1) → **e21023d1** (P1b), cherry-pick từ `erp-v2` `2eba1274` + `337fa9bb`. Migration `d2f45a8c9e10` → `f4d37c7600d0` → **`94f0a2c4e43c`**, chạy sạch lúc `api` khởi động. Build lại `api` + **`celery-worker` + `celery-beat`** (prod CÓ beat, khác dev). Sao lưu trước: `~/proc_backups/procurement_truoc_cr312_p1_20260910.sql.gz` (1,2 MB, 59 bảng, `gzip -t` sạch). | Xác minh: 5/5 domain HTTP 200 · `/api/health` 200 · `alembic current` = `94f0a2c4e43c (head)` một đầu · hash blob `request_middleware.py` trong container **khớp đúng** `main:` (`c896e838`) · `celery-worker` ready và **đăng ký đủ hai việc mới** `audit.archive` + `request_log.cleanup` · `tab_request_log` 21 cột, `tab_audit_log` 20 cột, đã có dòng đầu tiên. ⚠️ **`f4d37c7600d0` giữ NGUYÊN mã revision, chỉ đổi `down_revision`** sang head của `main` — merge `main` → `erp-v2` tới sẽ đụng độ đúng dòng đó, **giữ bản `erp-v2` (`d7f2a9c4e1b8`)**. ⚠️ Bốn thứ **cố ý không mang sang** vì ngoài phạm vi: task RAG trợ lý, hai nhãn `option_add`/`option_remove` của bao-CR-310, chốt *vai trò còn người giữ thì không xóa* + `perm_cache_clear()` sau khi xóa vai trò, hai hàm ảnh đại diện. ⚠️ **Prod dừng ở hết P1b** — chưa có P2 (`bao-CR-358`) lẫn P3a (`bao-CR-360`), nên BM-002 vẫn mở trên hệ thật. |
| 2026-09-10 | **dev** (deverp + devthumua) | **bao-CR-346** — rà soát bảo mật lớp nhật ký. `erp-v2` c049fdae → **337fa9bb** (kèm 11 commit frontend-v2 của đồng nghiệp, duoc-CR-343/344 + tab nhớ ở URL). Migration `f4d37c7600d0` → **`94f0a2c4e43c`** chạy sạch lúc api khởi động. Build lại `api` + `celery-worker` + `erp`. | Đo trên hệ thật sau deploy: một lượt `GET /api/employees` ăn 401 **đã sinh dòng nhật ký** — luật cũ bỏ hẳn cả GET lẫn lượt bị chặn, nên đây đúng là chỗ trước kia mù. `device_hash` = `1765D8372A7846CC`, `device_label()` tra ngược ra `chrome|windows|desktop`; `referer` lưu đủ. ⚠️ **Dev KHÔNG có `celery-beat`** (theo thiết kế), nên hai việc định giờ `request_log.cleanup` (03:40) và `audit.archive` **không tự chạy ở dev** — muốn thử thì gọi tay qua `.delay()`. ✅ **Đã đưa lên prod chiều cùng ngày** (`main` e21023d1) — cả P1 + P1b, cherry-pick chứ không merge; xem dòng đầu bảng deploy. Prod **CÓ** `celery-beat` nên hai việc định giờ chạy thật ngay đêm đầu, khác dev. |
| 2026-09-07 | **prod** | **bao-CR-311** — dấu vết gắn / gỡ / chốt phương án YCBG. `main` 68595dca → **07ce3498** (cherry-pick từ `erp-v2` 2dbb0f1f), build lại `api` + `celery-worker` + `celery-beat`. | Không có migration mới. Xung đột ở `audit/controller.py`: `ACTION_LABEL` trên `main` chỉ có **9 nhãn**, bản `erp-v2` có ~40 (đợt vá 971 dòng ngày 05/09 **chưa hề lên prod**) — giải bằng cách giữ bản `main` và chỉ thêm 4 nhãn mới, không kéo cả đợt vá sang. Nghĩa là prod vẫn hiện mã Anh trần cho `login` · `assign` · `sync_options` …, **còn nợ một CR riêng**. Đã xác nhận chữ ký hàm + 4 nhãn trong container thật. |
