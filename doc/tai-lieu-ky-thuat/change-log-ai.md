# NHẬT KÝ THAY ĐỔI — DẢI `ai`

Sổ CR riêng cho mảng **tự động hóa bằng AI** (Agent Hub), tách ngày **18/09/2026** theo lệnh
đại ca.

**Vì sao tách.** Ba lý do, không phải chỉ để tránh xung đột merge như lần tách `change-log-bao.md`:

- Mảng này **không phải nghiệp vụ thu mua**. Nó là bộ máy *làm ra* phần mềm, không phải một
  phân hệ của phần mềm. Trộn vào sổ nghiệp vụ thì người tra sổ để tìm "YCMH đổi gì" phải lội
  qua một đống dòng chẳng liên quan.
- Nó **có thể bị dừng hẳn** ở bậc 1 nếu con bot quản lý không đủ khôn. Một mảng có khả năng bị
  bỏ thì để riêng, dừng là gấp sổ lại, không phải đi xóa dòng rải rác trong sổ chung.
- Sau này một phần lớn dòng trong sổ này **do máy tự ghi**, không phải người. Dòng máy ghi và
  dòng người ghi để chung một chỗ thì không còn phân biệt được ai chịu trách nhiệm.

**Tệp này chứa gì.** Mọi CR mang tiền tố `ai-`, mọi quyết định `QĐ-AI-x`, và nhật ký triển khai
của Agent Hub.

**Tệp này KHÔNG chứa gì.** Thay đổi nghiệp vụ của ERP, kể cả khi do bot làm ra — cái đó vẫn ghi
vào `change-log.md` / `change-log-bao.md` như thường, và ghi thêm một dòng đối chiếu ở đây.

---

## Luật ghi

**Trạng thái:** `Đề xuất` → `Đã duyệt` → `Đang làm` → `Hoàn tất` (hoặc `Từ chối` / `Hoãn` / `Bỏ`).

**ID đầy đủ = `ai-CR-<số>-<slug>`**, slug kebab 3-5 chữ tóm tắt việc.

**Dải số `ai-CR-*` là DẢI RIÊNG**, bắt đầu từ `001`, **không chung** với `CR-*` và `bao-CR-*`.
Cấp số mới chỉ cần grep đúng tệp này:

```bash
grep -oh "ai-CR-[0-9]\+" doc/tai-lieu-ky-thuat/change-log-ai.md | sort -t- -k3 -n | tail -3
```

**ĐẶT CHỖ NGAY KHI NHẬN VIỆC** — cấp số xong ghi ngay một dòng `Đang làm` rồi mới bắt tay làm.

**Dòng do bot tự ghi** phải để `Người đề xuất` = `Bot (ai-CR-xxx)` và ghi rõ đại ca duyệt lúc nào.

---

## Quyết định đã chốt

| Mã | Ngày | Quyết định | Lý do |
|---|---|---|---|
| **QĐ-AI-1** | 2026-09-18 | Nguồn ticket = **Phiếu hỗ trợ ERP v2** (`tab_ticket`) **+ tin nhắn Telegram của đại ca**. Hai nguồn nhưng **một bảng, một luồng**. | Không đẻ hai nhánh mã cho cùng một việc. `tab_ticket` đã có sẵn, không dựng bảng ticket mới. |
| **QĐ-AI-2** | 2026-09-18 | Agent Hub là **service mới trong repo `procurement-tool`**, không dựng repo hay VPS riêng. | Tận dụng Celery + Redis + Qdrant + DB + lớp provider AI (`assistant/provider/gemini.py`) đã chạy sẵn. Ổ cứng VPS vừa dọn, không dựng hệ thứ hai. |
| **QĐ-AI-3** | 2026-09-18 | Bot quản lý dùng **Gemini API**; bot code dùng **Claude Code CLI chạy trên máy đại ca** bằng subscription. **Không dùng API key Anthropic.** | Bot code là chỗ đốt tiền nhất thì ăn vào subscription; bot quản lý chỉ tóm tắt nên Gemini Flash gần như miễn phí. Biến phí cả hệ thống xấp xỉ bằng không. |
| **QĐ-AI-4** | 2026-09-18 | Hệ quả của QĐ-AI-3: **hub chạy trên máy đại ca**, không đặt ở VPS. Telegram dùng **long-polling**, ticket từ ERP prod thì **quét theo mốc sửa**. | Khỏi cần địa chỉ công khai, khỏi tunnel, khỏi webhook, khỏi ký HMAC trên nút bấm. Đổi lại máy tắt thì hệ thống đứng — chấp nhận được vì trạm nào cũng chờ đại ca duyệt. |
| **QĐ-AI-5** | 2026-09-18 | **Hai chỗ dừng bắt buộc không được cấu hình bỏ qua**: giữa PLAN và CODE, giữa REVIEW và PROD. | Đây là toàn bộ lý do hệ thống này an toàn. Không bao giờ thêm cờ "tự duyệt" cho hai chỗ đó. |
| **QĐ-AI-6** | 2026-09-18 | Bot code **cấm tuyệt đối tự sinh migration**. Cần đổi cấu trúc DB thì dừng và leo thang cho người. | Migration hỏng trên một cơ sở dữ liệu 141 bảng đang chạy thật không cứu được bằng revert commit. |
| **QĐ-AI-10** | 2026-09-18 | Bot Telegram **kiêm luôn cửa vào của Trợ lý AI sẵn có**: tin bắt đầu bằng `/hoi` chuyển thẳng cho `assistant.service.ask()`, trả lời tại chỗ, **không** tạo task. Chạy dưới **một tài khoản ERP khai cứng** ở `AGENT_ASSISTANT_USER`; để trống = tắt hẳn lệnh đó. | Đại ca hỏi "sao không dùng luôn trợ lý qua Telegram". Đúng là rẻ — cầu nối Telegram đã dựng, `ask()` đã có đủ tool-calling — và hỏi đáp với giao việc cùng đi qua một cái điện thoại, tách hai bot thì phải nhớ đang chat với con nào. ⚠️ Trợ lý lọc dữ liệu theo **người đăng nhập** mà Telegram thì không đăng nhập, nên bắt buộc chỉ đích danh một tài khoản; chạy không người dùng là chạy không phạm vi. Hàng rào còn lại vẫn là `AGENT_TELEGRAM_CHAT_ID`, nên **đừng khai tài khoản quản trị**. |
| **QĐ-AI-9** | 2026-09-18 | Bot quản lý dùng **kho vector RIÊNG** `agent_docs`, không dùng chung `kb_docs` của Trợ lý AI. | Cân nhắc dùng chung theo đề nghị của đại ca rồi bỏ. `kb_docs` hiện chỉ có `help_article` + `faq` — HDSD cho người dùng cuối, không có change-log, không có mã CR, tức không có đúng thứ bot cần. Còn nạp tài liệu kỹ thuật vào đó thì `rag/search.py` **không lọc theo `source`**, người dùng cuối hỏi nghiệp vụ sẽ nhận về nhật ký deploy — muốn chặn phải sửa đường tra cứu đang chạy thật. Tách kho tốn đúng một tham số `VectorStore(collection=...)`. |
| **QĐ-AI-8** | 2026-09-18 | **Lệch có chủ ý so với bản thiết kế §5**: bảng nối đổi từ `tab_agent_task_ticket(ticket_id)` thành **`tab_agent_task_item(source, ref_id)`**, và thêm bảng thứ năm **`tab_agent_cursor`**. | Bậc 1 không có `tab_ticket` nào (QĐ-AI-7), nên bảng chỉ trỏ được sang `tab_ticket` sẽ **rỗng suốt bậc 1** — mà "bot gom việc được không" lại đúng là một trong hai câu bậc 1 sinh ra để trả lời. Cặp `(source, ref_id)` chạy được cho cả Telegram lẫn `tab_ticket` sau này, nối thêm nguồn là thêm dòng chứ không sửa bảng. `tab_agent_cursor` giữ con trỏ `getUpdates`: Telegram ôm tin chưa xác nhận tới 24h, không lưu con trỏ xuống DB thì mỗi lần restart là bot đọc lại tin cũ và đẻ một loạt task trùng. |
| **QĐ-AI-7** | 2026-09-18 | **Phạm vi bậc 1 co lại còn lõi nhất**: chỉ nhận ticket từ **Telegram**, **không** dựng màn hình quản lý trong ERP v2 (xem sổ bằng Adminer), dùng **key Gemini riêng** `AGENT_GEMINI_API_KEY`. | Bậc 1 có thể bị bỏ hẳn nếu con quản lý không đủ khôn — nên cắt mọi thứ không phục vụ câu hỏi đó. Nối `tab_ticket` và dựng màn hình đều là việc cộng thêm sau, không phải sửa lại. Key riêng để bot đốt hết hạn mức thì Trợ lý AI đang chạy thật không chết theo, và đo được riêng chi phí của bot. |

---

## Việc còn nợ

Bốn câu `AN-001`…`AN-004` đại ca đã trả lời hết ngày 18/09/2026, gói vào `QĐ-AI-7` ở trên.

| Mã | Nội dung | Trạng thái |
|---|---|---|
| **AN-001** | Bot Telegram | **Xong** — đại ca đã có sẵn bot, tự điền `AGENT_TELEGRAM_BOT_TOKEN` + `AGENT_TELEGRAM_CHAT_ID` vào `.env`. Token KHÔNG đi qua chat, KHÔNG commit |
| **AN-002** | Key Gemini | **Xong** — tách riêng `AGENT_GEMINI_API_KEY`, không dùng chung với Trợ lý AI |
| **AN-003** | Nguồn ticket bậc 1 | **Xong** — chỉ Telegram. Nối `tab_ticket` để dành bậc sau |
| **AN-004** | Màn hình quản lý | **Xong** — chưa dựng. Bậc 1 xem sổ bằng Adminer |
| **AN-005** | Nối `tab_ticket` của ERP làm nguồn vào (vòng quét theo mốc sửa, khuôn `pull_updated` của `legacy_datxe`) | Hoãn tới sau bậc 1 |
| **AN-006** | Màn danh sách + chi tiết task trong ERP v2 (xem sổ, lịch sử gọi model, chi phí) | Hoãn tới bậc 2 |
| **AN-007** | Ý tưởng của đại ca 23/09/2026: **xem thử bản sửa qua link Cloudflare tunnel trên máy local**, khỏi phải đẩy lên dev mới test được; kèm luật **Đậu Đậu được vào DB local, còn DB trên VPS phải hỏi đại ca trước**. Phương án em đề xuất: stack xem thử riêng cho từng việc (api + giao diện v2 chạy đúng nhánh `bot/*`, DB sao từ local) + tunnel + nút «Xem thử» trên thẻ kết quả, thử ổn mới gộp; DB local đọc bằng tài khoản chỉ SELECT; DB trên VPS thì thẻ nêu đúng câu SELECT, đại ca đồng ý hub mới chạy qua SSH, khóa DB không vào Claude Code. Bốn câu chờ quyết: tên miền riêng có Cloudflare Access hay link tạm ngẫu nhiên · có sao DB local cho stack xem thử · DB VPS chỉ dev hay cả prod · làm chung một đợt hay tách. | **Ghi nhận, để sau** — đại ca 23/09: *"thôi ghi nhận lại đi, cứ đẩy lên dev như bản đầu đi rồi mình sẽ có việc để làm phần này sau"*. Trong lúc chờ, thử bản sửa vẫn đi đường ai-CR-014 (gộp erp-v2 + deploy dev có hỏi trước) |

---

| CR | Ngày | Người đề xuất | Nội dung | Ảnh hưởng scope | Trạng thái | Tài liệu liên quan |
|---|---|---|---|---|---|---|
| **ai-CR-042-thong-tin-tai-khoan-doc-duoc** | 2026-09-24 | Đại ca (*"anh không xem được thông tin cá nhân hay tài khoản anh mới đăng nhập hả ta"*) — `/taikhoan` in «#238», câu «cho tôi thông tin tài khoản» bị trả lời bằng hướng dẫn đăng nhập | `service.describe_user`: «Họ tên (mã NV)» + «phòng … · tên đăng nhập …» (tài khoản không email trước đây ra «#id»), dùng ở câu đăng nhập thành công, `/taikhoan` và câu dặn Trợ lý. `_account_fact` dặn thêm: hỏi thông tin tài khoản / cá nhân thì trả bằng chính thông tin đó (tra tool nếu cần), KHÔNG giảng lại cách đăng nhập; `BOT_LOGIN_FACTS` mở đầu «chỉ dùng khi hỏi CÁCH đăng nhập / đổi tài khoản» (ai-CR-040 dặn quá tay). | `backend/app/modules/agent_hub/{service,constants}.py` · `test/backend/test_agent_hub.py` (+2 bài, 179/179) | **Xong** (chưa push) | — |
| **ai-CR-041-bai-kiem-khong-goi-mang-that** | 2026-09-24 | Đại ca (*"mấy bài test nó tốn token quá em ơi"*) | **Chặn mọi lượt gọi mạng thật trong `test_agent_hub.py`.** Đo bằng lưới chặn: 0 lượt gọi Gemini (không tốn tiền API), nhưng 125 lượt gọi Telegram THẬT bằng token thật mỗi lần chạy tệp (các hàm `answer_callback` · `clear_buttons` · `edit_text` · `send_document` không được giả lập). Fixture `autouse` `_chan_mang_that` bọc `requests.Session.request`: máy chủ local đi thẳng, `api.telegram.org` trả «ok» tại chỗ, host khác (Gemini, GitHub…) thì ném lỗi VÀ làm bài đó đỏ. Thời gian chạy tệp 80 giây → 21 giây. | `test/backend/test_agent_hub.py` (177/177) | **Xong** (chưa push) | — |
| **ai-CR-040-bot-noi-dung-cach-dang-nhap** | 2026-09-24 | Đại ca đăng nhập tài khoản khác trên web rồi hỏi bot, bot bịa «quét mã QR / token phiên» | **Bot nói đúng cách đăng nhập.** Đại ca đăng nhập tài khoản khác trên web rồi hỏi, Trợ lý bịa «quét mã QR / token phiên». Nay mỗi lượt hỏi Trợ lý kèm `BOT_LOGIN_FACTS` (cơ chế thật của ai-CR-038: tài khoản chat KHÔNG đổi theo web; đổi bằng mã ở Trang cá nhân → Telegram rồi `/dangnhap <mã>`; không QR, không mật khẩu) + `_account_fact` (chat đang dùng tài khoản nào, đã liên kết hay đang dùng tài khoản chung). Lệnh mới `/taikhoan` (chat đại ca + chat đã liên kết) trả lời chắc chắn không qua model; câu trợ giúp có thêm ba lệnh tài khoản. | `backend/app/modules/agent_hub/{service,constants}.py` · `test/backend/test_agent_hub.py` (+2 bài, 177/177) | **Xong** (chưa push) | — |
| **ai-CR-039-an-man-viec-cua-bot** | 2026-09-24 | Đại ca (*"cái màn quản trị của Việc của bot Telegram tạm thời bỏ nó em nhé, ẩn nó đi. anh muốn xem thông tin gì thì anh hỏi bot nó trả lời"*) | **Ẩn màn «Việc của bot Telegram» (ai-CR-036)**: gỡ mục menu Quản trị và lối tắt trang Tổng quan Quản trị; GIỮ route `/system/agent-tasks`, trang, API `/api/agent-hub/*` và khóa `agent_task` để bật lại chỉ cần thêm lại hai mục. Tab «Telegram» ở `/me` (ai-CR-038) giữ nguyên. Đếm lối tắt về 10. | `frontend-v2/src/modules/system/{routes.tsx,config/dashboard-shortcuts(.test).ts}` | **Xong** (chưa push) | — |
| **ai-CR-038-dang-nhap-erp-trong-telegram** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-07 của `doc/agent-hub/04` | **Mỗi người tự đăng nhập tài khoản ERP trong Telegram (ai-CR-004 làm xong ở đây).** Bảng mới `tab_agent_chat_link` (migration `5c8e2a7d9f14`), module `agent_hub/chat_link.py`: trang cá nhân ERP phát mã 6 số dùng một lần (`AGENT_LINK_CODE_MINUTES`=10, chỉ lưu băm, lấy mã mới thì mã cũ hủy) → nhắn `/dangnhap <mã>` (hoặc bấm link `t.me/<bot>?start=<mã>` khi khai `AGENT_TELEGRAM_BOT_USERNAME`) trong chat RIÊNG → chat gắn tài khoản `AGENT_LINK_DAYS`=30 ngày → `/dangxuat` (`/doitaikhoan`) hoặc gỡ ở web. KHÔNG hỏi mật khẩu trong chat; sổ tin che mã; sai 5 lần/giờ thì chat bị lờ; nhóm chat không liên kết được; hết hạn thì báo MỘT lần. Người đã liên kết CHỈ hỏi Trợ lý AI dưới đúng tài khoản của họ (`_assistant_user(db, chat_id)`), tin của họ không bao giờ vào hàng việc sửa mã; chat khác chưa liên kết vẫn bị lờ im lặng; chat đại ca liên kết thì /hoi chạy dưới tài khoản thật của đại ca, chưa liên kết thì lùi về `AGENT_ASSISTANT_USER` như cũ (chat khác KHÔNG bao giờ lùi về tài khoản đó). API tự phục vụ chỉ đòi đăng nhập: `GET /api/agent-hub/links`, `POST /links/code`, `DELETE /links/{id}` (chỉ gỡ được liên kết của mình). Frontend-v2: tab «Telegram» ở `/me` (lấy mã, chép lệnh, link mở bot, danh sách chat đang đăng nhập + gỡ) + test. Hạn chế: nút xác nhận thẻ đề xuất sửa phiếu vẫn chỉ bấm được ở chat đại ca. | `backend/app/modules/agent_hub/{chat_link,service,controller,model}.py` · `backend/app/core/config.py` · migration `5c8e2a7d9f14` · `frontend-v2/src/app/{pages/profile-page.tsx,components/profile/profile-telegram-tab(.test).tsx}` · `frontend-v2/src/modules/system/{api/agent-hub-api.ts,hooks/use-telegram-links.ts}` · `frontend-v2/src/shared/constants/query-keys.ts` · `test/backend/test_agent_hub.py` (+5 bài, 175/175) · vitest profile + system 166/166 · typecheck 0 | **Xong** (chưa push) | — |
| **ai-CR-037-phieu-ho-tro-lam-nguon-viec** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-06 của `doc/agent-hub/04` | **Phiếu hỗ trợ ERP làm nguồn việc (AN-005).** Vòng beat mỗi phút `agent.pull_tickets` → `service.pull_tickets`: hai cửa, cửa nào trống là tắt — (1) `AGENT_TICKET_ASSIGNEE` = email tài khoản ERP của bot, nhóm hỗ trợ GIAO phiếu cho tài khoản này là bot nhận (kể cả phiếu cũ); (2) `AGENT_TICKET_DEPARTMENTS` = nhãn «Bộ phận / Nhóm» cách nhau dấu phẩy, chỉ nhận phiếu MỚI hơn mốc `erp_ticket_since` đặt lúc bật (không kéo lịch sử). Chỉ phiếu `open`/`in_progress`, chưa gắn việc nào. Một phiếu = một việc (không qua trạm gom): mô tả gồm mã phiếu, người gửi, bộ phận, ưu tiên, tiêu đề, nội dung đầu, trang đang đứng; `AgentTaskItem(SRC_ERP_TICKET, ticket.id)`; phiếu nhận dòng trả lời của nhóm hỗ trợ «đã chuyển cho bot, mã việc…» và sang «Đang xử lý»; nhắn đại ca rồi vào bước rà soát; tôn trọng trần việc/ngày. Việc «xong» → phiếu «Đã trả lời» + dòng báo đã lên dev; việc «bỏ» → phiếu về «Mới», gỡ giao cho bot. Chưa đưa tệp đính kèm của phiếu cho bot; chưa gửi chuông cho người gửi (dòng trả lời hiện trong phiếu). | `backend/app/modules/agent_hub/{service,tasks}.py` · `backend/app/core/{config,celery_app}.py` · `test/backend/test_agent_hub.py` (+4 bài, 170/170 xanh) | **Xong** (chưa push) | — |
| **ai-CR-036-man-quan-ly-viec-cua-bot** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-05 của `doc/agent-hub/04` | **Màn «Việc của bot Telegram» trong ERP v2 (AN-006), CHỈ ĐỌC.** Backend `agent_hub/controller.py`: `GET /api/agent-hub/tasks` (lọc trạng thái + chữ, phân trang, tổng chi phí/thời gian bot/số lượt gom MỘT truy vấn cho cả trang), `GET /tasks/{id}` (yêu cầu, rà soát, kế hoạch, câu hỏi, dòng thời gian, bảng lượt chạy, hội thoại Telegram), `GET /stats` (việc theo trạng thái, chi phí theo ngày giờ VN và theo bước). Khóa quyền mới **`agent_task`** (ENTITIES 65 → 66, PUBLIC ở `SCOPE_FIELDS`, nằm trong `_SYS_ENTITIES` nên Quản lý thu mua không tự có). `STAGE_LABELS`. Frontend-v2: `/system/agent-tasks` (3 thẻ số + bảng lọc) và `/system/agent-tasks/:id`, menu + lối tắt trang Tổng quan Quản trị, `agent-task-format.ts` (+ test). Service mới `agent-erp` (profile `giao-dien`, cổng 8084) chạy giao diện v2 của worktree gọi `agent-api` — nơi đại ca xem màn và nơi chạy cổng kiểm v2. Cổng: typecheck 0 lỗi · lint 0 lỗi · vitest `src/modules/system` 135/135. Bài canh `test_muc_menu_manage_khong_mo_bang_hanh_dong_ma` đang đỏ SẴN trên nhánh (coffee_member, login_session), không do việc này. | `backend/app/modules/agent_hub/{controller,constants}.py` · `backend/app/{main,seed}.py` · `backend/app/core/{permissions,scoping}.py` · `frontend-v2/src/modules/system/{api/agent-hub-api.ts,hooks/use-agent-tasks.ts,utils/agent-task-format(.test).ts,components/agent-task-status-badge.tsx,pages/agent-task-{list,detail}-page.tsx,routes.tsx,config/{permission-groups,dashboard-shortcuts(.test)}.ts}` · `frontend-v2/src/{core/authorization/permission-types,shared/constants/{app-routes,query-keys}}.ts` · `docker-compose.agent.yml` · `test/backend/{test_agent_hub,test_pham_vi_khai_du_b07}.py` (191 xanh trừ 1 bài đỏ sẵn) | **Xong** (chưa push) | — |
| **ai-CR-035-nhan-anh-chup-loi** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-04 + N-02 của `doc/agent-hub/04` | **Bot nhận ảnh (N-02) và đưa ảnh chụp lỗi cho bước rà soát + sửa mã (A-04).** `telegram.download_file` (getFile + tải, trần `AGENT_FILE_MAX_MB`=20, lỗi không mang URL có token). `service.handle_message`: ảnh kèm chú thích = một tin chữ có ảnh; ảnh không chữ nằm chờ (`cho_chu_anh`) và báo nhận MỘT lần, tin chữ kế tiếp trong 10 phút nhận ảnh (`_adopt_pending_photos`); ảnh cùng album (`media_group_id`) ghép vào một tin; tải hỏng thì bảo gửi lại. Ảnh lưu ở volume `agent_files` (`/agent-files/tg/<yyyymm>/<chat>_<msg>.<ext>`; poller/worker/api ghi, runner CHỈ ĐỌC), cột mới `tab_agent_message.files` (JSON, migration `9a1f3c5e7b20`). Gom việc ghi «Kèm n ảnh chụp» vào mô tả (Gemini chỉ đọc chữ); `coder.task_images` + `image_block` liệt kê ảnh trong đề bài rà soát và sửa mã, mọi lượt `claude` được `--add-dir /agent-files`. Ba dấu ảnh vào `NOISE_ACTIONS` nên gửi ảnh giữa chừng không làm đứt mạch hẹn giờ / trả lời kế hoạch. Chưa có hạn xóa ảnh tự động. Tin thoại chưa làm (T-07, cụm Thư ký). | `backend/app/modules/agent_hub/{service,coder,telegram,model,constants}.py` · `backend/app/core/config.py` · `backend/migrations/versions/9a1f3c5e7b20_agent_hub_tep_dinh_kem_tin.py` · `docker-compose.agent.yml` · `test/backend/test_agent_hub.py` (+4 bài, 162/162 xanh) | **Xong** (chưa push) | — |
| **ai-CR-034-cong-kiem-frontend-v1** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-03 của `doc/agent-hub/04` | **Cổng kiểm `frontend/` bản cũ.** Bản cũ chỉ có typescript (không eslint, không vitest) và sẵn vài lỗi kiểu cũ; câu Q4 để trống nên em chọn: `coder.run_fe_v1_gate` chạy `tsc --noEmit` cả cây `frontend/`, CHỈ đỏ khi lỗi nằm trong tệp bot vừa sửa, lỗi ở tệp khác đếm thành «n lỗi kiểu ở tệp khác, có thể là lỗi cũ». Thư viện cài một lần theo lockfile vào `/worktrees/.deps/fe-v1-*` (`ensure_fe_deps`/`link_fe_deps` nhận `fe_dir`), gắn sẵn trước lượt claude khi kế hoạch có tệp `frontend/`; Claude được phép tự chạy `npm --prefix frontend exec -- tsc --noEmit -p frontend`. Kết quả vào `gate["frontend_v1"]`, gộp vào trạng thái cổng, thẻ gọn/đầy đủ/thân PR và đề bài «Sửa cho xanh». | `backend/app/modules/agent_hub/coder.py` · `test/backend/test_agent_hub.py` (+2 bài, 158/158 xanh) | **Xong** (chưa push) | — |
| **ai-CR-033-don-nhanh-bot-sau-khi-xong** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-02 của `doc/agent-hub/04` | **Dọn worktree + nhánh `bot/*` khi việc ĐÓNG** («xong» hoặc «bỏ»; câu Q3 để trống nên em chọn lúc đóng vì sau gộp vẫn còn «thu hồi» và «hỏi thêm» cần worktree + phiên). `service._schedule_cleanup` giao `agent.cleanup_task` (hàng `agent_code`, xếp sau lượt đang chạy vì runner `-c 1`); `coder.cleanup_task_branch` gỡ worktree, `branch -D` trong kho base, `push --delete` trên GitHub (nhánh chưa từng đẩy thì bỏ qua êm), ghi `note`. Chỉ đụng nhánh bắt đầu bằng `bot/`. Nhánh cũ của AI-0005..0007 chưa dọn (dọn khi đóng). | `backend/app/modules/agent_hub/{coder,service,tasks}.py` · `backend/app/core/celery_app.py` · `test/backend/test_agent_hub.py` (+2 bài, 156/156 xanh) | **Xong** (chưa push) | — |
| **ai-CR-032-do-thoi-gian-mot-viec** | 2026-09-24 | Đại ca 24/09 (*"phần Q1 Q6 để qua 1 bên đi, làm các phần mình còn thiếu trước em nhé, làm full 1 lần rồi anh test luôn"*) — mục A-01 của `doc/agent-hub/04` | **Dòng «Thời gian» trên thẻ kết quả (gọn + đầy đủ), thẻ deploy và /xem**: `coder.timing_line` cộng `duration_ms` của mọi lượt chạy theo bước (rà soát · kế hoạch · sửa mã · gộp/deploy · thu hồi · hỏi thêm; lượt đang chạy tính tới giờ hiện tại), tổng = từ `created_at` tới `closed_at`/bây giờ, phần chênh = chờ duyệt/hàng đợi. `fmt_minutes` in «45 giây» · «6,6 phút» · «33 phút». Không đổi bảng. | `backend/app/modules/agent_hub/{coder,service}.py` · `test/backend/test_agent_hub.py` (+2 bài, 154/154 xanh) | **Xong** (chưa push) | — |
| **ai-CR-031-danh-sach-tinh-nang** | 2026-09-24 | Đại ca (*"anh cần danh sách công việc cần phải làm, danh sách chức năng cần làm"*) sau khi hỏi thêm trợ lý biên bản họp từ mp3, lịch + nhắc việc, nghiên cứu/kiểm chứng, chấp nhận nhiều bot | **Tệp mới `doc/agent-hub/04-danh-sach-tinh-nang.md`**: 29 tính năng (A 8 Đậu Đậu còn lại gồm AN-005/006/007 + ai-CR-004 · N 5 nền nhiều bot · T 12 Thư ký · R 4 Nghiên cứu), nguyên tắc chia bot theo QUYỀN, sáu đợt làm, sáu câu chờ Q1-Q6. Đã rà: `meeting-recap/doc` chỉ có thiết kế, chưa có mã; khóa Gemini của bot là loại trả phí (đại ca xác nhận). | `doc/agent-hub/{04-danh-sach-tinh-nang,README}.md` | **Xong** (tài liệu) | — |
| **ai-CR-030-chot-stack-agenthub-khong-dung-nham** | 2026-09-24 | Đại ca báo lỗi «Bind for 0.0.0.0:3306 failed: port is already allocated» ở container `procurement-agent-hub-db-1` | **Nguyên nhân:** stack `agenthub` đã mất container, rồi một lần `docker compose up` trơn trong worktree (thiếu `-p agenthub` và tệp override) dựng nguyên bộ ERP thứ hai tên `procurement-agent-hub`; MySQL của nó giành cổng 3306 với `procurement-tool`. **Xử lý:** gỡ bộ dựng nhầm (`down`, giữ volume); chín service của tệp gốc gắn profile `stack-goc` trong `docker-compose.agent.yml` nên không bao giờ dựng ở stack bot; `.env` của worktree thêm `COMPOSE_PROJECT_NAME=agenthub` + `COMPOSE_FILE` (không commit) để `docker compose up -d` trơn tự thành đúng stack bot năm service. Dựng lại bot: năm service chạy, poller kéo tin, runner sẵn sàng, ERP thật không bị đụng. | `docker-compose.agent.yml` · `.env` (máy, không commit) | **Xong** | — |
| **ai-CR-029-tach-gop-va-deploy-dev** | 2026-09-23 | Đại ca (*"anh bảo gộp mà có bảo là sẽ đẩy lên dev đâu em"*) — đại ca nhắn «gộp AI-0007», bot gộp erp-v2 (cbe43c1568) VÀ deploy dev luôn vì ai-CR-014 gắn cứng hai bước. | **Tách gộp khỏi deploy dev.** (1) Dòng sổ STAGE_DEPLOY mang cờ `deploy`; `coder.merge_and_deploy` với `deploy=False` chỉ gộp + đẩy nhánh nền, việc về PROD với `deployed_dev_at` trống, thẻ `send_merge_card` «Dev CHƯA deploy». Thiếu cờ (nút bấm cũ «Gộp + deploy dev») = cả hai như trước. (2) Lệnh chữ: «gộp» = chỉ gộp; «gộp và deploy dev» / «… lên dev» = cả hai (`_DEPLOY_WORDS`); lệnh mới «deploy dev» cho bản đã gộp, chưa gộp thì bảo nhắn «gộp và deploy dev». Hẹn giờ giữ cờ. (3) Deploy tách lượt: nhánh nền được phép đi tiếp, chỉ đòi VPS HEAD còn chứa bản gộp (`_require_ancestor`); dịch vụ build lấy theo tệp của bản vá. (4) Thu hồi bản chưa từng lên dev thì chỉ revert nhánh nền, không SSH. (5) Mọi câu mời/nhắc (thẻ kết quả, tình trạng, hẹn giờ, nhãn xác nhận, câu nhắc Gemini `deploy_dev`) nói rõ gộp hay deploy. AI-0007 đã lỡ lên dev: giữ nguyên (health 200), đại ca muốn gỡ thì «thu hồi AI-0007». | `backend/app/modules/agent_hub/{coder,service,manager}.py` · `test/backend/test_agent_hub.py` (+4 bài, 152/152 xanh) | **Xong** (chưa push) | — |
| **ai-CR-028-hieu-y-dinh-thao-tac-theo-ngu-canh** | 2026-09-23 | Đại ca (*"thay vì lệnh thì anh nhớ là trợ lý có thể phân tích ý định dựa trên văn bản mà làm được mà ta, ví dụ anh nói đồng ý"*) | **Bước phân loại sẵn có đọc thêm ngữ cảnh và trả loại thứ tư `thao_tac`** (không thêm lượt gọi model). (1) `manager.run_intent(text, context, tasks)`: `tasks` = danh sách việc đang mở (dòng tình trạng chữ trơn, tối đa 8) + tin bot vừa nhắn; model trả `action` (13 nhãn tiếng Việt -> tên thao tác nội bộ qua `ACTIONS`), `task`, `when`, `detail`, `confident`. Nhãn lạ thì hạ về `mo_ho`. (2) `service._act_by_intent`: chắc thì chạy qua đúng `_run_task_command` của ai-CR-027; gộp / thu hồi / bỏ còn phải khớp bước hợp lệ của việc (`_candidates`), lệch thì hỏi lại. Chưa chắc thì hỏi một câu «Ý đại ca là … việc AI-xxxx phải không?» và đóng dấu `cho_xn:<thao tác>` trên tin hỏi. (3) `_confirm_by_text`: «đúng/ok/đồng ý…» hoặc «không/thôi…» trả lời câu hỏi đó chạy bằng DẤU đã ghi, không hỏi model lần hai; giờ hẹn / ý sửa lấy lại từ tin gốc; dấu bị gỡ sau khi dùng nên «đúng» lần hai không chạy lại. (4) Lệnh gõ đúng mẫu vẫn đi đường nhanh bằng regex. **Đo thật với Gemini + sổ dev:** «đồng ý» sau tin mời gộp -> gộp AI-0007 chắc chắn; «merge giúp anh cái lọc công nợ lúc 8h tối» -> gộp AI-0007 hẹn 20h; «thêm cột ngày giao vào màn công nợ» -> việc mới; «cái đó xong chưa em» -> mập mờ. Mỗi lượt ~1650 token vào, ~80 ra. | `backend/app/modules/agent_hub/{manager,service,constants}.py` · `test/backend/test_agent_hub.py` (+6 bài, 148/148 xanh) | **Xong** (chưa push) | — |
| **ai-CR-027-the-gon-va-lenh-bang-chu** | 2026-09-23 | Đại ca (*"chuyển qua nói logic đã làm thôi, chừng nào anh hỏi chi tiết rồi mới nói chi tiết… anh cũng không thích cái chọn option dưới dòng tin nhắn, anh thích anh nhắn vào hơn, ví dụ anh muốn bot tự merge code từ commit sửa mới này qua erp thì sao"*) | **Chế độ gọn `AGENT_TG_COMPACT` (mặc định BẬT).** (1) Thẻ kết quả chỉ còn: dòng `TÓM TẮT:` do Claude viết cuối tổng kết (đề bài bắt buộc: sửa LOGIC gì, đã kiểm gì, đánh giá rủi ro, 1-3 câu) · «Đã kiểm: …» một dòng · lệnh nhắn tiếp; không danh sách tệp, không tổng kết dài, không gửi `.diff` (chi tiết: «chi tiết AI-xxxx» / commit). (2) `reply` bỏ MỌI nút; các thẻ còn lại có dòng gợi ý lệnh (`coder.cmd_hint`). Nút trên thẻ cũ vẫn bấm được (callback giữ nguyên, thao tác tách thành hàm chung `approve_task`/`cancel_task`/`start_fix_gate`/`start_continue`/`close_done`; `answer_callback` bỏ qua khi không có nút). (3) **Lệnh bằng chữ** (`route_task_command`, đi trước mạch trả lời kế hoạch và phân loại): duyệt · sửa: … · gộp (kèm giờ = hẹn) · thu hồi · xong · bỏ việc/bỏ AI-x · làm tiếp · sửa cho xanh · chi tiết · mở PR · ghi sổ/không ghi · «làm luôn»/«ghi việc» thay hai nút mơ hồ. Chống nhầm: là lệnh chỉ khi có mã việc, hoặc cụm chỉ vào việc CÓ DANH TỪ («việc/fix/code/commit … này/vừa/mới»), hoặc lệnh TRƠN (`_BARE`); câu HỎI («được không», «nào», «?») chỉ trả tình trạng (`task_status_line`); không rõ việc nào thì hỏi lại bằng chữ. Gõ «gộp» = đồng ý gộp (luật ai-CR-014). Có bài canh «bỏ nút tạo mới…», «gộp hai cột…», «bỏ ô từ đến» vẫn là việc MỚI. 8 bài kiểm mới, 142/142 xanh. | `backend/app/modules/agent_hub/{service,coder,telegram}.py` · `backend/app/core/config.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-026-sua-cho-xanh-va-quyen-vitest** | 2026-09-23 | Trợ lý (lượt «Làm tiếp» AI-0007 ra cổng frontend ĐỎ: 2 bài vitest do chính bot viết, bot không tự chạy được vitest vì bị chặn) | **(1) Quyền chạy vitest theo từng thư mục.** Mẫu `Bash(npm --prefix frontend-v2 run test -- src/:*)` của ai-CR-019 KHÔNG khớp `… -- src/modules/finance` (Claude Code so theo nguyên từ) → AI-0007 bị chặn cả hai lần tự kiểm, giao bản vá có 2 bài vitest sai. Nay `allowed_tools(worktree)` sinh một mẫu cho MỖI thư mục có thật dưới `src/modules|shared|core`; không mẫu nào chạy được cả bộ. **(2) Nút «Sửa cho xanh»** (`fixg:`) trên thẻ khi cổng đỏ: `run_code_task(fix_gate=True)` nối phiên của lượt sửa vừa xong, gỡ commit cũ của bot bằng `reset --soft` (giữ thay đổi), đưa nguyên văn phần đỏ vào đề bài (`build_fix_gate_brief`, 40 lượt), rồi cổng kiểm / commit lại một lần / thẻ như thường; đã gộp thì không cho. Chạy thật AI-0007: 9,5 phút, 25 lượt, cổng typecheck + lint + vitest finance đều XANH. 4 bài kiểm mới, 134/134 xanh. | `backend/app/modules/agent_hub/{coder,service,tasks}.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-025-poller-doc-du-lieu-cu** | 2026-09-23 | Đại ca (*"nãy giờ gần 30p rồi mà nó chưa trả lời mình là xong hay chưa xong luôn"*) | **Nút bấm bị xử trên dữ liệu cũ.** Đại ca bấm «Làm tiếp» AI-0007 lúc 15:31:24, sổ ghi lượt bấm nhưng bot không làm gì, không nhắn gì. Nguyên nhân: `poll_once` đọc con trỏ (mở giao dịch) rồi GIỮ giao dịch đó suốt 25 giây long-poll và cả lúc xử tin; MySQL mặc định REPEATABLE READ nên mọi thứ tiến trình khác ghi trong lúc chờ đều vô hình — bot thấy AI-0007 còn «Thất bại», chưa có phiên (trợ lý gắn phiên lúc 15:31:10), rơi vào nhánh chỉ hiện toast. Lỗi chung, không riêng nút này (vd runner vừa sửa xong mà bấm «Gộp» trong cùng 25 giây). Sửa: đọc con trỏ xong thì `commit` NGAY rồi mới chờ Telegram, xử tin trong giao dịch mới. Chạy tiếp lượt «Làm tiếp» đại ca đã bấm. | `backend/app/modules/agent_hub/service.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-024-sua-ma-noi-phien-ra-soat** | 2026-09-23 | Đại ca (*"fix 1 chức năng có chút xíu mà chạy 12 phút sao nó dữ thần dị ta"*; *"làm ai-CR-024 đi em"*) | **Sửa mã đi tiếp phiên rà soát.** AI-0007 tốn ~6,5 phút rà soát rồi ~12 phút sửa mã, vì bước sửa mở phiên Claude MỚI đọc lại gần như từ đầu (40 lần đọc tệp) những gì phiên rà soát vừa đọc. Nay `run_code_task` gọi `claude -p --resume <phiên rà soát>` (quyền sửa mở ở lượt này, lượt rà soát vẫn chỉ đọc) trên ĐÚNG worktree rà soát đã dùng (`scan_task` ghi `branch_name`); đề bài bỏ trích đoạn tài liệu + đoạn rà soát (đã nằm trong phiên) và dặn không đọc lại. Chỉ nối khi: lượt rà soát thành công, xong trong 6 tiếng (`SCAN_REUSE_MAX_AGE_SEC`), worktree còn và sạch (`git status --porcelain` rỗng) — cắt lại worktree thì mọi tệp bị coi là «đổi sau khi đọc». Phiên rà soát mất («No conversation found») thì tự lùi về phiên mới. 3 bài kiểm mới, 130/130 xanh. Hiệu quả đo được ở việc kế tiếp. | `backend/app/modules/agent_hub/coder.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-023-het-luot-thi-lam-tiep** | 2026-09-23 | Đại ca (ảnh AI-0007: *"Bot sửa mã AI-0007 hỏng: claude báo lỗi [error_max_turns]"* — *"lỗi rồi nè"*) | **Hết lượt không còn là thất bại.** AI-0007 chạm trần `--max-turns 80` do BOT đặt (không phải hạn mức gói Max) sau ~12 phút: 40 lần Read + 21 Grep đi rà thư viện dùng chung và màn khác, mới sửa xong nửa việc (4 tệp +232/−17), rồi bị đóng «Thất bại» và mất cả mã phiên (không lưu). Sửa: (1) `MaxTurnsError` tách khỏi lỗi thường ở `parse_cli_json`; (2) mã phiên ghi vào lượt CODE NGAY trước khi gọi `claude`; (3) hết lượt thì KHÔNG commit, giữ phần dở, việc về «Đang hỏi lại» + thẻ «đã sửa n tệp (+a/−d)» với nút «Làm tiếp» (`cont:`) · «Bỏ việc này»; (4) «Làm tiếp» = `claude -p --resume <phiên>` trên đúng worktree dở, thêm `CONTINUE_MAX_TURNS=60`, rồi đi tiếp cổng kiểm/commit/thẻ như thường (`run_code_task(resume=True)`, Celery `agent.code_task` kwargs `resume`); (5) trần mặc định 80 → 120; (6) đề bài có mục «Ngân sách lượt»: đọc thẳng tệp rà soát đã chỉ, đọc cả tệp một lần, không rà thư viện dùng chung. Cứu AI-0007: gắn lại phiên `d046f410` (phiên sửa mã, khác phiên rà soát), gửi thẻ «Làm tiếp». 127/127 xanh. | `backend/app/modules/agent_hub/{coder,service,tasks}.py` · `backend/app/core/config.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-022-bot-noi-gon-va-bot-ton-tien** | 2026-09-23 | Đại ca (*"sao anh thấy nó trả lời dài dữ vậy ta, tốn token khá khá đó em… gần 20k tiền Việt rồi mà chưa làm được cái gì hết"*) | **Đo trước khi sửa:** các lượt Gemini của bot (gom, phân loại, 7 lượt lập kế hoạch) cộng lại ~29k token vào / 43k ra ≈ 3 nghìn đồng; khoản lớn là **hai lần nạp lại cả kho tài liệu** (22 và 23/09, trợ lý chạy tay) — mỗi lần ~3,5 triệu ký tự ≈ 1,2 triệu token nhúng. Cột chi phí của bot toàn 0 vì bảng giá thiếu tên thật `gemini-3.8-flash` mà bí danh trả về. **Sửa:** (1) nạp kho theo phần thay đổi — tệp mà các đoạn trong Qdrant y hệt thì giữ vector, chỉ đóng dấu lượt mới (`_unchanged`/`_retag`, so bằng chữ nên kho cũ dùng lại luôn); chạy thật: 45/48 tệp giữ nguyên, nhúng 1381 thay vì 4569 đoạn. (2) Lập kế hoạch khi đã có rà soát thì TẮT suy nghĩ; không có rà soát thì trần suy nghĩ 4096; lớp bot gửi `thinkingBudget: 0` cả với bí danh (lớp dùng chung không gửi nên model tự nghĩ ~2,5k token) — đo thật: một lượt kế hoạch 13k → 420 token ra, ~900 → ~60 đồng. (3) Nói gọn: kế hoạch tối đa 5 bước một câu, kiểm thử tối đa 3 dòng, đoạn rà soát tối đa 12 dòng; thẻ bỏ «Tài liệu đã tra» và câu giải thích rủi ro (AI-0007: 1675 → 435 ký tự). (4) Vá câu đã trả lời bị hỏi lại (thẻ AI-0007 vẫn «Còn chờ đại ca quyết» sau «oke theo ý của em»): trả lời thì đánh dấu `answered` trên lượt rà soát. (5) Bảng giá thêm `gemini-3.8-flash`; trả chú thích lạc chỗ ở constants. Claude Code (rà soát, sửa mã) chạy bằng gói thuê bao, không tốn tiền Gemini. 125/125 xanh. | `backend/app/modules/agent_hub/{memory,manager,service,coder,constants}.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-021-bao-nhan-va-bao-dang-chay** | 2026-09-23 | Đại ca (*"khi gửi thì phải có gì gửi lại như em đã nhận tin, anh chờ em xíu… sau đó nếu qua 1p hoặc 90s thì nhắn thêm 1 tin nữa anh đợi em xíu, để anh biết là việc nó đang chạy"*) | **(1) Báo nhận ngay.** Tin được xếp là việc thì nhắn «Em nhận tin rồi, anh chờ em xíu…» (dấu `da_nhan`); cả chùm tin liên tiếp chỉ một câu; nút «Ghi thành việc sửa mã» cũng mang dấu đó. **(2) Báo đang chạy.** Beat `agent-heartbeat` mỗi phút: việc ở rà soát / lập kế hoạch / sửa mã / gộp+deploy mà im quá 90 giây thì nhắn «em vẫn đang …, đã n phút» (dấu `dang_chay`), sau đó SỬA chính tin đó (`telegram.edit_text`) cho đúng số phút, không kêu chuông lần nữa; thôi sau 60 phút. Mạch hẹn giờ gộp và hỏi thêm bản vá bỏ qua hai dấu này (`NOISE_ACTIONS`) để tin báo của việc khác không làm đứt mạch. **(3) Hỏi một chỗ, gọn** (đại ca thấy AI-0007 hỏi hai lần quá dài): đoạn phân tích thôi liệt kê «Cần đại ca quyết»; thẻ kế hoạch là chỗ duy nhất hỏi; «Chưa làm: … chờ đại ca quyết: Q» chỉ giữ Q, giả định thường rút thành «… — đại ca đồng ý không?», câu trùng bỏ (`merge_questions`); câu rà soát mà kế hoạch quên thì tự thêm. **(4) Vá JSON cụt** (AI-0007 lập lại kế hoạch hỏng ở ký tự 1781): Gemini tính token suy nghĩ vào trần đầu ra, lượt trước nghĩ ~13 nghìn → lớp `AgentGeminiProvider` chặn suy nghĩ `thinkingBudget` 8192 và cộng vào trần (provider dùng chung với Trợ lý AI không đổi); vẫn cụt thì thử lại một lần không suy nghĩ. **(5) Hỏng thì có lối ra**: câu báo lỗi lập kế hoạch kèm nút «Lập lại kế hoạch» · «Bỏ việc này» (AI-0007 từng kẹt ở «Đang hỏi lại» không câu hỏi, không nút). **(6)** Bỏ câu «dính tiền nên em vẫn hỏi» (việc công nợ câu nào cũng dính). Gỡ kẹt AI-0007: lập lại kế hoạch thật, 3,8 nghìn token, 3 tệp v2, thẻ Duyệt đã gửi. 121/121 xanh. | `backend/app/modules/agent_hub/{service,coder,manager,tasks,telegram,constants}.py` · `backend/app/core/celery_app.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-020-lenh-xem-lich-su-viec** | 2026-09-23 | Đại ca (*"AI-0006 anh bấm bỏ rồi mà ta, vậy không có chỗ nào ghi nhận lại log của nó hả ta"*; *"làm ai-CR-020 đi, chỉ cần /xem là đủ"*) | **(1) `/xem <mã>`** (`AI-0006` · `ai-6` · `6`): lịch sử một việc KỂ CẢ việc đã đóng — trạng thái, rủi ro, giờ tạo/đóng, ghi chú, nhánh/PR/bản gộp/lên dev, số tin, **bảng các bước** trong `<pre>` (Telegram không có thẻ bảng; chữ đều khổ, dưới ~32 ký tự một dòng cho vừa điện thoại), rồi yêu cầu · đoạn rà soát · kế hoạch cuối chia nhau chỗ còn lại (trần ~3700 ký tự). `/ds` giữ nguyên (chỉ việc đang mở) theo đại ca chốt. **(2) Bấm «Bỏ việc này»** nay nhắn một dòng xác nhận kèm `/xem <mã>` — trước chỉ có toast, khung chat không còn dấu vết. **(3) Vá lỗi có sẵn: giờ UTC.** Container bot và MySQL chạy UTC (đo 23/09: 05:00 khi máy 12:00): hẹn «14:30» ở ai-CR-014 thành 14:30 UTC = 21:30 giờ Việt Nam; các mốc giờ bot in lệch 7 tiếng; trần 5 việc/ngày reset lúc 7 giờ sáng. Mô-đun `timeutil` (+7 cố định, VN không đổi giờ): giờ hẹn đọc theo giờ VN rồi lưu UTC, mọi chỗ in giờ đổi sang VN, «hôm nay» của trần tính theo VN. Chạy thật `/xem AI-0006` gửi Telegram được. 4 bài kiểm mới + sửa 1 bài ai-CR-014, 114/114 xanh. | `backend/app/modules/agent_hub/{service,coder,timeutil}.py` · `test/backend/test_agent_hub.py` | **Xong** | — |
| **ai-CR-019-cong-kiem-frontend-v2** | 2026-09-23 | Đại ca (*"ai-CR-019 thì em cứ tiếp tục nhé"*) | **Cổng kiểm frontend-v2 trong runner.** `coder.run_gate` gộp backend (pytest) + `run_fe_gate`: `tsc --noEmit` cả cây · `eslint` tệp vừa đụng · `vitest run` thư mục phân hệ/khu vừa đụng (`fe_vitest_targets`), trạng thái none/pass/fail/skip, skip (không có thư viện) KHÔNG tính xanh. Thư viện: `ensure_fe_deps` cài `npm ci` một lần mỗi băm `package-lock.json` vào `/worktrees/.deps` (cache npm cùng volume, giữ 2 bộ), `link_fe_deps` gắn liên kết `frontend-v2/node_modules` trước lượt claude + ghi `info/exclude`, hỏng thì chỉ trả lý do. Claude được thêm đúng hai lệnh `npm --prefix frontend-v2 run typecheck` / `run test -- src/…`. Thẻ + thân PR có dòng «Frontend v2: …» riêng, dòng «CHƯA chạy» cũ chỉ còn cho `frontend/`. Đo thật trên AI-0006: cài 12 giây, cổng 109 giây, xanh, git sạch; lệnh của claude chạy đúng thư mục (9/9 bài `src/core/api`). QĐ-04 (v1 trước) GIỮ NGUYÊN — đại ca chưa quyết đổi. 4 bài kiểm mới, 110/110 xanh. | `backend/app/modules/agent_hub/coder.py` · `test/backend/test_agent_hub.py` | **Xong** — commit `0c4a0850` (chưa push) | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §7 |
| **ai-CR-018-ra-soat-so-main-va-tai-lieu-o-may** | 2026-09-23 | Đại ca (chốt hai kiến nghị sau lượt AI-0006: *"Cho Đậu Đậu xem thêm nhánh main trên GitHub… Mount thư mục tài liệu ở máy vào dạng chỉ đọc, và nạp lại kho tài liệu từ đó"*) | **(1) So main.** `coder.fetch_base` kéo cả `main` từ GitHub (lùi về bản ở máy nếu không khóa); đề bài rà soát bắt chạy `git log origin/erp-v2..origin/main`, lỗi sửa ở main chưa gộp thì ghi `already_fixed`. **(2) Tài liệu ở máy.** `../procurement-tool/doc` mount chỉ đọc vào runner `/local-docs` + `claude --add-dir`; đề bài chỉ thẳng change-log-bao/change-log/nhật ký chưa commit. Kho `agent_docs` nạp từ `doc/` + `CLAUDE.md` ở máy (celery-worker đè hai mount cùng đích), `doc/agent-hub/` lấy từ chỗ mount sổ (`memory.rel_path`). Rà thư mục trước khi mount: không `.env`/khóa/bản sao DB; hai giá trị trong tài liệu POS365 là chỗ giữ chỗ 2 ký tự. Kiểm thật: runner đọc được, ghi bị chặn; kho base có erp-v2 `bbff5f5c` (= GitHub) + main `b57c5c9b`; nạp lại 48 tệp / 4569 đoạn. Phát hiện: thư mục ở máy cũng CHƯA có bao-CR-465 (làm ở thư mục làm việc khác) — hai nguồn bù nhau. 3 bài kiểm mới/sửa, 106/106 xanh. | `backend/app/modules/agent_hub/{coder,memory}.py` · `backend/app/core/config.py` · `docker-compose.agent.yml` · `test/backend/test_agent_hub.py` | **Xong** — commit `0c4a0850` (chưa push) | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §6 |
| **ai-CR-017-ra-soat-ma-truoc-ke-hoach** | 2026-09-23 | Đại ca (*"làm ai-CR-017 đi, mọi việc đều rà soát"*) | **Rà soát mã thật trước kế hoạch, mọi việc.** Gom xong → «Đang rà soát mã» (`ST_SCANNING=13`) + nhắn «Đậu Đậu nhận việc» → runner `agent.scan_task`: worktree từ erp-v2 mới nhất, `claude -p` chỉ đọc (40 lượt, 15 phút) → nhắn đại ca đoạn phân tích mở bằng Kết luận + «Cần đại ca quyết» → lập kế hoạch với kết quả rà soát trong đề bài (`run_plan(review=)`), tên tệp trơn nối đủ đường dẫn, câu nghiệp vụ chưa trả lời thì không tự quyết. Hỏng ở đâu cũng lập kế hoạch theo tài liệu; vòng beat nhặt rà soát kẹt (45 phút xếp hàng / quá trần). **Vá lỗi có sẵn**: kho base của runner lấy nhánh nền từ `.git` máy đại ca (chậm 7 commit so với GitHub) → `fetch_base` kéo thẳng từ GitHub. **Vá lỗ bài kiểm**: container bot bật `AGENT_CODER_ENABLED` nên một bài gom việc đã gửi `agent.scan_task` THẬT sang Redis (runner từ chối vì chưa nạp mã) → fixture `bot` tắt cờ + chặn mọi `dispatch_*`. Chạy thật AI-0006: 6,6 phút, tìm đúng gốc lỗi (gửi tên phòng thay vì mã, dò theo tên trả 0 khi trùng tên giữa pháp nhân); từ lượt đó vá thêm: cắt câu dẫn «Giờ trả lời», hiện câu cần đại ca quyết. 8 bài kiểm mới, 103/103 xanh. | `backend/app/modules/agent_hub/{coder,service,tasks,manager,constants}.py` · `backend/app/core/celery_app.py` · `test/backend/test_agent_hub.py` | **Xong** — commit `0c4a0850` (chưa push) | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §6 |
| **ai-CR-016-ten-dau-dau-va-nhat-viec-ket** | 2026-09-23 | Đại ca (*"gọi là Đậu đậu đi"*, *"sửa lời nhắc cho bot tự xưng là Đậu Đậu đi em"*; *"nó không trả lời luôn, nó im 3 phút rồi"*) | **(1) Bot tự xưng Đậu Đậu trên Telegram.** `constants.BOT_NAME` + `BOT_PERSONA`; câu hỏi từ Telegram gửi Trợ lý AI kèm `system=BOT_PERSONA` (chỉ CHÈN THÊM vào cuối, không đè định nghĩa + rào an toàn), nên Trợ lý AI trên web vẫn là «Trợ lý AI». Lời chào `/start`, câu lỗi, lời nhắc lập kế hoạch và đề bài Claude Code gọi tên Đậu Đậu. Tên hiển thị của tài khoản Telegram đại ca đổi ở BotFather. **(2) Việc kẹt ở trạm gom không ai nhặt lại.** Ca AI-0006: đại ca nhắn 10:10, bot gom xong 10:13 rồi đang lập kế hoạch thì trợ lý khởi động lại worker (restart mặc định chờ 10 giây, lượt Gemini cần 30-90 giây) — lượt PLAN chết không để dấu, việc nằm ở TRIAGE mãi, bot im. Vá hai lớp: `service.resume_stuck_plans` (vòng beat mỗi phút lập lại kế hoạch cho việc TRIAGE quá 3 phút, tối đa 2 lượt PLAN hỏng có ghi sổ) + `stop_grace_period: 120s` cho celery-worker của stack bot. AI-0006 đã lập lại tay, thẻ đã về Telegram. 3 bài kiểm mới, 95/95 xanh. | `backend/app/modules/agent_hub/{constants,service,tasks,manager,coder}.py` · `docker-compose.agent.yml` · `test/backend/test_agent_hub.py` | **Xong** — commit `0c4a0850` (chưa push) | — |
| **ai-CR-015-so-quyet-dinh-giam-hoi-xac-nhan** | 2026-09-23 | Đại ca (*"tổ chức bộ câu hỏi... sao chép những thứ anh biết, hay quyết định quen thuộc thành 1 file.md, để em dựa vào đó đánh giá tình huống hợp lý hay không"*; chốt 3 đề xuất: bot chỉ đề xuất ghi, sổ nằm trong kho mã, việc dính tiền/công nợ/phân quyền luôn hỏi) | **Sổ quyết định cho bot tra trước khi hỏi.** (1) `doc/agent-hub/03-so-quyet-dinh.md` bản 1.0, 11 mục gom từ các lần đại ca đã chốt, mỗi mục có tình huống / bot làm / không áp khi / nguồn. (2) Lượt PLAN và đề bài Claude Code nạp sổ; có mục khớp thì ghi «Theo QĐ-xx», không có mà có đường an toàn thì «Em giả định»; §D thôi dừng vì «không tái hiện được» / «thiếu dữ liệu». (3) Luật 3 thi hành trong mã: việc rủi ro cao không nạp sổ, giả định của nó thành câu hỏi; `playbook.protected_topic` chặn đề xuất và chặn ghi mục chạm tiền/công nợ/phân quyền/DB/prod/main/gộp mã. (4) **Vá lỗ có sẵn**: câu trả lời cho câu hỏi lại trước đây đẻ thành việc MỚI; nay gắn vào việc cũ (30 phút, không xen tin khác; nút «Trả lời câu hỏi» mở lại cửa) rồi lập lại kế hoạch ngay, không cần /gom; «Sửa lại» đi cùng đường. (5) Sau đó bot nháp mục sổ (`STAGE_RULE=24`) và gửi thẻ «Ghi vào sổ» / «Không, lần nào cũng hỏi»; ghi = nối `QĐ-<số kế>` vào cuối tệp. (6) Tệp `.md` không tính lệch kế hoạch; sổ là tệp cấm với `claude`. Đo thật với Gemini: ca thiếu mã phiếu nay không hỏi mà áp QĐ-01; vá hai lỗi lộ ra khi chạy thật (trần token 4096 cắt JSON → 16384; «thêm cột» giao diện bị bắt nhầm là DB). 10 bài kiểm mới, 92/92 xanh. | `backend/app/modules/agent_hub/{playbook,manager,service,coder,constants}.py` · `backend/app/core/config.py` · `docker-compose.agent.yml` (mount `doc/agent-hub` → `/agent-docs`) · `test/backend/test_agent_hub.py` | **Xong** — commit `0c4a0850` (chưa push) | `doc/agent-hub/03-so-quyet-dinh.md` · `01-thiet-ke-ky-thuat.md` §6 · `02-bo-quy-tac-bot.md` §D |
| **ai-CR-014-luat-nhanh-va-deploy-thu-vps** | 2026-09-22 | Đại ca (*"các nhánh chính quan trọng như main, dev, erp-v2... cần a merge thông qua merge request, còn lại em có thể đẩy thẳng xong vào ssh vps rồi deploy để anh test chỉ cần hỏi trước a 1 tiếng"*) | Đại ca chốt bốn câu 22/09 (*"em có thể đẩy lên nhánh của em, sau đó hỏi anh là có cần merge, hoặc đẩy lên dev luôn không, nếu em ok thì em cứ merge thẳng vào erp… bảo bỏ thì nhả code về"* · *"cứ sử dụng ssh như hiện tại, trên máy này có"* · *"hỏi trước 1 tiếng… nghĩa là phải hỏi trước anh để anh trả lời, dùng để xác nhận"* · *"branch protection cứ để như hiện tại, nhưng muốn merge code thì phải hỏi ý anh"*). Làm: thẻ kết quả thêm nút «Gộp erp-v2 + deploy dev» (khi `AGENT_DEPLOY_ENABLED`) → thẻ hỏi kể ba bước + số tệp/dòng/cổng kiểm → «Đồng ý: gộp + deploy ngay» · «Hẹn giờ» (nhắn giờ, `parse_schedule_time`; beat `agent-deploy-due` mỗi phút nhặt) · «Thôi». Runner: worktree `agent-merge` từ `origin/erp-v2`, `merge --no-ff`, đẩy `erp-v2` KHÔNG `--force`, SSH `bash -s` script qua stdin (reset cứng + `up -d --build` đúng service theo thư mục đụng), chờ health 200 → ST_PROD + thẻ «Thu hồi» / «Xong». Thu hồi = `git revert -m 1` + đẩy + deploy lại → «Đang hỏi lại». Hỏng trước đẩy → REVIEW; sau đẩy → ST_PROD không `deployed_dev_at` + «Deploy dev lại» (không gộp lần hai). Khóa SSH: `AGENT_VPS_SSH_KEY_FILE` (đường dẫn) bind-mount chỉ-đọc vào runner, chép 0600 tạm mỗi lượt, `claude` không thấy. Trạng thái mới `ST_DEPLOYING=12`, giai đoạn `STAGE_DEPLOY=22`/`STAGE_REVERT=23`. Kiểm thật: SSH từ runner lên VPS đọc HEAD dev OK; 14 bài kiểm mới, 82/82 xanh. **23/09 đại ca chốt thêm**: đường mặc định là bot gộp theo lệnh; link PR chỉ gửi khi đại ca muốn tự merge → thẻ kết quả xếp nút gộp đầu, nút PR đổi nhãn «Gửi link PR để anh tự merge», `AGENT_PR_ENABLED` giữ tắt; việc chạy thử AI-0005 đại ca bảo bỏ qua, đã đóng «Đã bỏ» trong sổ bot (PR #95 để nguyên trên GitHub). **Sự cố cùng ngày: giá trị `AGENT_GITHUB_TOKEN` bị in ra bản ghi phiên khi rà `.env` — kiến nghị cấp lại PAT.** | `backend/app/modules/agent_hub/{constants,coder,service,tasks}.py` · `backend/app/core/{config,celery_app}.py` · `docker/Dockerfile.runner` (+openssh-client) · `docker/vps_ssh_key.placeholder` · `docker-compose.agent.yml` · `.env.example` · `test/backend/test_agent_hub.py` | **Xong** — commit `0c4a0850` (chưa push; chưa chạy lượt gộp thật) | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §7 · `02-bo-quy-tac-bot.md` C10 + §E |
| **ai-CR-013-hoi-them-ve-ban-va** | 2026-09-22 | Đại ca (*"tiếp phần hỏi thêm giúp tôi nhé"*) | **Bậc 2 GĐ2b·1: hỏi thêm về bản vá bằng đúng phiên Claude Code đã sửa việc.** Thẻ kết quả (cả thẻ leo thang) thêm nút «Hỏi thêm về bản vá» (`ask:<id>`); bấm thì bot KHÔNG gỡ nút thẻ, nhắn lời mời (dấu `cho_hoi_va`). Tin chữ kế tiếp trong 10 phút sau dấu `cho_hoi_va`/`hoi_va`/`tra_loi_va` là câu hỏi: đi trước mọi rẽ nhánh của `_route_plain_text` (không qua Gemini), đóng dấu `hoi_va`, Celery `agent.ask_task(task_id, message_id)` vào hàng đợi runner. Runner `coder.answer_patch_question`: lượt `tab_agent_run` trạm mới `STAGE_ASK = 21`, `claude -p --resume <phiên>` với `--allowedTools` chỉ đọc (Read/Glob/Grep/git diff/status/log), 15 lượt, 10 phút; đề bài dặn trả lời ngắn Markdown đơn giản, không sửa tệp, câu hỏi là đòi đổi bản vá thì bảo bấm «Sửa». Trả lời `markdown=True` + nút «Hỏi tiếp»; hỏng → câu lý do + nút «Hỏi lại», trạng thái việc không đổi; `No conversation found` dịch thành "phiên không còn — bấm Sửa". **Sửa gốc phiên mất**: `CLAUDE_CONFIG_DIR = <AGENT_WORKTREE_ROOT>/.claude` (volume, chown runner) thay cho `~/.claude` trong lớp ghi container. Kèm: tổng kết bot trên thẻ đi qua `md_to_html` (hết `**đậm**` thô); `agent.publish_task`/`agent.ask_task` khai `task_routes`. 9 bài kiểm mới, tệp 68 xanh. | `agent_hub/constants.py` (`STAGE_ASK`, `ACT_WAIT_PATCH_Q`/`ACT_PATCH_Q`/`ACT_PATCH_ANSWER`) · `coder.py` (`claude_config_dir`, `_run_cli`, `build_question_brief`, `run_claude_resume`, `latest_code_run`, `session_id_for`, `dispatch_question`, `answer_patch_question`, `send_review_card`) · `service.py` (`_patch_question_target`, `_ask_patch`, `_invite_patch_question`, nút `ask`) · `tasks.py` (`agent.ask_task`) · `core/celery_app.py` · `test/backend/test_agent_hub.py` | **Xong 22/09/2026**, kiểm chứng phiên sống trong volume (`/worktrees/.claude/projects/...jsonl`, `--resume` nhớ lượt trước). Phiên AI-0005 đã mất trước đó nên hỏi việc đó sẽ nhận câu "phiên không còn". Chưa commit. Chưa chạy thật trên một việc mới. | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §7 *Hỏi thêm về bản vá* |
| **ai-CR-012-bac-2-day-github-mo-pr** | 2026-09-22 | Đại ca (*"a dán code git hub vào .env rồi đó"*) | **Bậc 2, giai đoạn 2a: runner đẩy nhánh bot lên GitHub + mở PR vào `erp-v2`.** Sau commit, runner `git push --force` lên `refs/heads/bot/<mã>-<slug>` rồi `POST /pulls`; mô tả PR dựng từ việc + kế hoạch + tệp +/− + cổng kiểm + tổng kết bot; `pr_url` ghi vào việc, thẻ kết quả thêm dòng PR + nút liên kết «Mở PR trên GitHub». Cờ `AGENT_PR_ENABLED` (mặc định tắt): tắt thì thẻ có nút «Đẩy GitHub + mở PR» → `pr:<id>` → Celery `agent.publish_task` trong runner → `coder.publish_existing` (lấy tệp/cổng kiểm/tổng kết từ `artifact` lượt CODE gần nhất); nút đó cũng là «Đẩy GitHub lại» khi tự đẩy hỏng — đẩy hỏng KHÔNG làm hỏng việc. Khóa `AGENT_GITHUB_TOKEN` (PAT chi tiết, một kho, Contents + Pull requests) đại ca tự dán `.env`; đọc thẳng `os.environ`, vào `git push` bằng `GIT_CONFIG_*` (không lên dòng lệnh), không bao giờ vào `claude`. PR trùng nhánh → lấy PR đang mở. Nhánh bot không tự xóa sau merge (mặc định giữ, đại ca chưa quyết). Nút Telegram nhận URL thành nút liên kết. 8 bài kiểm mới, tệp 59 xanh. | `agent_hub/coder.py` · `service.py` · `tasks.py` · `telegram.py` · `core/config.py` · `.env.example` | **Xong, CHẠY THẬT 22/09/2026**: bấm nút đẩy tay cho AI-0005 (nhánh GĐ1 còn trong runner) → 7 giây → **PR #95** `bot/ai-0005-n-009-...` → `erp-v2`, 2 tệp +87/−4, GitHub báo mergeable — PR đầu tiên do bot mở, nợ N-009 chờ đại ca merge. Chưa commit. Còn: GĐ2b (hỏi thêm `--resume` + công tắc đẩy dev), bật branch protection `main`/`erp-v2` trên GitHub, ai-CR-004 OTP để cuối. | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §7 |
| **ai-CR-011-bac-2-runner-claude-code** | 2026-09-22 | Đại ca (*"làm cái này trước đi em. Bậc 2: bot tự sửa mã bằng Claude Code... Runner thì chạy trên docker đi em"*) | **Bậc 2, giai đoạn 1: bấm Duyệt là bot sửa mã thật.** Tới nay bấm Duyệt chỉ ghi sổ. Nay khi cờ `AGENT_CODER_ENABLED` bật, Duyệt chuyển việc sang trạm CODE và ném vào hàng đợi riêng `agent_code`; service mới `agent-runner` (ảnh riêng có git + Node + Claude Code CLI, 3 GB RAM, 2 CPU, một việc một lúc) nhận việc: tạo worktree `bot/<mã>-<slug>` từ `origin/erp-v2` trong volume riêng (kho chính mount chỉ đọc, không đổi một byte) → dựng đề bài từ kế hoạch + `plan_files` + tài liệu Qdrant + bộ quy tắc C1..C10/§D → chạy `claude -p --output-format json` với danh sách công cụ hẹp (đọc/sửa tệp, pytest, git diff/status/log; KHÔNG git commit/push, KHÔNG docker/curl), đề bài qua stdin, phiên mới mỗi việc → `git add -A` rồi so tệp đã đụng với `plan_files`: đụng tệp cấm (`.env*`, khóa, migration, seed prod, `permissions.py`, `scoping.py`, workflow CI, `CLAUDE.md`, `.claude/`, bộ quy tắc) / hơn 25 tệp / hơn 30 % ngoài kế hoạch / không sửa gì → `git reset`, KHÔNG commit, việc sang NEEDS_INPUT; còn lại chạy pytest các tệp test/backend vừa đụng, commit trong container, việc sang REVIEW → gửi thẻ kết quả (số tệp, +/− dòng, số lượt, phút, chi phí, từng tệp có đánh dấu NGOÀI kế hoạch, cổng kiểm XANH/ĐỎ, tổng kết của bot) kèm tệp `<mã>.diff`. Chốt trước khi giao: kế hoạch không có `plan_files` (B2) hoặc đã đụng tệp cấm thì Duyệt cũng không giao. **Bí mật:** Celery chạy root nhưng git/claude/pytest hạ xuống người dùng `runner`; tiến trình `claude` nhận MÔI TRƯỜNG SẠCH dựng từ đầu, chỉ có `CLAUDE_CODE_OAUTH_TOKEN` (từ `claude setup-token`, đại ca tự dán vào `.env`, cố ý không khai trong Settings), token Telegram/key Gemini/mật khẩu DB không xuống tới nó; git và pytest không thấy cả khóa Claude. Không có `ANTHROPIC_API_KEY` (QĐ-AI-3). **Chưa có ở GĐ1:** đẩy GitHub/mở PR, hỏi đáp về bản vá, công tắc đẩy dev bằng thẻ Telegram (GĐ2); cổng kiểm frontend (GĐ3); chặn egress của runner (compose không có allowlist — ghi rủi ro mở ở thiết kế §11). Sai lệch cố ý so với luật C2: tên nhánh `bot/<mã việc>-<slug>` chứ không phải `<tên>-CR-<số>`, vì bot không cấp được số CR an toàn khi hai tệp change-log đang được nhiều phiên sửa. | Chỉ stack bot ở máy làm việc. Thêm 9 cờ cấu hình (mặc định tắt/hợp lý), 1 hằng trạm `STAGE_CODE`, 1 định tuyến Celery, 1 ảnh Docker + 1 service + 1 volume; không đụng bảng, không migration (bốn cột bậc 2 của `tab_agent_task` đã khai sẵn từ ai-CR-001). Khi cờ tắt, hành vi bấm Duyệt y như bậc 1. Prod không dính. | **Hoàn tất GĐ1** 22/09/2026 — 51 bài kiểm Agent Hub xanh (16 bài mới cho bậc 2) + 8 bài Gemini; ảnh `agent-runner` dựng xong, worker nghe đúng hàng đợi `agent_code`, thử tạo worktree thật từ kho chính trong container (tệp thuộc `runner`, 97 MB), thử gọi `claude` với khóa sai ra đúng câu 401 hướng dẫn `setup-token`. **Chạy thật lần đầu 22/09/2026 — việc AI-0005 = nợ N-009 (câu lỗi 502 Trợ lý AI lộ lỗi thư viện):** Duyệt qua đúng `handle_callback` → runner sửa 2/2 tệp trong phạm vi (+87/−4, thêm `logger.exception` + câu cố định, tệp bài kiểm mới 3 ca), 0 tệp ngoài kế hoạch, cổng kiểm XANH 3/3, commit `e1a1c971` trên `bot/ai-0005-...` trong volume runner, thẻ REVIEW + `AI-0005.diff` về Telegram; 3,3 phút · 54 lượt · ~$1,43 ước của CLI. **Lỗi tìm ra:** bot gõ `python3 -m pytest` (mẫu cho phép chỉ khớp `python`) rồi thử lại hơn nửa số lượt kể cả lệnh `docker compose exec` chép từ CLAUDE.md, tổng kết ghi "chưa chạy được bài kiểm" — vá ngay: `ALLOWED_TOOLS` nhận `python3` + `py_compile`, luật C6 trong đề bài nói rõ không có Docker và lệnh bị chặn thì đừng lặp. Bẫy vận hành: đổi `.env` phải `--force-recreate` CẢ `agent-api` (lần bấm đầu rơi vào "bot đang tắt" vì API còn env cũ). Bản vá N-009 chưa sang `erp-v2` (GĐ2). Chưa commit. | `backend/app/modules/agent_hub/coder.py` (mới) · `service.py` (`_dispatch_coder`) · `tasks.py` (`code_task`) · `constants.py` · `core/config.py` · `core/celery_app.py` · `docker/Dockerfile.runner` (mới) · `docker-compose.agent.yml` (service `agent-runner` + volume) · `.env.example` · `doc/agent-hub/01-thiet-ke-ky-thuat.md` §7/§9/§10/§11 · `test/backend/test_agent_hub.py` |
| **ai-CR-010-doi-ten-may-chu-stack-bot** | 2026-09-22 | Em kiến nghị (đại ca: *"ok đổi tên đi em, dựng lại stack bot luôn"*) | **Stack bot đá người dùng ra khỏi ERP, gốc nằm ở tên service.** Stack `agenthub` nối vào chung mạng của stack chính để dùng lại cơ sở dữ liệu, Redis và Qdrant; máy chủ của nó tên `api`, trùng đúng tên máy chủ thật. Docker Compose **luôn** cấp cho container một bí danh mạng bằng đúng tên service, trên mọi mạng nó nối vào, nên mạng chung có hai container cùng mang bí danh `api` và tên miền nội bộ trả về luân phiên hai địa chỉ. Giao diện ERP v2 gọi API qua bí danh đó, khoảng một nửa số lệnh gọi rơi sang máy chủ bot — nơi không có phiên đăng nhập của người dùng — nên trả lỗi hết phiên, làm giao diện gọi làm mới vé, cũng hỏng, rồi đá thẳng ra màn đăng nhập. Triệu chứng đánh lừa vì đăng nhập vẫn được và vài lệnh đầu vẫn chạy. Đo bằng một vé hợp lệ, hai mươi lượt gọi mỗi đường: gọi thẳng máy chủ thật đúng hai mươi trên hai mươi, gọi qua giao diện chỉ đúng chín trên hai mươi. **Bí danh mặc định không gỡ được, chỉ thêm được**, nên cách duy nhất là đặt tên khác: khai lại đầy đủ máy chủ của stack bot dưới tên `agent-api` và bỏ hẳn service trùng tên. Trước đó đã vá tạm phía tiêu dùng bằng một tệp cấu hình cục bộ ở máy làm việc (cấp cho máy chủ thật bí danh riêng), tệp đó đã nằm trong danh sách bỏ qua của git nên không commit. | Đổi tên service trong `docker-compose.agent.yml`: thêm `agent-api` khai đủ (build, env_file, gắn thư mục mã, cổng 8010, hai mạng), `celery-worker` và `celery-beat` phải ghi đè danh sách chờ bằng `!override` vì tệp gốc chờ service `api` không còn dựng nữa. Container đổi tên `agenthub-api-1` thành `agenthub-agent-api-1`, cổng 8010 giữ nguyên. Lệnh chạy bài kiểm của stack bot đổi theo tên mới. Không đụng bảng, không đụng biến môi trường, cơ sở dữ liệu và kho vector giữ nguyên. | **Hoàn tất** 22/09/2026 — sau khi dựng lại, hỏi tên miền `api` từ giao diện sáu lần liên tiếp đều ra đúng một địa chỉ của máy chủ thật, gọi qua giao diện đúng hai mươi trên hai mươi; bốn container của stack bot chạy, máy chủ bot trả lời ở cổng 8010, vòng kéo tin Telegram sống. Cụm bài kiểm của Agent Hub chạy lại trên container mới: **43 bài xanh**. Prod không dính vì stack bot chỉ chạy ở máy làm việc | `docker-compose.agent.yml` (chú thích đầu tệp ghi luôn lý do cấm đặt lại tên `api`) · `procurement-tool/docker-compose.override.yml` (vá tạm, không commit) |
| **ai-CR-009-ket-qua-tool-ra-telegram** | 2026-09-22 | Đại ca (*"nối mấy cái tool AI trong quản lý ra ngoài tái sử dụng nó trên bot tele được không em"*) | **Kết quả tool của Trợ lý AI phải tới tay đại ca trên Telegram, không phải chỉ dòng chữ.** Bot đã dùng lại nguyên bộ 49 tool qua `assistant_service.ask()` từ `ai-CR-006`, nhưng hai loại kết quả bị rơi: (1) tool **xuất báo cáo** trả `download_url` cần Bearer của web — trên Telegram bấm vào là 401; (2) tool **đề xuất sửa phiếu** trả `confirm_token` + form xác nhận chỉ web mới vẽ được. Vá ở `service.deliver_tool_results()` chạy sau câu trả lời: khối `file` → chốt sở hữu **y hệt** đường tải web (`created_by` = tài khoản bot **và** khóa nằm trong `assistant-report/`), rồi tải byte từ kho và `telegram.send_document()` (multipart, trần 50 MB của Bot API, bật «đang tải tệp lên»); gửi hỏng thì đưa link tải web. Khối `proposal` → thẻ *Đề xuất sửa* (trước → sau từng ô, chưa sửa, hạn 15 phút) kèm hai nút **Xác nhận sửa** / **Không sửa**; `callback_data` của Telegram chỉ chịu 64 byte nên **token Fernet nằm trong sổ** (`tab_agent_message` chiều ra, dấu `de_xuat`, thân = JSON khối proposal), nút chỉ mang id dòng sổ. Bấm xác nhận thì bot gọi **đúng `update_tool.confirm_update()` của web** — không mở đường ghi riêng, nên hết hạn / mất quyền / lệch trạng thái đều ăn câu lỗi của web; xong thì đóng dấu `da_sua` / `bo_sua`, bấm lần hai báo *"đã xử rồi"*. Khối `draft` (biểu mẫu nháp) chỉ thông báo và đưa link web vì form không dựng được trong chat. Bốn dấu mới (`tep` · `de_xuat` · `da_sua` · `bo_sua`) là tin chiều ra, **không lọt** vào mạch hội thoại `_recent_turns`. | Không đụng bảng, không migration. Mọi tệp/đề xuất gắn với tài khoản `AGENT_ASSISTANT_USER` (local đang là `admin`) — đổi tài khoản là đổi luôn ai được xác nhận. `telegram._call()` nhận `files=` (multipart) — đường JSON cũ giữ nguyên. | **Hoàn tất** 22/09/2026 — `test_agent_hub.py` 28 lên **35 bài** (gửi tệp đính kèm; tệp của người khác không gửi; thẻ đề xuất hai nút + sổ giữ token + nút ≤ 64 byte; xác nhận đi đúng token và không bấm lại được; Không sửa thì không ghi; hết hạn báo lý do và đóng thẻ; `sendDocument` đi multipart và chặn tệp quá nặng), tổng cụm 43 xanh. Đã restart `agent-poller` + `celery-worker` stack `agenthub`. Prod chưa (tạm dừng deploy) | `backend/app/modules/agent_hub/service.py` · `telegram.py` · `constants.py` · `test/backend/test_agent_hub.py` · `doc/agent-hub/01-thiet-ke-ky-thuat.md` §9 |
| **ai-CR-008-bot-cham-poller-rieng** | 2026-09-22 | Đại ca (*"nó trả lời lâu dữ lắm, nó bị chậm em nhé... khi clear mọi thứ oke, rồi mình làm luôn"*) | **Bot trả lời chậm và không có dấu hiệu gì đang làm.** Đo trên sổ `tab_agent_run`/`tab_agent_message`: mỗi câu mất 4-7 giây phía máy chủ (phân loại ý định ~3 giây + Trợ lý AI chạy tool ~4 giây) **cộng 0-10 giây chờ vòng beat thấy tin**, suốt thời gian đó Telegram im lìm; ca tệ nhất 2,5 phút khi tin rơi nhầm vào INBOX (đã vá ở `ai-CR-007`). Ba việc: (1) **Tiến trình `agent-poller` riêng** (`backend/app/modules/agent_hub/poller.py`, service mới trong `docker-compose.agent.yml`) giữ kết nối Telegram **25 giây** (`telegram.LONG_POLL_TIMEOUT`, `fetch_updates(offset, timeout=)`, trần HTTP = timeout + 20) — tin tới là thấy ngay; hỏng thì rollback, nghỉ 3 giây, kéo tiếp, không bao giờ tự chết; `SIGTERM` thì xong lượt đang kéo mới dừng (`stop_grace_period: 30s`). Cờ mới **`AGENT_LONG_POLL`** (mặc định `false`) đặt ngay trong `environment` của celery-beat + celery-worker ở tệp compose agent: **true** thì `celery_app.py` bỏ `agent-poll-telegram` khỏi lịch **và** `poll_telegram_task` tự trả rỗng (chặn hai lớp — lịch cũ còn trong tệp PersistentScheduler vẫn có thể bắn). Hai bên cùng đọc một con trỏ là xử trùng một tin. (2) **"Đang soạn tin..."** — `telegram.send_chat_action()` (`sendChatAction`, nuốt lỗi) gọi ngay sau khi chốt tin vào trong `handle_message` và gọi lại trong `answer_question` trước `ask()` (dấu chỉ sống ~5 giây, trạm phân loại đã ăn 2-3 giây). Chat lạ vẫn im hẳn. (3) **Gemini 429 thử lại một lần** — `gemini._post` đọc `retryDelay` trong thân lỗi, chờ rồi gọi lại đúng một lần **chỉ khi** Google bảo chờ ≤ `RETRY_429_MAX_WAIT` 10 giây; hạn mức theo phút của gói miễn phí (bảo chờ 20-50 giây) thì báo lỗi ngay như cũ. Áp cho cả Trợ lý AI web. | Thêm service `agent-poller` (512m / 0.5 cpu), cờ `AGENT_LONG_POLL`, `.env.example`. Không đụng bảng. Chưa gộp trạm phân loại vào lượt gọi Trợ lý AI (tiết kiệm thêm ~3 giây nhưng cần allowlist tool riêng cho bot) — để CR sau nếu đại ca còn thấy chậm. Stack `docker-compose.yml` thuần (không có tệp agent) vẫn chạy vòng beat 10 giây như cũ. | **Hoàn tất** 22/09/2026 — `test_agent_hub.py` 22 lên **28 bài** (báo đang soạn trước khi hỏi model; chat lạ không báo; `fetch_updates` giữ kết nối và trần HTTP lớn hơn; poller không chết khi một lượt hỏng; cầu dao tắt thì ngủ; việc beat nhường khi có poller), thêm `test_assistant_gemini_429.py` **4 bài**; stack `agenthub` đã dựng `agent-poller` + recreate beat/worker, log poller *"giữ kết nối 25s/lượt, cầu dao BẬT"*, beat chỉ còn bắn `agent-triage-inbox`. Prod chưa (đang tạm dừng deploy) | `backend/app/modules/agent_hub/poller.py` · `telegram.py` · `service.py` · `tasks.py` · `core/celery_app.py` · `core/config.py` · `assistant/provider/gemini.py` · `docker-compose.agent.yml` · `.env.example` · `doc/agent-hub/01-thiet-ke-ky-thuat.md` §2 đính chính + §9 + §10 |
| **ai-CR-007-noi-mach-hoi-thoai** | 2026-09-22 | Đại ca (ảnh chụp: Trợ lý hỏi *"nghỉ ngày nào, lý do gì?"*, đại ca đáp *"cho anh nghỉ vào thứ 6 tuần này, lý do là đi du lịch"*, bot đẻ ra việc **AI-0004 "Xử lý yêu cầu xin nghỉ phép"** kèm 4 câu hỏi; *"ok làm bước 1 vá nối mạch đi em"*) | **Câu trả lời cho câu bot vừa hỏi không được đem đi phân loại lại.** Gốc: trạm phân loại (`ai-CR-003`) chỉ nhìn MỘT câu trơ trọi, và hỏng (Gemini 429) thì rơi về giao việc — nên một câu đáp ngắn thành một đầu việc sửa mã. Ba chốt: (1) **Nối thẳng** — tin hội thoại gần nhất của chat là câu trả lời của Trợ lý AI có dấu hỏi, trong `FOLLOW_UP_WINDOW` 10 phút (so bằng `created_at` của hai tin, đồng hồ DB) thì tin mới đi thẳng Trợ lý AI, không tốn lượt phân loại, không phụ thuộc hạn mức. Dấu hỏi là chốt hẹp cố ý: bot trả lời xuôi rồi đại ca báo lỗi màn hình thì đó là việc mới. (2) **Phân loại có mạch** — `run_intent(text, context=)` nhận lượt hỏi-đáp gần nhất (2 lượt, 600 ký tự mỗi lượt); câu nhắc viết lại: `hoi` = mọi thứ Trợ lý AI làm được (tra cứu **và nhờ làm nghiệp vụ**: tạo đơn nghỉ phép, lập báo cáo, duyệt; **và** đang trả lời bot), `viec` = chỉ nhờ **sửa phần mềm**. (3) **Phân loại hỏng thì hỏi lại kèm hai nút** *Làm luôn* / *Ghi thành việc sửa mã*, không còn âm thầm xếp vào INBOX. | `manager.run_intent` thêm `context=`; `service` thêm `_is_follow_up` · `_intent_context` · `_ask_intent_choice`. Không đụng bảng. AI-0004 đang mở trong sổ local, đại ca bấm *Bỏ việc này* hoặc để đó. | **Hoàn tất** 22/09/2026 — 22 bài `test_agent_hub.py` xanh (3 mới: nối thẳng khi bot vừa hỏi; quá 10 phút thì phân loại lại; trả lời xuôi thì phân loại nhưng có mạch; 1 bài sửa: hỏng thì hỏi lại). Đã restart worker `agenthub` | `backend/app/modules/agent_hub/service.py` · `manager.py` · `test/backend/test_agent_hub.py` · `doc/agent-hub/01-thiet-ke-ky-thuat.md` §9 |
| **ai-CR-006-telegram-nhu-tro-ly-ai** | 2026-09-22 | Đại ca (*"tôi hỏi có 1 câu nhưng nó trả lại 4 câu... nó còn không có dạng 1. 2. hay bản như trợ lý, em có thể tham khảo con trợ lý để có bản in đậm nhạt chứ hay nghiên... trước mắt nó sẽ là như con trợ lý trên web erp hiện tại vậy á, nhưng ở ngoài tele thôi"*) | **Nhánh hỏi đáp phải là đúng Trợ lý AI trên web, chỉ khác chỗ đứng là Telegram.** Ba việc: (1) **Định dạng** — thêm `telegram.md_to_html()` đổi Markdown của Trợ lý sang HTML Telegram (đậm · nghiêng · mã · link · gạch đầu dòng `•` · tiêu đề thành đậm · bảng thành từng dòng, ô cách nhau ` · `); chữ thường thoát HTML hết, chỉ thẻ do hàm sinh mới đi qua. Telegram không có phông chữ, bảng kẻ ô hay màu — trần nền tảng. (2) **Mạch hội thoại** — `_recent_turns()` lấy các cặp hỏi/đáp gần đây của chat (dấu `hoi` / dấu mới `tra_loi`, tối đa 8 lượt, 2 giờ tính theo đồng hồ DB, trần 6000 ký tự) đưa vào `ask(history=...)`; sổ giữ Markdown gốc. (3) **Link thật** — `absolute_url()` nối `FRONTEND_URL` vào đường dẫn tương đối; `.env` agent khai `FRONTEND_URL=http://localhost:8083`. Kèm chốt chống trả lời lặp: `db.commit()` ngay sau khi ghi tin vào và trước khi giao cho Trợ lý AI, để rollback của tool (`bao-CR-463`) không kéo con trỏ đi. | `reply()` thêm `markdown=` + `action=`; `answer_question()` thêm `before_id=`; hằng `ACT_ANSWER`. Không đụng bảng. Chưa có: đăng nhập từng người (`ai-CR-004`), đính kèm tệp/ảnh, model trả phí (local đang Gemini miễn phí nên gọi dồn là 429). | **Hoàn tất** 22/09/2026 — 19 bài `test_agent_hub.py` xanh (4 bài mới: đổi Markdown, mạch chỉ lấy hỏi/đáp, sổ giữ Markdown còn dây gửi HTML, con trỏ không trôi khi tool rollback). Đã restart worker stack `agenthub` | `backend/app/modules/agent_hub/telegram.py` · `service.py` · `constants.py` · `doc/agent-hub/01-thiet-ke-ky-thuat.md` §9 |
| **ai-CR-005-gom-xong-ra-thang-ke-hoach** | 2026-09-22 | Đại ca (*"nè lập kế hoạch là sao nữa, ví dụ như phải linh động như trợ lý AI vậy anh, nếu cần xác nhận thì gửi để tôi xác nhận lại chứ rồi AI-0003 cái này chi nữa, bỏ nó đi, ngta quan tâm thông tin thôi"*) | **Bỏ nút "Lập kế hoạch".** Gom xong là bot chạy thẳng trạm PLAN và gửi **một** thẻ duy nhất: phương án + tệp sẽ đụng + kiểm thử + rủi ro, kèm ba nút *Duyệt* / *Sửa lại* / *Bỏ*. Tấm thẻ trung gian cũ chỉ nhai lại lời đại ca vừa nhắn rồi bắt bấm thêm một nút mới ra được nội dung — hai tiếng chuông cho một việc, không thêm thông tin nào. Cảnh báo **rủi ro CAO** và dòng *Gom từ n tin nhắn* dời sang thẻ kế hoạch để không mất. Hợp `QĐ-AI-5`: chặng bắt buộc dừng là **PLAN → CODE**, không phải TRIAGE → PLAN. | Mỗi việc nay tốn 2 lượt Gemini (gom + lập kế hoạch) ngay lúc gom thay vì chờ đại ca bấm; trần `AGENT_DAILY_TASK_CAP` giữ nguyên nên chi phí vẫn có nóc. Xóa `send_task_card`, giữ nhánh nút `plan:` để mấy thẻ cũ còn trong Telegram bấm vẫn chạy. Không đụng bảng. | **Hoàn tất** 22/09/2026 — 15 bài kiểm `test_agent_hub.py` xanh | `backend/app/modules/agent_hub/service.py` · `test/backend/test_agent_hub.py` |
| **ai-CR-004-telegram-tu-dang-nhap** | 2026-09-22 | Đại ca (*"sau này mình cho họ tự đăng nhập trên tele của họ, ghi nhớ lại trên đó, nếu họ muốn đổi tài khoản thì phải có lệnh đổi sau"*) | **Mỗi người tự đăng nhập tài khoản ERP của mình ngay trong Telegram, bot nhớ theo người chat.** Bỏ dần cách khai cứng MỘT tài khoản ở `AGENT_ASSISTANT_USER` (`QĐ-AI-10`): người mới nhắn thì bot mời đăng nhập, đăng nhập xong bot gắn `chat_id` ↔ tài khoản ERP và nhớ luôn, các câu sau chạy đúng phạm vi dữ liệu của chính người đó. Có **lệnh đổi tài khoản** để đăng xuất rồi đăng nhập người khác trên cùng máy điện thoại. Hướng làm: thêm bảng nối `chat_id` ↔ `user_id` (hoặc mở rộng `tab_agent_cursor`), phát mã đăng nhập một lần thay vì gõ mật khẩu trong khung chat — Telegram lưu lịch sử chat vĩnh viễn nên **không hỏi mật khẩu thẳng**, và phải có hạn phiên. | Thêm một bảng nối + nhánh đăng nhập/đăng xuất trong `service.handle_message`. Thay đổi gốc rễ luật phạm vi dữ liệu của `/hoi`, nên phải làm cùng lúc với việc siết `AGENT_TELEGRAM_CHAT_ID` (nhiều người dùng thì hàng rào một `chat_id` không còn đủ). | **Đề xuất** — đại ca chốt hướng "để sau", chưa làm ở bậc 1 | `backend/app/modules/agent_hub/service.py` |
| **ai-CR-003-bo-tien-to-lenh** | 2026-09-22 | Đại ca (*"lệnh theo kiểu này thì người dùng khó chịu lắm"*) | **Bỏ bắt buộc gõ tiền tố `/hoi` · `/ds` · `/gom`.** Hiện bot rẽ nhánh bằng chữ đầu dòng: có `/hoi` mới là hỏi, chữ thường là giao việc. Người dùng không muốn nhớ lệnh. Hướng đề xuất: mỗi tin chữ thường đi qua một lượt Gemini phân loại ý định (HỎI / GIAO VIỆC / XEM DANH SÁCH / GOM NGAY) rồi mới rẽ; hỏi thì trả lời tại chỗ, giao việc thì vào INBOX như cũ. Giữ các lệnh `/` làm đường tắt cho ai quen. Chỗ mập mờ thì hỏi lại một câu kèm hai nút thay vì đoán. | Sửa `service.handle_message` + thêm một prompt phân loại trong `manager.py`; tốn thêm một lượt gọi Gemini Flash mỗi tin. Không đụng bảng. | **Hoàn tất** 22/09/2026 — chạy thật trên stack `agenthub`. Cách làm: mọi tin **không** bắt đầu bằng `/` đi qua trạm mới `STAGE_INTENT` (`manager.run_intent`, Gemini Flash, `temperature=0`, 256 token) trả về `hoi` / `viec` / `mo_ho`; `hoi` thì trả lời tại chỗ rồi **đóng dấu `action="hoi"`**, `viec` thì để nguyên dấu trống cho vòng gom nhặt, `mo_ho` thì đóng dấu `cho_y` và hỏi lại kèm hai nút *Trả lời luôn* / *Ghi thành việc*. Tin bắt đầu bằng `/` đóng dấu `lenh` và **không tốn lượt gọi model**. Phân loại hỏng thì coi như giao việc (tin nằm lại sổ, không bốc hơi). Đóng dấu `action` chính là chỗ vá lỗi cũ: `/start` và câu hỏi vừa trả lời từng bị vòng gom đẻ thành AI-0001/AI-0003. 5 bài kiểm mới | `backend/app/modules/agent_hub/service.py` · `manager.py` · `constants.py` · `test/backend/test_agent_hub.py` |
| **ai-CR-002-dung-bac-1-quan-ly** | 2026-09-18 | Trợ lý (đề xuất) | **Dựng bậc 1 của Agent Hub.** Phạm vi đã co lại theo `QĐ-AI-7`: **chỉ nguồn Telegram, không dựng màn hình**. Gồm: **năm** bảng sổ `tab_agent_task` · `tab_agent_task_item` · `tab_agent_run` · `tab_agent_message` · `tab_agent_cursor` (xem `QĐ-AI-8`); cầu nối Telegram **kéo tin mỗi 10 giây** (không long-polling — `celery-worker` chạy `-c 1`, giữ một kết nối treo là chặn hết mọi việc nền khác) kèm nút Duyệt/Sửa/Bỏ và khóa theo `chat_id`; bot quản lý Gemini chạy hai trạm TRIAGE (gom tin cùng loại + tóm tắt) và PLAN (đề xuất cách sửa + `plan_files`); index kho tài liệu dự án vào Qdrant (collection riêng `agent_docs`, xem `QĐ-AI-9`) làm trí nhớ; thêm nhánh `/hoi` gọi thẳng Trợ lý AI có sẵn (`QĐ-AI-10`). **Đầu ra bậc 1 chỉ là in bản PLAN ra Telegram và ghi vào sổ — chưa gọi `claude -p`, chưa đụng một dòng mã nào của hệ thống.** | Thêm 1 module backend + 5 bảng + 1 bộ mã trạng thái. **Không đụng** phân hệ nghiệp vụ nào đang chạy. Cầu dao `AGENT_HUB_ENABLED` mặc định tắt. | **Đang làm** — đại ca duyệt thiết kế 18/09/2026. **22/09/2026:** trả nợ migration `8023c5f8bee4_agent_hub` (autogenerate rồi cắt tay còn đúng 5 bảng `tab_agent_*`, bỏ hết lệch chỉ mục/NOT NULL nhặt được từ migration cũ); dựng stack thử `agenthub` ở worktree `procurement-agent-hub` **ghép vào stack local đang chạy** (`docker-compose.agent.yml`: dùng chung MySQL qua database riêng `dego-agent` nhân bản từ `dego-erp`, Redis ngăn 1, Qdrant collection riêng; chỉ dựng mới api 8010 + worker + beat — service này đổi tên thành `agent-api` tối 22/09 vì trùng bí danh mạng `api` với API thật của stack local). 9 bài kiểm xanh. Đại ca đã điền token/khóa và bật cầu dao: cầu nối Telegram kéo tin đúng nhịp 10 giây, nạp kho `agent_docs` xong **47 tệp / 4064 đoạn**. Bật `/hoi` bằng `AGENT_ASSISTANT_USER=admin` — **tạm thời, chỉ dưới máy local**, đúng cảnh `QĐ-AI-10` dặn đừng khai tài khoản quản trị; đường đi thật là để mỗi người tự đăng nhập, xem `ai-CR-004`. Chạy thử `/hoi` thì lòi ra lỗi của Trợ lý AI khi chạy bằng Gemini, vá ở **`bao-CR-462-gemini-enum-so`** (ghi sổ nghiệp vụ, đây chỉ là dòng đối chiếu) | `doc/agent-hub/01-thiet-ke-ky-thuat.md` §10 bậc 1 |
| **ai-CR-001-thiet-ke-agent-hub** | 2026-09-18 | Đại ca (*"tôi định làm 1 hệ thống... khi tôi có ticket thì tôi cần 1 chổ nào đó có thể thông qua 1 AI kiểu như quản lý, nhận thông tin đó, tóm gọn lại dựa trên memory hoặc là thông tin từ hệ thống, sau đó nhắn qua tele cho tôi..."*, sau đó *"vậy thì phải có 1 bộ quy tắt đúng không, em có thể ghi nhận thiết kế đi em, sổ CR thì em tách ra 1 sổ nữa riêng cho phần AI này em nhé"*) | **Viết bản thiết kế kỹ thuật + bộ quy tắc cho Agent Hub, và tách sổ CR riêng cho mảng AI.** Ba tệp mới: bản thiết kế (kiến trúc, bảy trạm, mô hình dữ liệu, cấu hình, lộ trình bốn bậc, bảng rủi ro), bộ quy tắc cho bot (ba luật trùm, luật cho từng con bot, điều kiện dừng và leo thang, danh sách cấm cứng), và chính tệp sổ này. Chốt được sáu quyết định `QĐ-AI-1`…`QĐ-AI-6`. Rà lại hạ tầng có sẵn và xác nhận **phần lớn việc là ráp chứ không phải xây**: provider Gemini, Qdrant/RAG, `tab_ticket`, Celery, sổ `tab_sync_log` đều đã chạy. | Chỉ tài liệu. Không một dòng mã nào. | **Hoàn tất** | `doc/agent-hub/README.md` · `01-thiet-ke-ky-thuat.md` · `02-bo-quy-tac-bot.md` |
