# DIỄN ĐÀN — LỘ TRÌNH PHASE

Ngày lập: 26/08/2026. Chia việc của [`00`](./00-pham-vi-va-ke-hoach.md) mục 5 thành các phase làm được. **Không có mốc thời gian** (QĐ-D5 — làm từ từ, song song việc khác); thứ tự và điều kiện chuyển phase mới là thứ quan trọng. Thiết kế chi tiết từng phần ở [`01`](./01-giao-dien-va-logic-hien-thi.md).

Nguyên tắc chia: **mỗi phase kết thúc là hệ vẫn nguyên vẹn** — dừng giữa chừng bao lâu cũng được, không để bảng mồ côi hay màn hình gọi API chưa tồn tại.

---

## Sơ đồ phụ thuộc

```
F0 nền dữ liệu ──> F1 API bài viết ──> F2 khung FE + feed đọc ──> F3 đăng bài + trang cá nhân
                                                                        │
                                              F4 tương tác (like+comment+chuông) <──┘
                                                                        │
                                                    F5 quản trị ẩn/xóa ─┘──> F6 deploy chạy thử
                                                                                  │
                                     (đợt 2) F7 Help Center tab · F8 duyệt bài · F9 ghim + tìm kiếm
```

F0–F1 là backend thuần, F2–F3 là FE thuần — **hai người làm song song được** từ khi F1 chốt hợp đồng API (viết schema Pydantic xong là FE dựng theo được, không cần chờ service xong).

---

## Đợt 1 — MVP

### F0 — Nền dữ liệu và quyền — **XONG 27/08/2026**

- Bảng `tab_forum_post` (audience + dept/company đóng băng, `status` SMALLINT + IntEnum chừa `pending_review` theo QĐ-D2), `tab_forum_reaction`, `tab_forum_moderation_log`. Migration + khai `all_models.py`.
- Entity `forum_post` vào `ENTITIES` + `SCOPE_FIELDS` (sentinel PUBLIC — quyền xem đi theo luật audience riêng, không theo scope RBAC) + `FILE_POLICY` (chỉ đuôi ảnh, 10MB, 10 tấm).
- Vai trò `forum_admin` trong seed (khuôn `help_admin`); quyền không ghi đè trên prod theo luật seed_prod.
- **Điều kiện đủ:** `alembic heads` một đầu; test backend cho enum trạng thái + ràng buộc unique like + FILE_POLICY.

### F1 — API bài viết — **XONG 27/08/2026**

- CRUD bài: đăng (kèm `file_ids` khuôn tải-trước-gắn-sau), xóa bài của mình, đọc một bài, **feed con trỏ `before_id` + WHERE audience** (mục 4.2–4.3 của `01`), feed trang cá nhân.
- Bộ query gom theo trang: tác giả, số like, đã-like-chưa, số comment, ảnh — mỗi loại 1 query/trang.
- Mở comment: thêm `forum_post` vào `comment_registry` + nhánh `resolve_doc()` kiểm theo audience (chỗ duy nhất phải viết thêm của phần comment).
- **Điều kiện đủ:** test backend — người phòng khác không thấy bài `dept`, người công ty khác không thấy bài `company`, tác giả luôn thấy bài mình, con trỏ không lặp/không sót khi có bài chen giữa, comment vào bài không được xem bị 403.

#### Hợp đồng API F1 (chốt 27/08/2026 — FE dựng F2 theo đây, không chờ service)

Mọi endpoint đều đòi **đăng nhập** (Bearer), KHÔNG đòi grant RBAC — ai thấy bài nào đi theo
luật audience mục 4.2 của `01`. Phong bì chuẩn `{success, message, data}`.

| Endpoint | Vào | Ra (`data`) |
|---|---|---|
| `GET /api/forum/posts?limit=20&before_id=0` | con trỏ `before_id` = id nhỏ nhất đang hiện (0 = trang đầu) | `{items: [PostOut...] mới → cũ, next_before_id, has_more}` |
| `GET /api/forum/users/{user_id}/posts?limit&before_id` | feed trang cá nhân; xem trang CHÍNH MÌNH thấy cả bài bị ẩn | như trên |
| `GET /api/forum/posts/{id}` | | `PostOut` — 403 nếu ngoài đối tượng xem |
| `POST /api/forum/posts` | `{body, body_format, audience, file_ids}` — `audience` 1=phòng ban 2=công ty 3=toàn tập đoàn; `body_format` 0=chữ thuần 1=HTML rich (CR-261, server sanitize ngay cửa ghi); ảnh tải trước qua `POST /api/attachments/upload-file` (entity=`forum_post`) lấy `file_ids` | `PostOut` (201) |
| `DELETE /api/forum/posts/{id}` | chỉ tác giả (admin ẩn/xóa là F5, đường khác) | `null` |
| `POST /api/forum/posts/{id}/like` | bấm thích/bỏ thích | `{liked, count}` |
| `GET /api/forum/posts/{id}/likes` | ai đã thích | `[{user_id, name}]` |

`PostOut` = `{id, body, body_format, status, audience, dept_id, company_id, author_id, author_name,
author_code, author_avatar, created_at, can_delete, like_count, liked, comment_count,
images: [{link_id, file_id, filename, url, content_type, size}]}`.

Ràng buộc tầng service: bài chữ thuần (`body_format=0`, dữ liệu cũ) tối đa **10.000 ký tự**;
bài rich (`body_format=1`, CR-261) tối đa **40.000 ký tự TÍNH CẢ MARKUP** (xấp xỉ 10.000 chữ
nhìn thấy) — đo TRƯỚC khi sanitize để chặn HTML khổng lồ từ ngoài cửa; tối đa **10 tệp media/bài** — ảnh +
video `mp4`/`webm`, mỗi tệp **50MB** theo `FILE_POLICY` (D-Q3 chốt 27/08/2026; bản F1 gốc
là "chỉ ảnh, 10MB"), bài rỗng (không chữ không ảnh) bị 400; chọn `audience` phòng ban/công ty
mà hồ sơ nhân sự chưa gắn phòng/pháp nhân cũng 400 — không đoán, bắt sửa dữ liệu nhân sự.
Comment dùng nguyên bộ `/api/comments` với `entity=forum_post`.

### F2 — Khung FE + feed đọc — **XONG 27/08/2026**

- Dựng **phân hệ `forum` trong `frontend-v2`** (QĐ-D6 — không còn app riêng): thêm dòng vào `module-registry`, route gốc `/forum`, **layout riêng một cột** (mục 1 của `01`), lazy-load, tab shell + responsive điện thoại trong shell ERP.
- Feed đọc: `useInfiniteQuery` cuộn vô hạn, thẻ bài (ảnh lưới + đèn chiếu + "Xem thêm"), nút "Có bài viết mới", trang `/forum/posts/:id`.
- **Điều kiện đủ:** đăng nhập bằng tài khoản thật ở local (cổng 8083), lướt được feed dữ liệu mẫu cả desktop lẫn devtools mobile; `npm run check` xanh.

### F3 — Đăng bài + trang cá nhân — **XONG 27/08/2026**

- Hộp thoại đăng bài: chữ + ảnh (kéo thả/dán, tải-trước-gắn-sau) + chọn đối tượng xem (nhớ lựa chọn trước); xóa bài của mình.
- Trang cá nhân `/forum/me`, `/forum/users/:id` — đầu trang hồ sơ + feed lọc tác giả.
- **Điều kiện đủ:** đăng bài 10 ảnh từ điện thoại (qua devtools mobile) trơn tru; bài `dept` không hiện với tài khoản phòng khác trên UI.

### F4 — Tương tác — **XONG 27/08/2026**

- Like bài (không chuông), khối comment nhúng (component mới trên FE diễn đàn, gọi bộ API `/api/comments` sẵn có), chuông "bình luận mới trong bài của bạn" + web push.
- **Điều kiện đủ:** hai tài khoản comment qua lại nhận chuông đúng luật (không tự báo mình, nhắc tên chỉ một chuông).

### F5 — Quản trị — **XONG 27/08/2026**

- `forum_admin` ẩn/xóa bài: **bắt buộc nhập lý do**, ghi `tab_forum_moderation_log`, bắn chuông cho tác giả kèm lý do (QĐ-D1). Khôi phục bài ẩn. Tác giả xem bài ẩn của mình kèm nhãn lý do.
- **Điều kiện đủ:** test — ẩn không lý do bị 400; tác giả nhận đúng một chuông; bài ẩn biến khỏi feed mọi người nhưng còn ở trang cá nhân tác giả và mắt admin.

### F6 — Deploy chạy thử

- **Không còn hạ tầng mới** (QĐ-D6 — hết Dockerfile/nginx/domain/tunnel riêng, D-Q1 giải thể): diễn đàn lên theo nhịp deploy ERP dev sẵn có (build lại ảnh + migration). Bật trước cho **một nhóm nhỏ** (IT + HR) dùng thử một thời gian rồi mới thông báo toàn công ty; lên prod thì đi cùng đợt bật service `erp` trên prod.
- **Điều kiện đủ:** nhóm thử đăng bài thật từ điện thoại qua domain ERP; backup R2 đã phủ bảng mới.

---

## Đợt 2 — sau khi MVP sống

| Phase | Nội dung | Ghi chú |
|---|---|---|
| F7 | Help Center thành tab **Hướng dẫn** (QĐ-D4) | Đọc chung API `help_article`; tab nay nằm trong ERP (QĐ-D6) — đích redirect của domain help cũ chốt khi làm F7 |
| F8 | Bật cơ chế **duyệt bài** khi sếp cần (QĐ-D2) | Chỉ là config + màn hàng chờ — dữ liệu đã chừa sẵn từ F0 |
| F9 | ~~Ghim bài~~ · ~~tìm kiếm bài viết~~ · **sửa bài** kèm nhãn "đã chỉnh sửa" | Phần **ghim bài** kéo lên làm sớm 27/08/2026 (F9a/CR-199); phần **tìm kiếm** xong 03/09/2026 trong CR-263 (tab Quản trị + `/forum/search` cho mọi người — nhật ký bên dưới); F9 chỉ còn sửa bài |
| F9a — làm sớm (CR-199) | **Ghim bài + tab «Thông báo»**: `forum_admin` ghim/bỏ ghim bài bất kỳ; bài ghim hiện dải riêng đầu Bảng tin + tab «Thông báo» liệt kê mọi bài đang ghim (thông báo công ty kiểu nghỉ lễ 2/9 nhìn một chỗ) | Cột `pinned_at` DATETIME NULL trên `tab_forum_post` (ghim = có mốc, sắp mới→cũ); quyền đi theo `forum_post.write` sẵn có của F5, không thêm quyền mới |
| F12 (CR-200) — **XONG 27/08/2026** | **Clone bài thông báo từ Văn thư**: lúc **ban hành văn bản** bên phân hệ Văn thư, người ban hành chọn «Đăng thông báo lên diễn đàn» → hệ tạo một bài diễn đàn ĐÃ GHIM (tiêu đề + tóm tắt + link về văn bản), diễn đàn chỉ giữ bản sao thông báo — văn bản gốc vẫn ở Văn thư (chốt với sếp 27/08/2026: «clone thành 1 bài viết bên này là đúng logic hơn») | Móc thực tế nằm ở `document/issue_notification.create_forum_announcement` + `controller.approve_document` (không phải `issue_service.py` như dự kiến) — ghép lỏng, Văn thư là nguồn sự thật; đối chiếu dòng J12 ở `doc/erp/van-thu/01-danh-sach-tinh-nang.md` |
| F11 | **Cho người đăng tự chọn bố cục ảnh** kiểu Facebook/Instagram (sếp gợi ý 27/08/2026 — mới là ý tưởng, CHƯA chốt làm) | Hiện lưới tự xếp theo số ảnh (1 nguyên khổ · 2 chia đôi · 3 = 1 ngang + 2 vuông · 4+ ô 2x2 đè «+N») — đã là khuôn Facebook. Muốn cho tự chọn thì thêm cột `layout` SMALLINT+IntEnum trên bài + ô chọn bố cục trong hộp đăng bài + nhánh vẽ theo `layout` ở `post-image-grid.tsx`; dữ liệu cũ `layout=0` = tự xếp như nay |
| F10 — **XONG 27/08/2026** (làm sớm theo lệnh sếp, trước F4) | **Bài tự động theo sự kiện**: đổi avatar (cả hai cửa — thẻ danh tính `/me` VÀ menu tài khoản mọi màn hình) → hộp thoại "Đăng lên diễn đàn?" kèm caption + đối tượng xem (nhớ lựa chọn trước), "Để sau" là thôi — hỏi chứ không ép. Bài đăng mang `kind=AVATAR_UPDATE`, thẻ bài vẽ dòng hệ thống "đã cập nhật ảnh đại diện" cạnh tên | Cột `kind` SMALLINT+IntEnum (0=thường, 1=avatar), migration `7889a09c627e`; service ép AVATAR_UPDATE đúng 1 ảnh + caption được phép trống; FE: `avatar-post-dialog.tsx` (tải LẠI tệp avatar lên kho đính kèm diễn đàn — hai kho khác nhau, không mượn chéo); khuôn mở rộng sau cho thăng chức/sinh nhật/nhân sự mới = thêm giá trị enum |

| F13a (QĐ-D7) — **XONG 03/09/2026** (CR-269, migration `e7b3c9d41f28`, test `test_forum_board.py` — 17 bài tính cả bài bổ sung ở F13b; con số "15" ghi lần đầu là đếm sót) | **Backend chuyên mục kiểu VOZ**: bảng `tab_forum_board` (`parent_id` NULL = nhóm chỉ làm tiêu đề, con = box; `name`, `description`, `icon` VARCHAR(50) — tên icon hoặc 1 emoji do admin chọn lúc tạo box (bổ sung 03/09 theo VOZ), `sort_order`, `status` SMALLINT+IntEnum active/hidden, `audience` SMALLINT — đợt đầu ép PUBLIC theo QĐ-D7a); 3 cột mới trên `tab_forum_post`: `board_id` FK NULL (NULL = bài Bảng tin thuần), `title` VARCHAR(255) NULL (**bắt buộc khi có `board_id`**, bài feed không có tiêu đề như cũ), `prefix` SMALLINT+IntEnum (0 không · 1 thảo luận · 2 thắc mắc · 3 kiến thức · 4 khoe · 5 đánh giá — giá trị ở `ForumPrefix` (model), nhãn khai `core/forum_codes.py` vào sổ `status_catalog`, TS sinh bằng `gen_status_ts.py`). API: `GET /api/forum/boards` (cây nhóm→box; mỗi box kèm **bộ đếm** số thread + tổng bình luận và **khối bài-mới-nhất**: id + tiêu đề + prefix + thời gian hoạt động cuối + tên/avatar người viết cuối — COUNT trực tiếp, chưa denormalize) · `GET /api/forum/boards/{id}/threads` (**phân trang số trang** `page/per_page`, sắp theo hoạt động cuối = max(bài, comment cuối); KHÔNG dùng cursor `before_id` của feed) · CRUD box cho `forum_admin` · `POST /posts` nhận `board_id+title+prefix` (box ẩn/không tồn tại 400; có `board_id` mà thiếu title 400; audience ép theo box). Bài box vẫn ra feed (QĐ-D7b — query feed không đổi vì audience public); ghim trong box = `pinned_at` sẵn có lọc theo `board_id`. **Nguyên tắc kiểm duyệt (chốt 03/09/2026):** cấu trúc nhóm/box = `forum_admin` quyết thủ công; bài trong box đăng là hiện ngay KHÔNG duyệt trước, sắp tự động theo hoạt động cuối; admin chỉ ghim + gỡ hậu kiểm (cơ chế F5 sẵn có, lý do bắt buộc); duyệt trước theo box = cờ `require_approval` để dành cho F8, chưa làm đợt này. Entity `forum_board` vào ENTITIES + SCOPE_FIELDS (PUBLIC) — test B-07 tăng số đếm | **Điều kiện đủ:** `alembic heads` một đầu; test — box ẩn không nhận bài, thiếu title bị 400, thread list phân trang không lặp/sót, bài box hiện cả trong feed lẫn thread list, số đếm box khớp |
| F13b (QĐ-D7) — **XONG 03/09/2026** (CR-269, chi tiết ở nhật ký) | **FE tab «Diễn đàn»**: tab mới trong `forum-layout` (Bảng tin · **Diễn đàn** · Thông báo); 2 màn mới `/forum/boards` (danh sách box gom theo nhóm: icon box + tên + mô tả, dòng đếm gọn "X chủ đề · Y bình luận" — KHÔNG format kiểu 1K+ của VOZ, và dòng bài-mới-nhất bấm được: avatar + tên người viết cuối + tiêu đề + thời gian; mobile 375px giấu dòng bài-mới-nhất, giữ số đếm) và `/forum/boards/:id` (danh sách thread: chip prefix màu + tiêu đề + tác giả + số comment + hoạt động cuối, thread ghim đứng đầu, phân trang số); màn thread = trang `/forum/posts/:id` sẵn có vẽ thêm tiêu đề + chip prefix + breadcrumb về box. Hộp đăng bài trong ngữ cảnh box: thêm ô tiêu đề + chọn prefix, ẨN ô đối tượng xem (theo box); trên Bảng tin composer giữ nguyên. Thẻ bài thuộc box trên feed: chip tên box + tiêu đề đậm trên đầu nội dung, bấm mở thread | **Điều kiện đủ:** duyệt thật local — tạo thread từ trong box, thấy nó ở cả box lẫn Bảng tin; mobile 375px không tràn; `npm run check` xanh |
| F13c (QĐ-D7) — **XONG 03/09/2026** (CR-271, chi tiết ở nhật ký) | **Sidebar phải cho tab «Diễn đàn»** (chốt 03/09/2026 theo màn hình VOZ): 3 khối bên phải màn `/forum/boards` — (1) **Nổi bật** = các bài đang ghim (`pinned_at` sẵn có, admin quyết); (2) **Đang sôi nổi** = top 5 thread AUTO theo số bình luận + reaction 7 ngày gần nhất, không ai chọn tay; (3) **Mới nhất** = N thread mới toàn diễn đàn. Chỉ hiện desktop màn rộng; mobile ẩn sidebar, dồn khối Nổi bật lên đầu trang. Kèm việc nhỏ: rà composer + ô bình luận, nếu chưa có **nút chọn emoji** thì thêm (picker emoji hệ thống, KHÔNG làm sticker — sếp chốt 03/09: "trước mắt cho có emoji là được, khỏi sticker") | Backend chỉ thêm 2 query đọc (trending + latest) trên dữ liệu sẵn có, không bảng mới, không migration; khối Nổi bật dùng API bài ghim sẵn có. Làm ngay sau F13b, khối lượng nhỏ |

Đợt 3 (follow/feed cá nhân hóa, nhóm kín, khảo sát trong bài) giữ nguyên như `00` mục 5 — chỉ bàn khi đợt 2 xong và sếp còn muốn. Riêng **video** vốn nằm đợt 3 đã kéo lên làm xong 27/08/2026 (D-Q3).

**Nợ đề xuất (chưa xếp lịch):** trợ lý AI đọc diễn đàn — 2 tool `forum_search`/`forum_post_read` ép quyền theo người hỏi + mở rộng search sang bình luận; KHÔNG nạp thô diễn đàn vào gói tri thức, tri thức đi đường chưng cất qua HDSD/Văn thư. Chi tiết + điều kiện đủ ở [`03-de-xuat-ai-doc-dien-dan.md`](./03-de-xuat-ai-doc-dien-dan.md) (ghi 03/09/2026).

---

## Việc bắt đầu được ngay, không chờ ai

1. F0 nguyên phần dữ liệu — không đụng câu hỏi mở nào.
2. Chốt hợp đồng API của F1 (schema vào/ra) để người làm FE khởi động F2 song song.
3. ~~Hỏi sếp câu **D-Q2 — ai làm forum_admin**~~ — **đã chốt 27/08/2026**: không có cơ chế riêng, `forum_admin` chính là VAI TRÒ RBAC seed sẵn từ F0; ai được gán vai trò đó qua màn phân quyền sẵn có thì là quản trị diễn đàn. (D-Q1 tên domain đã giải thể theo QĐ-D6.)

## Nhật ký

| Ngày | Nội dung |
|---|---|
| 26/08/2026 | Lập tài liệu — chia 6 phase đợt 1 + 3 phase đợt 2, điều kiện đủ từng phase |
| 27/08/2026 | **F0 xong**: module `backend/app/modules/forum/` (3 bảng + 3 IntEnum), migration `9eba2501f2c4` (một head), entity `forum_post` vào ENTITIES (45) + SCOPE_FIELDS (PUBLIC) + FILE_POLICY (`__self__`, chỉ ảnh, 10MB), vai trò `forum_admin` trong STD_ROLES (loại `forum_post` khỏi quyền tự động của `pur_manager`), test `test_forum.py` + cập nhật `test_pham_vi_khai_du_b07.py` 44 → 45 |
| 27/08/2026 | **Chốt hợp đồng API F1** (bảng ở trên) trước khi viết service — FE bắt đầu F2 song song được từ đây |
| 27/08/2026 | **F1 xong**: `forum/schema.py + service.py + controller.py` (prefix `/api/forum`, nối `main.py`), luật audience `can_view`/`_visible_cond` (thiếu phòng/pháp nhân thì CHẶN, khuôn B-07), 4 map gom theo trang (tác giả/like/số comment/ảnh), mở comment (`comment_registry` + nhánh `resolve_doc` kiểm audience), nhánh `_check_forum` + chặn gắn trực tiếp trong `attachment/controller.py`, xóa bài cuốn theo comment + like + ảnh. Test `test_forum_api.py` 13 bài phủ đủ 5 điều kiện đủ; bộ comment/đính kèm cũ 62 bài vẫn xanh |
| 27/08/2026 | **QĐ-D6 — gộp vào ERP v2**: F2 thành phân hệ `forum` trong `frontend-v2` (route gốc `/forum`), F6 gọn lại không còn hạ tầng riêng, D-Q1 giải thể. Backend F0/F1 và hợp đồng API F1 không đổi |
| 27/08/2026 | **F2 xong**: phân hệ `frontend-v2/src/modules/forum/` (layout một cột `customLayout: true` — cờ mới trong `module-definition.ts`, registry tách `moduleRoutes`/`customModuleRoutes`, gắn thẳng dưới `ProtectedRoute`); feed `useInfiniteQuery` con trỏ `before_id` + lính canh IntersectionObserver, thẻ bài (lưới ảnh + đèn chiếu + "Xem thêm"), nút "Có bài viết mới" (poll 45s khóa `feedHead` riêng), trang `/forum/posts/:id` (403/404 quy về một câu chung); `formatRelativeTime` vào `shared/utils/format-date.ts`, `/forum` vào `V2_PREFIXES` của notification-link. `npm run check` xanh (791 test); duyệt thật ở local 8083 tài khoản TESTREQ cả desktop lẫn mobile 375px (13 bài mẫu tạo qua đúng đường API F1: login → upload-file → đăng bài) |
| 27/08/2026 | **Chốt D-Q2**: `forum_admin` = vai trò RBAC seed sẵn (F0), gán qua màn phân quyền sẵn có — không cần cơ chế riêng. Câu hỏi mở giải thể |
| 27/08/2026 | **F3 xong**: hộp thoại đăng bài (`post-composer-dialog` — chữ + ảnh kéo thả/dán/chọn tệp, tải-trước-gắn-sau qua `uploadForumImages`, đếm ký tự gần trần 10.000, lọc tệp `pick-image-files` gom lỗi thành toast tiếng Việt), nhớ đối tượng xem lần trước (`last-audience` localStorage), xóa bài của mình (`post-actions-menu` — menu ba chấm theo `can_delete` + AlertDialog xác nhận), trang cá nhân `/forum/me` + `/forum/users/:id` dùng chung `forum-profile-page` (đầu trang: chính mình lấy auth store, người khác lấy từ bài đầu hoặc state của link nên mở tức thì), reset query `feed` + `userPostsAll` sau đăng/xóa. `npm run check` xanh (122 tệp / 801 test). Duyệt thật local 8083: đăng chữ + đăng 3 ảnh desktop, đăng 10 ảnh viewport mobile 375px không tràn ngang (điều kiện đủ 1), xóa bài chạy đúng, bài `dept` của TESTREQ (phòng 21) KHÔNG hiện với DEMO_STAFF phòng 14 (điều kiện đủ 2 — lưu ý DEMOTP/DEMOTP2/DEMONV cùng phòng 21 với TESTREQ trên DB local nên không dùng kiểm được) |
| 27/08/2026 | Sếp gợi ý **bài tự động theo sự kiện** (đổi avatar kèm caption, kiểu Facebook) — ghi thành **F10** đợt 2, có hỏi trước khi đăng, không ép |
| 27/08/2026 | **F10 xong** (kéo lên làm trước F4 theo lệnh sếp): backend thêm `ForumPostKind` + cột `kind` (`model/schema/service/controller`, migration `7889a09c627e` — autogenerate lôi drift ~20 bảng cũ kèm cả một lệnh drop cột thật của `tab_survey_product_line`, đã cắt sạch chỉ giữ `add_column kind`); service ép AVATAR_UPDATE đúng 1 ảnh (0 hay 2 đều 400), caption trống hợp lệ, kind lạ 400 — `test_forum_api.py` lên 16 bài. FE: `FORUM_POST_KIND` + `kind` vào types, `avatar-post-dialog.tsx` (xem trước ảnh tròn + caption + đối tượng xem nhớ lần trước, "Để sau"/"Đăng"), gắn vào **cả hai cửa đổi avatar**: `profile-identity-card.tsx` (`/me`) và `user-menu.tsx` (menu tài khoản mọi màn hình — cửa hay dùng nhất, suýt sót); `post-card.tsx` vẽ dòng "đã cập nhật ảnh đại diện" khi `kind=1` + test `post-card.test.tsx`. `npm run check` xanh (123 tệp / 803 test). Duyệt thật local 8083: đổi avatar cả hai cửa đều bật hộp thoại, đăng kèm caption lên feed đúng dạng (dòng hệ thống + caption + ảnh + Công ty), "Để sau" đóng không đăng gì |
| 27/08/2026 | **F4 xong**: like bài — `toggleForumPostLike`/`fetchForumPostLikes` vào `forum-api.ts`, hook `use-toggle-post-like` **vá thẳng cache** (`setQueryData` bài lẻ + `setQueriesData` cả hai infinite cache `feed`/`userPostsAll` — một bài hiện ở 3 chỗ, refetch cả ba mỗi cú bấm là giật cuộn nên KHÔNG invalidate), hộp "Người đã thích" (`post-likes-dialog` + `use-post-likes`, chỉ nạp khi mở), footer `post-card.tsx` thành hàng đếm + hai nút Thích/Bình luận kiểu Facebook; like KHÔNG chuông đúng D-Q6. Khối bình luận nhúng `post-comments.tsx` chạy trên bộ máy `/api/comments` sẵn có (`entity=forum_post`) với `forum-comment-api.ts` **khai lại type trong module forum** (luật cấm import chéo từ `modules/procurement`): bong bóng kiểu Facebook, nhắc tên qua `MentionInput` dùng chung, phản hồi 2 tầng nạp-khi-mở (state cục bộ), đính kèm ảnh + lightbox, like/xóa bình luận, "Xem N bình luận trước"; tạo/xóa bình luận invalidate cả `comments` lẫn `post` để số đếm trên thẻ bài khớp. Chuông dùng nguyên luật comment cũ, route registry `/forum/posts` đã sửa từ F1 nên link chuông điều hướng thẳng vào bài. `npm run check` xanh (123 tệp / 805 test). **Điều kiện đủ kiểm thật** local 8083 hai tài khoản TESTREQ/DEMOTP trên bài 11: TESTREQ bình luận → hộp chuông của CHÍNH TESTREQ trống (không tự báo mình), DEMOTP (tác giả) nhận đúng MỘT chuông "Bình luận mới" bấm vào ra đúng bài; DEMOTP bình luận nhắc tên TESTREQ → TESTREQ nhận đúng MỘT chuông "Bạn được nhắc tên" (không kèm chuông chung); phản hồi 2 tầng + like bài/bình luận + hộp ai thích đều chạy đúng trên UI |
| 27/08/2026 | **Chốt thiết kế thông báo hai tầng** (từ câu hỏi của sếp về chỗ hiện thông báo quan trọng/nghỉ lễ): tầng 1 = ghim bài + tab «Thông báo» (F9a/CR-199, kéo phần ghim của F9 lên làm ngay); tầng 2 = clone bài đã ghim từ luồng ban hành Văn thư (F12/CR-200, sếp chốt «clone thành 1 bài viết bên này là đúng logic hơn») — Văn thư đã có backend thật nên không còn bị chặn |
| 27/08/2026 | **F5 xong**: backend `service.moderate()` bảng chuyển trạng thái `_MOD_TRANSITIONS` (HIDE: published→hidden · RESTORE: hidden→published · REMOVE: published/hidden→removed), **bắt buộc lý do** trừ RESTORE (thiếu là 400), mỗi lần một dòng `tab_forum_moderation_log`, chuông cho tác giả kèm lý do (`_notify_author` — không tự báo mình, bài bị gỡ chuông không có link), map `hidden_reason_map` + cờ `can_moderate`/`hidden_reason` vào `PostOut`; 3 endpoint `hide/restore/remove` (`hide/restore` cần `forum_post.write`, `remove` cần `delete`); tác giả xóa vật lý bài đã REMOVED bị 404 để giữ vết. FE: `post-actions-menu` thêm nhánh quản trị (Ẩn/Khôi phục/Gỡ), `moderate-post-dialog` bắt nhập lý do mới cho bấm, nhãn "đã bị quản trị viên ẩn + lý do" trên thẻ bài, hook `use-moderate-forum-post` reset feed/trang cá nhân. `test_forum_api.py` lên 21 bài, `npm run check` xanh (123 tệp / 812 test). **Điều kiện đủ kiểm thật** local hai tài khoản (DEMOTP được gán `forum_admin` qua màn phân quyền): ẩn không lý do bị chặn ngay ở nút; ẩn bài 18 của TESTREQ → TESTREQ nhận đúng MỘT chuông kèm lý do, bài biến khỏi feed DEMONV nhưng còn ở trang cá nhân TESTREQ (kèm nhãn) và mắt admin; khôi phục qua UI về published |
| 27/08/2026 | **Chốt D-Q3 — cho video**: `FILE_POLICY` forum_post thành ảnh + `mp4`/`webm`, trần 10MB → **50MB/tệp** (trần chung, vẫn 10 tệp/bài). FE đổi `pick-image-files` → `pick-media-files` (`isVideoMedia` nhận diện theo content-type trước, đuôi tệp sau), hộp đăng bài nhận video (xem trước ô vuông câm), `post-image-grid` tách video đứng riêng dưới lưới ảnh nguyên khổ ngang + nút phát trình duyệt (không nhét vào lưới vì ô vuông cắt khung hình, đèn chiếu chỉ chiếu ảnh) |
| 27/08/2026 | **3 bài mẫu Hà Giang** đăng trên local qua đúng đường API (login → upload → đăng): bài 19 DEMOTP (đề xuất team-building Mã Pí Lèng — 1 video flycam + 2 ảnh), bài 20 TESTREQ (mùa tam giác mạch + Sủng Là, 4 ảnh), bài 23 DEMONV (Lũng Cú + hẻm Tu Sản, 2 ảnh). Media lấy từ Pexels (giấy phép miễn phí thương mại); video HD 16.8MB đi lọt cổng 50MB — chứng minh luôn đường mp4 mới chạy thật. Sếp gợi ý **cho người đăng tự chọn bố cục ảnh** kiểu Facebook/Instagram → ghi thành **F11** đợt 2 (chưa chốt làm) |
| 27/08/2026 | **Thumbnail sinh lúc upload** (sếp phát hiện feed kéo ảnh full-size + avatar nhòe vì không có CDN resize): `core/images.py` (`make_thumb` — Pillow sẵn trong ảnh api, EXIF transpose, nền trắng cho PNG trong suốt, bỏ qua khi nén không nhẹ hơn; GIF cố ý đứng ngoài kẻo mất chuyển động), cột `thumb_key`/`thumb_url` vào `tab_file` (migration tay `c3d9e14a58b7`, rỗng = FE fallback `url`), upload sinh bản `{file_key}.thumb.jpg` cạnh 1280 (avatar 320 — `set_user_avatar`), `User.avatar` ưu tiên thumb, xóa file dọn cả thumb. FE: `thumb_url` vào `ForumImage`/`ForumUploadedFile`, lưới feed + ô xem trước composer đọc thumb, đèn chiếu vẫn bản gốc. **Bẫy đã dính:** boto3 `upload_fileobj` ĐÓNG fileobj sau khi đẩy → phải sinh thumb TRƯỚC khi upload bản gốc (`make_thumb_for` tách khỏi `attach_thumb`); và uvicorn `--reload` desync bind-mount Windows không nạp code mới — upload đầu ra thumb rỗng, `docker compose restart api` mới ăn. Backfill một lần cho 57 tệp cũ (ảnh diễn đàn + avatar). Test `test_thumbnail.py` 5 bài; `npm run check` xanh (123 tệp / 812 test). Kiểm thật: feed 20/20 ảnh đọc `.thumb.jpg` |
| 27/08/2026 | **Làm mới data mẫu local**: xóa vật lý 17 bài mẫu cũ (giữ bộ Hà Giang 19/20/23) qua `service.delete_post`; đăng 3 bài du lịch mới bằng đúng đường API — bài 24 DEMO_MANAGER_PURCHASE (Đà Lạt, 4 ảnh), bài 25 TESTREQ (Hội An, 3 ảnh), bài 26 DEMONV (Sapa, 3 ảnh — audience 3 vì DEMONV không có pháp nhân), ảnh Pexels; tạo tương tác chéo 4 tài khoản: 15 like bài, 14 bình luận (có 2 tầng phản hồi + 2 nhắc tên `@[id]`), 6 like bình luận. Lưu ý: mật khẩu nhóm `DEMO_*` là `demo123` (không phải = mã như TESTREQ/DEMONV/DEMOTP) |
| 27/08/2026 | **F9a xong (CR-199) — ghim bài + tab «Thông báo»**: backend cột `pinned_at` DATETIME NULL trên `tab_forum_post` (migration tay `b8d4e6f1a2c9` — KHÔNG autogenerate vì drift cũ), `service.list_pinned_posts` (admin thấy cả bài ẩn đang ghim, người thường đi qua `_visible_cond` — bài `dept` ghim vẫn vô hình với phòng khác) + `set_post_pinned` (chỉ ghim bài published — ghim bài ẩn 400; ghim lại không làm mới mốc; trần `MAX_PINNED=50`), 3 endpoint `GET /posts/pinned` (đăng TRƯỚC `/posts/{pid}` — luật route tĩnh trước route động) + `POST /posts/{pid}/pin·unpin` (quyền `forum_post.write` của F5, không quyền mới, không log kiểm duyệt/chuông — ghim là quảng bá không phải chế tài), `pinned_at` vào `PostOut`. FE: hook `use-pinned-posts` + `use-pin-forum-post`, util chung `patch-post-caches.ts` (rút từ use-forum-comments — MỘT đường vá 4 cache: bài lẻ, feed, trang cá nhân, dải ghim; like/delete/moderate đã rà lại đủ nhánh pinned), dải «Thông báo» đầu Bảng tin (tối đa 2 bài + «Xem tất cả», bài lên dải rút khỏi dòng thời gian), tab «Thông báo» (`forum-announcements-page`) liệt kê mọi bài ghim, nhãn «Đã ghim» + mục Ghim/Bỏ ghim trong menu ba chấm (bài ẩn không có mục Ghim). `test_forum_api.py` lên 24 bài, `npm run check` xanh (131 tệp / 899 test). Kiểm thật local 8083 tài khoản DEMOTP (`forum_admin`): ghim bài 26 qua menu → toast + nhãn + dải; ghim 3 bài → dải giữ 2 + «Xem tất cả», bài thứ 3 nằm dòng thời gian kèm nhãn; tab Thông báo đủ 3 bài đúng thứ tự mới ghim trước; bỏ ghim qua menu → dải biến mất, bài vẫn nằm dòng thời gian |
| 27/08/2026 | **F12 xong (CR-200) — clone bài thông báo từ Văn thư khi ban hành**: hộp thoại Ban hành (F13 Văn thư) thêm ô «Đăng thông báo lên diễn đàn» (mặc định TẮT — không phải văn bản nào cũng đáng một bài ghim); tích thì backend clone văn bản vừa ban hành thành MỘT bài diễn đàn ĐÃ GHIM đứng tên người ban hành, đối tượng xem TOÀN TẬP ĐOÀN (tiêu đề + số hiệu + hiệu lực + link tuyệt đối về `/document/documents/{id}?readonly=1` — PostBody chỉ linkify URL có `https://`), dùng lại nguyên `create_post` + `set_post_pinned` của F9a. Backend: cờ `forum_announce` trên `ApproveIn` (schema), hàm `create_forum_announcement` trong `document/issue_notification.py` (import forum trong thân hàm — ghép lỏng), móc trong `controller.approve_document` bọc try/except best-effort (diễn đàn hỏng KHÔNG kéo đổ việc ban hành, chỉ ghi log; audit thêm đuôi «đăng thông báo lên diễn đàn» khi đăng thành công). FE: checkbox + Megaphone trong `document-issue-dialog.tsx` (reset khi đóng), nối `onConfirm` → `use-documents.approve` → `document-api.approve` (`forum_announce`). Test mới `test_dang_thong_bao_dien_dan_khi_ban_hanh.py` 4 bài (clone đúng ghim/PUBLIC/tên người ban hành/link + mặc định tắt không đẻ bài + diễn đàn lỗi vẫn ban hành xong + qua controller đúng một bài ghim), bộ 4 tệp liên quan 47 bài xanh; `npm run check` xanh (131 tệp / 899 test). Kiểm thật local 8083 tài khoản DEMOQL: ban hành văn bản `01/2026/TL-DEGO` có tích ô → bài ghim hiện đúng dải THÔNG BÁO trên feed (nhãn Đã ghim + Toàn tập đoàn + số hiệu + link), toast «Đã ban hành 01/2026/TL-DEGO»; dữ liệu thử đã dọn |
| 03/09/2026 | **QĐ-D7 — tab «Diễn đàn» kiểu chuyên mục VOZ** ghi thành F13a (backend board/thread) + F13b (FE 2 màn + tab). Ba lựa chọn thiết kế a/b/c chốt ở bảng QĐ của `00`. Cùng ngày bổ sung theo góp ý của sếp (soi màn hình VOZ): cột `icon` cho box, bộ đếm thread + bình luận, khối bài-mới-nhất trong `GET /boards`, và nguyên tắc kiểm duyệt (admin quản box thủ công, bài tự hiện không duyệt trước, hậu kiểm F5, `require_approval` để dành F8). Thêm F13c: sidebar phải 3 khối (Nổi bật=bài ghim · Đang sôi nổi=auto 7 ngày · Mới nhất). **Sticker: KHÔNG làm** — sếp chốt dùng emoji là đủ, chỉ rà/thêm nút chọn emoji ở composer + bình luận (gộp vào F13c). Chưa code — phase kế tiếp của diễn đàn sau F6 |
| 03/09/2026 | **F13a xong (CR-269)**: `tab_forum_board` + 3 cột mới `tab_forum_post` (migration `e7b3c9d41f28`, revises `c3a91d47f2b8`); service/controller/schema mở rộng (`GET /boards` cây + bộ đếm + bài-mới-nhất, `GET /boards/{id}/threads` phân trang số sắp theo hoạt động cuối viết bằng CASE thay GREATEST cho SQLite của bộ test, CRUD box, `POST /posts` nhận board_id+title+prefix); entity `forum_board` vào ENTITIES/SCOPE_FIELDS/ENTITY_LABELS + gán `forum_admin` ở seed (B-07 46→47); bộ mã `forum_prefix` ở `core/forum_codes.py` (nhãn FE, `statuses.ts` đã sinh lại — bộ thứ 16); test mới `test/backend/test_forum_board.py` (15 bài, 68/68 xanh cùng bộ forum + B-07); từ điển `05e` thêm mục `tab_forum_board` + 3 cột. Kế tiếp: F13b (FE) rồi F13c (sidebar + emoji picker) |
| 03/09/2026 | **F13b xong (CR-269) — FE tab «Diễn đàn»**: backend bổ sung `last_activity_at` cho từng thread trong `GET /boards/{id}/threads` (helper `service.last_comment_at_map` — 1 query GROUP BY mỗi trang, controller ghép max(bài, comment cuối); `test_forum_board.py` lên 17 bài). FE: 2 màn mới `forum-boards-page` (box gom theo nhóm — `BoardIcon` map ~21 icon lucide chọn tay + fallback emoji ≤4 ký tự, KHÔNG import cả bộ `icons` kẻo vỡ tree-shaking; đếm "X chủ đề · Y bình luận" không format 1K+; khối bài-mới-nhất là Link ĐỨNG CẠNH Link chính chứ không lồng — HTML cấm `<a>` trong `<a>`; mobile giấu khối này giữ số đếm; badge «Đang ẩn» cho admin) + `forum-board-threads-page` (chip prefix màu `thread-prefix-chip` nhãn đọc từ `FORUM_PREFIX` sinh sẵn, ghim đứng đầu, phân trang số `?page=` trên URL + `keepPreviousData`); tab «Diễn đàn» trong `forum-layout` (`end=false` để sáng cả ở `/forum/boards/:id`; dải tab thêm `overflow-x-auto` giấu thanh cuộn — 4 tab hết lọt màn 375px, không sửa là cụt «Trang của tôi» + chuông); composer ngữ cảnh box thêm ô tiêu đề (trần 255) + chọn prefix, ẨN ô đối tượng xem (backend ép theo box QĐ-D7a), không đụng `last-audience`; thẻ bài thuộc box trên feed vẽ chip tên box + chip prefix + tiêu đề đậm (ngoài feed bấm mở thread, trong chi tiết là `<h2>`); breadcrumb `/forum/posts/:id` đổi về box khi `board_id > 0`. Khóa query `forum.boards()` là GỐC nhánh boards nên một invalidate sau khi đăng quét cả cây box lẫn mọi trang thread. `post-card.test.tsx` thêm 2 ca (36 test forum xanh); typecheck + lint 0 lỗi — 10 test đỏ ở `payable-list-page.test.tsx` là việc DỞ DANG của phiên «thanh toán trước» chạy song song (mock thiếu `useOffsetPrepay`), không thuộc CR này. Duyệt thật local 8083 (TESTREQ): seed 2 nhóm + 4 box + 1 box ẩn — box ẩn không hiện với người thường; tạo thread từ box «Góc chia sẻ» (prefix Thảo luận) → thấy ở cả thread list lẫn Bảng tin kèm chip + tiêu đề; bình luận → bộ đếm box thành "1 chủ đề · 1 bình luận" + khối bài-mới-nhất cập nhật; mobile 375px `scrollW == innerW` không tràn ngang. Kế tiếp: F13c |
| 03/09/2026 | **Tab «Diễn đàn» thành tab MẶC ĐỊNH** (sếp chốt: «diễn đàn sẽ luôn mặc định trước»): `/forum` redirect về `/forum/boards`, Bảng tin dời sang `/forum/feed`; thứ tự tab Diễn đàn → Bảng tin → Thông báo → Trang của tôi (`app-routes.ts` + `routes.tsx` + `forum-layout.tsx`; các đường quay-về sau xóa/gỡ bài đổi theo qua helper `afterLeaveTarget`). Gộp vào CR-271 |
| 03/09/2026 | **F13c xong (CR-271) — sidebar phải + emoji picker**: backend `service.list_highlight_threads` (trending = điểm bình luận + reaction 7 ngày, 2 query GROUP BY gộp dict; latest = thread mới; cả hai lọc qua `_visible_cond` + box ẩn rơi theo nhóm, bài feed không board tự loại) + endpoint `GET /boards/highlights` đăng TRƯỚC nhóm `/boards/{id}` — không bảng mới, không migration; `test_forum_board.py` 17→19 bài. FE: `boards-sidebar.tsx` (3 SidebarCard: Nổi bật=bài ghim dùng `use-pinned-posts` sẵn có · Đang sôi nổi meta "box · N bình luận" · Mới nhất meta "box · thời gian"; khối rỗng tự ẩn, cả ba rỗng thì sidebar biến hẳn; chip prefix nằm TRONG span `line-clamp-2` kẻo `-webkit-box` đẩy tiêu đề xuống dòng riêng), màn boards nới `max-w-5xl` + grid `1fr_300px` từ `lg:`; mobile ẩn sidebar, `PinnedSpotlight` dồn Nổi bật lên đầu trang (không thêm lượt gọi API — TanStack dedupe theo khóa; khóa `boardHighlights` nằm dưới gốc `forum.boards` nên invalidate sau đăng bài quét luôn). Emoji: `emoji-picker-button.tsx` ~40 emoji dựng từ CODE POINT (luật cấm emoji literal trong mã nguồn), gắn vào composer (`insertEmoji` splice theo `selectionStart` của Textarea) + ô bình luận (`MentionInput` thêm `insertText` vào handle, cùng đường `execCommand` với dán chữ) — KHÔNG sticker theo lệnh sếp. Test FE +4 (`boards-sidebar.test.tsx`), forum 40 bài xanh, typecheck + lint 0 lỗi. Duyệt thật local 8083 (DEMOQL): redirect + thứ tự tab đúng; sidebar hiện Đang sôi nổi + Mới nhất, Nổi bật tự ẩn vì chưa có bài ghim; emoji chèn đúng giữa câu ở cả ô bình luận (đã gửi thật) lẫn hộp đăng bài; mobile 375px không tràn ngang. **Đợt F13 (a+b+c) đóng** — diễn đàn kiểu VOZ đủ bộ, kế là F6 deploy dev |
| 03/09/2026 | **CR-261 xong — rich text cho bài viết (cả Bảng tin lẫn box)**: sếp đưa màn hình Facebook chứng minh bài feed thực tế cũng có đậm nhạt + đánh số → đảo quyết định «đợt 1 chữ thuần» (bảng QĐ của `01` mục 7). Backend: `ForumBodyFormat` IntEnum (0=PLAIN 1=RICH_HTML) + cột `body_format` SMALLINT server_default 0 (migration tay `f8b3d6a2c714` — không autogenerate vì drift cũ), `create_post` nhận `body_format`: rich thì đo trần 40.000 markup TRƯỚC rồi `sanitize_html` của Help Center (MỘT bộ lọc dùng chung với Văn thư — script/on*/javascript:/srcdoc rụng ngay cửa ghi), rich toàn thẻ rỗng chuẩn hóa `body=""` (`strip_html_text`) — rỗng + không tệp vẫn 400; format lạ 400; bài chữ thuần đi nguyên đường cũ (`test_forum_board.py` 19→24 bài). FE: composer cả hai ngữ cảnh (feed + box) đổi Textarea → `RichTextField` dùng chung với văn thư (toolbar dưới, đậm/nghiêng/gạch, danh sách chấm + số, trích dẫn, canh lề), emoji chèn tại con trỏ qua handle mới `RichTextFieldHandle.insertText` (useImperativeHandle đặt TRƯỚC early-return `if (!editor)` — luật thứ tự hook); dán/kéo TỆP chặn ở capture-phase (`onPasteCapture`/`onDropCapture` + stopPropagation kẻo ProseMirror nuốt ảnh thành base64 — giữ đường tải-trước-gắn-sau), dán CHỮ có định dạng vẫn vào editor; `PostBody` hai nhánh theo `body_format` (rich = DOMPurify `sanitizeHtml` + class `doc-excerpt-preview` ăn chung luật hiển thị với văn thư, «Xem thêm» đo theo chữ đã bóc thẻ >400 hoặc >8 thẻ khối; plain = nguyên đường splitByUrls cũ); nhãn bài ghim ở sidebar bóc thẻ bằng `stripRichBodyText` (CHỈ gọi cho bài rich — gọi cho bài plain thì `a < b` bị regex ăn mất). Test FE +8 (`rich-body.test.ts` mới 4 + post-card 3 + boards-sidebar 1, forum 48 bài xanh), typecheck + lint 0 lỗi. Duyệt thật local 8083 (TESTREQ): đăng bài feed có đánh số + đậm + emoji → thẻ feed vẽ đúng marker `1.` + chữ đậm, DB lưu `body_format=1` HTML sạch; bài cũ plain không đổi; phím Return không tới được contenteditable là tật của driver trình duyệt (đã ghi nhận từ F13c), không phải lỗi mã |
| 27/08/2026 | **Chi tiết bài viết mở dạng popup** (sếp yêu cầu kiểu Facebook — bấm ra ngoài là đóng, vẫn đứng nguyên vị trí cuộn thay vì mất dấu như khi điều hướng sang trang riêng): `PostDetailDialog` nội bộ trong `post-card.tsx` (bài + toàn bộ bình luận trong dialog cuộn được, tối đa 90svh, tiêu đề "Bài viết của X"); 3 lối vào chi tiết trên thẻ feed (thời gian, số bình luận, nút Bình luận) đổi từ `Link` sang nút mở popup — áp cho cả bảng tin lẫn trang cá nhân vì nằm ngay trong `PostCard`. Thêm prop `flat` cho `PostCard`/`PostComments` (bỏ viền/bóng khi nằm trong dialog); trong popup thao tác xóa/gỡ KHÔNG điều hướng (bài biến khỏi cache là popup tự đóng). Trang riêng `/forum/posts/:id` GIỮ NGUYÊN cho link chia sẻ + thông báo chuông. `post` truyền thẳng từ cache feed nên like/bình luận trong popup cập nhật ngược ra thẻ ngoài ngay. Test `post-card.test.tsx` thêm 2 ca (11 ca xanh); typecheck + lint sạch. Kiểm thật trên local: mở popup giữ nguyên URL `/forum`, đóng bằng click overlay, scrollY giữ nguyên 1200 |
| 03/09/2026 | **CR-263 xong — tab «Quản trị» + tìm kiếm cho MỌI người** (phần *tìm kiếm* của F9 đóng ở đây, F9 chỉ còn sửa bài): backend 3 endpoint đọc, KHÔNG migration — `GET /posts/search` (từ khóa title+body · người tạo LIKE tên/mã NV/email · `company_id`/`dept_id` đóng băng trên bài · phân trang số; người thường ÉP qua `_visible_cond` ngay trong SQL, tham số `status` chỉ người có `forum_post.write` dùng được, người thường truyền là lờ) · `GET /search/filters` (distinct công ty/phòng ban đã có bài) · `GET /moderation-logs` (bảng ghi từ F5 giờ mới có màn đọc). FE: `/forum/admin` 3 tab con (Chuyên mục = dựng cây nhóm-box trên CRUD F13a qua `board-form-dialog`, chỉ xóa nhóm/box RỖNG · Bài ghim bỏ ghim tại chỗ · Nhật ký kiểm duyệt badge Ẩn/Gỡ/Khôi phục) — tab chỉ hiện khi có grant, route thiếu quyền tự đá về `/forum/boards`; `/forum/search` nút kính lúp cạnh chuông cho mọi người, form nháp-tách-áp-dụng, kết quả vẽ `PostCard` sẵn có nên kiểm duyệt được ngay trên kết quả (`use-moderate-forum-post` invalidate thêm `searchAll`/`moderationLogsAll`). KHÔNG làm quản lý comment (khách chốt: có quyền thì vào bài xóa). Test `test_forum_search.py` 9 ca + FE `forum-api.test.ts` 3 ca (hợp đồng: bộ lọc rỗng/0 không gửi lên URL); `npm run check` xanh 1950 test. Duyệt thật local hai tài khoản: DEMOQL không thấy tab + bị đá route + tìm "cuối tuần" ra 2 bài đúng audience; admin dựng box, ghim/bỏ ghim, ẩn có lý do vào nhật ký, tìm bài Đang ẩn rồi khôi phục ngay trên kết quả |
| 03/09/2026 | **bao-CR-272 xong — ô tìm nhìn thấy được + bộ lọc Quản trị + phóng to khung đọc/soạn** (khách chê: không thấy khung search, Quản trị không có filter, popup đăng bài nhỏ, khung bài đọc nhỏ, chữ nhỏ, hai bên trống): **①** Header có `Input` tìm thật từ ≥md (màn hẹp giữ icon cũ), Enter nhảy `/forum/search?q=...` — bẫy HTML: form MỘT input KHÔNG có nút submit thì Enter bị bỏ qua (implicit submission), phải kèm `<button type="submit" class="sr-only">`; trang Tìm nhận `?q=` rồi CHẠY LUÔN (chỉnh state ngay trong render qua khuôn `seenUrlQ`, không useEffect kẻo dính luật lint `set-state-in-effect`). **②** Quản trị đủ 3 tab có lọc: Chuyên mục lọc client tên/mô tả (nhóm khớp giữ cả cây con, nhóm rỗng sau lọc tự rụng) · Bài ghim lọc tiêu đề/người đăng · Nhật ký kiểm duyệt lọc BACKEND `action` + `q` (LIKE `escape_like`, khớp lý do HOẶC tiêu đề/nội dung bài qua `scalar_subquery`); Select đổi là lọc ngay, ô chữ bấm Lọc; khóa query `moderationLogs(page, action, q)`. **③** Composer gần-toàn-màn `sm:max-w-3xl lg:max-w-5xl` + `max-h-[92svh]` flex-col (thân cuộn giữa, nút Đăng đứng yên), vùng gõ tiptap `45svh–58svh` chữ 15px — bẫy CSS: luật `.doc-rich-field` ở `index.css` nằm NGOÀI `@layer` nên đè mọi utility bất kể specificity, override phải mang `!`; chốt với khách: to popup trước, trang soạn riêng chỉ làm nếu vẫn chật. **④** Bề ngang theo route: boards/Quản trị `max-w-7xl`, trang đọc bài `max-w-4xl`, feed còn lại `max-w-3xl`; trang đọc tiêu đề `text-xl` + thân 17px/1.7 + BỎ «Xem thêm» (prop `detail` trên `PostBody`); boards tên box `text-base`, khối bài-mới-nhất `w-72`. Cổng: typecheck + lint 0, vitest forum 51 xanh, pytest forum 64 xanh. Duyệt thật local 8083 (admin): submit ô tìm → `?q=Tô Lâm` tự chạy ra 1 bài; 3 bộ lọc Quản trị chạy đúng (log bắn `action=3&q=quảng cáo` lên API); composer 1024px vùng gõ 360px; trang bài 896px chữ 17px không còn nút Xem thêm |
| 03/09/2026 | **bao-CR-273.1 xong — ô tìm header sổ gợi ý top 5 khi gõ** *(số gốc 273 trùng với CR trợ lý AI cùng ngày, đổi đuôi theo luật change-log)* (khách chỉnh UX của bao-CR-272: "search thì sổ top 5/top 10 xuống chứ, bấm show full hoặc lọc nâng cao mới vào page search"): tách ô tìm thành `forum-header-search.tsx` — gõ đủ 2 ký tự là dropdown top 5 bài khớp (tiêu đề thread hoặc thân bài bóc `stripRichBodyText`, meta người đăng · box), bấm bài nhảy thẳng `/forum/posts/:id`, mũi tên + Enter chọn được bằng phím; Enter/«Xem tất cả N kết quả»/«Bộ lọc nâng cao» mới qua `/forum/search?q=` tự chạy như cũ. Hook `use-forum-quick-search.ts` TỰ debounce 250ms, khóa cache thêm `quick` (vẫn dưới tiền tố `['forum','search']` cho kiểm duyệt invalidate cả cụm); backend KHÔNG đổi — `GET /posts/search` sẵn `per_page`, FE chỉ thêm vào `ForumSearchParams`. Bẫy: đóng dropdown phải check `relatedTarget` còn trong cụm không — bấm nút trong dropdown là một lần blur input, đóng ngay thì click không kịp ăn. Cổng: typecheck + lint 0, vitest forum 57 xanh (6 test mới). Duyệt thật local 8083: gõ «Tô Lâm» sổ 1 gợi ý đúng bấm ra `/forum/posts/30`, «Xem tất cả 2 kết quả» ra trang Tìm chạy sẵn, request mang đúng `per_page=5` |
| 03/09/2026 | **bao-CR-275-anh-bai-rich xong — bài rich dán từ web: chú thích ảnh ra đúng kiểu + bấm ảnh trong thân bài phóng to** (khách dán bài VnExpress vào rồi chê "chữ in nghiêng nhỏ không parse ra đúng" + "ảnh vào được mà không bấm to ra được"): **①** thêm luật `figure`/`figcaption` cho `.doc-excerpt-preview` (index.css) — chú thích nghiêng, `--muted-foreground`, 0.85em, căn giữa; KHÔNG bám `[data-editor-image]` vì sanitize server lột thuộc tính data-*, HTML lưu về chỉ còn `<figure>` trần; vá CSS nên ăn ngược mọi bài đã lưu. Ghi nhận không sửa: caption của `ImageWithSize` lưu `textContent` nên `<em>` trong chú thích gốc rơi từ lúc dán (by design — caption sửa bằng textarea trơn). **②** `PostBody` nhánh rich nối vào `ImageLightbox`/`useImageLightbox` dùng chung (NF-10, cùng bộ với lưới ảnh đính kèm): click ỦY QUYỀN trên khung (HTML vẽ bằng `dangerouslySetInnerHTML` không gắn handler từng img được), gom img từ DOM lúc bấm, mở đúng ảnh được bấm, con trỏ `cursor-zoom-in`. Test `post-body.test.tsx` mới 3 ca (giữ chữ figcaption · bấm ảnh 2 mở đúng 2/2 · bấm chữ trơn không mở); cổng typecheck + lint 0, vitest forum 60 xanh. Duyệt thật local 8083 post 30: chú thích nghiêng xám 14.45px căn giữa, bấm ảnh VnExpress mở lightbox nền đen đúng ảnh |
