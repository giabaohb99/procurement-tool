# Help Center — app Hướng dẫn sử dụng (React + Vite, dev server + HMR)
FROM node:20-alpine

WORKDIR /app

COPY help-center/package.json /app/package.json
RUN npm install

COPY help-center/ /app/

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
